**/luau_packages/
**/lune_packages/
**/roblox_packages/

/.pesde
*.rbxl.lock

.rodeo/.temp

# pkgTests.ts artifacts in repo root (test bodies write here via fs.open and
# rely on fs.remove to clean up — if a test errors before the cleanup line,
# the file lingers; ignoring keeps it out of commits).
/rodeo-test-*

# Build artifacts
/build
/rodeo-plugin/build/
/bin/
/.claude/*
!/.claude/skills/
/.plans
/.vscode
/.scratch/

# Rust
/target/
/Cargo.lock
/rodeo-cli/target/
node_modules

.DS_Store

*/.temp

.env
/out

/rodeo-cli/target

# Local reference clones (Luau ecosystem projects pulled for docs reference)
/submodules/lute/
/submodules/lune/
.testlogs/
