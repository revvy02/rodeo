---
// Empty placeholder — replaces Starlight's built-in theme selector so the
// site stays dark-only. The CSS in src/styles/custom.css forces dark tokens
// regardless of which data-theme is set.
---
