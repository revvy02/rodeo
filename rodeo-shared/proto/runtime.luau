--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require("./proto")
local typeRegistry = require("./proto/typeRegistry")

type _Messages = {
	Ok: _OkMessage,
	StreamOpenRequest: _StreamOpenRequestMessage,
	StreamOpenResponse: _StreamOpenResponseMessage,
	StreamReadChunkRequest: _StreamReadChunkRequestMessage,
	StreamReadChunkResponse: _StreamReadChunkResponseMessage,
	StreamReadLineRequest: _StreamReadLineRequestMessage,
	StreamReadLineResponse: _StreamReadLineResponseMessage,
	StreamReadAllRequest: _StreamReadAllRequestMessage,
	StreamReadAllResponse: _StreamReadAllResponseMessage,
	StreamWriteRequest: _StreamWriteRequestMessage,
	StreamReadBytesRequest: _StreamReadBytesRequestMessage,
	StreamReadBytesResponse: _StreamReadBytesResponseMessage,
	StreamWriteBytesRequest: _StreamWriteBytesRequestMessage,
	StreamCloseRequest: _StreamCloseRequestMessage,
	FsExistsRequest: _FsExistsRequestMessage,
	FsExistsResponse: _FsExistsResponseMessage,
	FsStatRequest: _FsStatRequestMessage,
	FsStatResponse: _FsStatResponseMessage,
	FsTypeRequest: _FsTypeRequestMessage,
	FsTypeResponse: _FsTypeResponseMessage,
	FsMkdirRequest: _FsMkdirRequestMessage,
	FsListdirRequest: _FsListdirRequestMessage,
	FsListdirResponse: _FsListdirResponseMessage,
	FsDirEntry: _FsDirEntryMessage,
	FsRemoveRequest: _FsRemoveRequestMessage,
	FsRmdirRequest: _FsRmdirRequestMessage,
	FsCopyRequest: _FsCopyRequestMessage,
	ProcessOptions: _ProcessOptionsMessage,
	ProcessGetInfoRequest: _ProcessGetInfoRequestMessage,
	ProcessGetInfoResponse: _ProcessGetInfoResponseMessage,
	ProcessGetInfoResponse_EnvEntry: _ProcessGetInfoResponse_EnvEntryMessage,
	ProcessExitRequest: _ProcessExitRequestMessage,
	ProcessRunRequest: _ProcessRunRequestMessage,
	ProcessRunResponse: _ProcessRunResponseMessage,
	ProcessSystemRequest: _ProcessSystemRequestMessage,
	ProcessCreateRequest: _ProcessCreateRequestMessage,
	ProcessCreateResponse: _ProcessCreateResponseMessage,
	ProcessRunHandleRequest: _ProcessRunHandleRequestMessage,
	ProcessKillRequest: _ProcessKillRequestMessage,
	McpCallRequest: _McpCallRequestMessage,
	McpCallResponse: _McpCallResponseMessage,
	ClientRpcCall: _ClientRpcCallMessage,
	ClientRpcResponse: _ClientRpcResponseMessage,
}
local messages: _Messages = {} :: _Messages

type _OkImpl = {
	__index: _OkImpl,
	new: (fields: _OkPartialFields?) -> Ok,
	encode: (self: Ok) -> buffer,
	decode: (input: buffer) -> Ok,
	jsonEncode: (self: Ok) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> Ok,
	descriptor: proto.Descriptor,
}

type _OkFields = {}

export type _OkPartialFields = {}

export type Ok = typeof(setmetatable({} :: _OkFields, {} :: _OkImpl))
type _OkMessage = proto.Message<Ok, _OkPartialFields>

type _StreamOpenRequestImpl = {
	__index: _StreamOpenRequestImpl,
	new: (fields: _StreamOpenRequestPartialFields?) -> StreamOpenRequest,
	encode: (self: StreamOpenRequest) -> buffer,
	decode: (input: buffer) -> StreamOpenRequest,
	jsonEncode: (self: StreamOpenRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamOpenRequest,
	descriptor: proto.Descriptor,
}

type _StreamOpenRequestFields = {
	handle: string,
	path: string,
	mode: string,
}

export type _StreamOpenRequestPartialFields = {
	handle: string?,
	path: string?,
	mode: string?,
}

export type StreamOpenRequest = typeof(setmetatable({} :: _StreamOpenRequestFields, {} :: _StreamOpenRequestImpl))
type _StreamOpenRequestMessage = proto.Message<StreamOpenRequest, _StreamOpenRequestPartialFields>

type _StreamOpenResponseImpl = {
	__index: _StreamOpenResponseImpl,
	new: (fields: _StreamOpenResponsePartialFields?) -> StreamOpenResponse,
	encode: (self: StreamOpenResponse) -> buffer,
	decode: (input: buffer) -> StreamOpenResponse,
	jsonEncode: (self: StreamOpenResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamOpenResponse,
	descriptor: proto.Descriptor,
}

type _StreamOpenResponseFields = {
	handle: string,
}

export type _StreamOpenResponsePartialFields = {
	handle: string?,
}

export type StreamOpenResponse = typeof(setmetatable({} :: _StreamOpenResponseFields, {} :: _StreamOpenResponseImpl))
type _StreamOpenResponseMessage = proto.Message<StreamOpenResponse, _StreamOpenResponsePartialFields>

type _StreamReadChunkRequestImpl = {
	__index: _StreamReadChunkRequestImpl,
	new: (fields: _StreamReadChunkRequestPartialFields?) -> StreamReadChunkRequest,
	encode: (self: StreamReadChunkRequest) -> buffer,
	decode: (input: buffer) -> StreamReadChunkRequest,
	jsonEncode: (self: StreamReadChunkRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamReadChunkRequest,
	descriptor: proto.Descriptor,
}

type _StreamReadChunkRequestFields = {
	handle: string,
	size: number?,
}

export type _StreamReadChunkRequestPartialFields = {
	handle: string?,
	size: number?,
}

export type StreamReadChunkRequest = typeof(setmetatable(
	{} :: _StreamReadChunkRequestFields,
	{} :: _StreamReadChunkRequestImpl
))
type _StreamReadChunkRequestMessage = proto.Message<StreamReadChunkRequest, _StreamReadChunkRequestPartialFields>

type _StreamReadChunkResponseImpl = {
	__index: _StreamReadChunkResponseImpl,
	new: (fields: _StreamReadChunkResponsePartialFields?) -> StreamReadChunkResponse,
	encode: (self: StreamReadChunkResponse) -> buffer,
	decode: (input: buffer) -> StreamReadChunkResponse,
	jsonEncode: (self: StreamReadChunkResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamReadChunkResponse,
	descriptor: proto.Descriptor,
}

type _StreamReadChunkResponseFields = {
	data: string,
	eof: boolean,
}

export type _StreamReadChunkResponsePartialFields = {
	data: string?,
	eof: boolean?,
}

export type StreamReadChunkResponse = typeof(setmetatable(
	{} :: _StreamReadChunkResponseFields,
	{} :: _StreamReadChunkResponseImpl
))
type _StreamReadChunkResponseMessage = proto.Message<StreamReadChunkResponse, _StreamReadChunkResponsePartialFields>

type _StreamReadLineRequestImpl = {
	__index: _StreamReadLineRequestImpl,
	new: (fields: _StreamReadLineRequestPartialFields?) -> StreamReadLineRequest,
	encode: (self: StreamReadLineRequest) -> buffer,
	decode: (input: buffer) -> StreamReadLineRequest,
	jsonEncode: (self: StreamReadLineRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamReadLineRequest,
	descriptor: proto.Descriptor,
}

type _StreamReadLineRequestFields = {
	handle: string,
}

export type _StreamReadLineRequestPartialFields = {
	handle: string?,
}

export type StreamReadLineRequest = typeof(setmetatable(
	{} :: _StreamReadLineRequestFields,
	{} :: _StreamReadLineRequestImpl
))
type _StreamReadLineRequestMessage = proto.Message<StreamReadLineRequest, _StreamReadLineRequestPartialFields>

type _StreamReadLineResponseImpl = {
	__index: _StreamReadLineResponseImpl,
	new: (fields: _StreamReadLineResponsePartialFields?) -> StreamReadLineResponse,
	encode: (self: StreamReadLineResponse) -> buffer,
	decode: (input: buffer) -> StreamReadLineResponse,
	jsonEncode: (self: StreamReadLineResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamReadLineResponse,
	descriptor: proto.Descriptor,
}

type _StreamReadLineResponseFields = {
	data: string,
	eof: boolean,
}

export type _StreamReadLineResponsePartialFields = {
	data: string?,
	eof: boolean?,
}

export type StreamReadLineResponse = typeof(setmetatable(
	{} :: _StreamReadLineResponseFields,
	{} :: _StreamReadLineResponseImpl
))
type _StreamReadLineResponseMessage = proto.Message<StreamReadLineResponse, _StreamReadLineResponsePartialFields>

type _StreamReadAllRequestImpl = {
	__index: _StreamReadAllRequestImpl,
	new: (fields: _StreamReadAllRequestPartialFields?) -> StreamReadAllRequest,
	encode: (self: StreamReadAllRequest) -> buffer,
	decode: (input: buffer) -> StreamReadAllRequest,
	jsonEncode: (self: StreamReadAllRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamReadAllRequest,
	descriptor: proto.Descriptor,
}

type _StreamReadAllRequestFields = {
	handle: string,
}

export type _StreamReadAllRequestPartialFields = {
	handle: string?,
}

export type StreamReadAllRequest = typeof(setmetatable(
	{} :: _StreamReadAllRequestFields,
	{} :: _StreamReadAllRequestImpl
))
type _StreamReadAllRequestMessage = proto.Message<StreamReadAllRequest, _StreamReadAllRequestPartialFields>

type _StreamReadAllResponseImpl = {
	__index: _StreamReadAllResponseImpl,
	new: (fields: _StreamReadAllResponsePartialFields?) -> StreamReadAllResponse,
	encode: (self: StreamReadAllResponse) -> buffer,
	decode: (input: buffer) -> StreamReadAllResponse,
	jsonEncode: (self: StreamReadAllResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamReadAllResponse,
	descriptor: proto.Descriptor,
}

type _StreamReadAllResponseFields = {
	data: string,
}

export type _StreamReadAllResponsePartialFields = {
	data: string?,
}

export type StreamReadAllResponse = typeof(setmetatable(
	{} :: _StreamReadAllResponseFields,
	{} :: _StreamReadAllResponseImpl
))
type _StreamReadAllResponseMessage = proto.Message<StreamReadAllResponse, _StreamReadAllResponsePartialFields>

type _StreamWriteRequestImpl = {
	__index: _StreamWriteRequestImpl,
	new: (fields: _StreamWriteRequestPartialFields?) -> StreamWriteRequest,
	encode: (self: StreamWriteRequest) -> buffer,
	decode: (input: buffer) -> StreamWriteRequest,
	jsonEncode: (self: StreamWriteRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamWriteRequest,
	descriptor: proto.Descriptor,
}

type _StreamWriteRequestFields = {
	handle: string,
	data: string,
}

export type _StreamWriteRequestPartialFields = {
	handle: string?,
	data: string?,
}

export type StreamWriteRequest = typeof(setmetatable({} :: _StreamWriteRequestFields, {} :: _StreamWriteRequestImpl))
type _StreamWriteRequestMessage = proto.Message<StreamWriteRequest, _StreamWriteRequestPartialFields>

type _StreamReadBytesRequestImpl = {
	__index: _StreamReadBytesRequestImpl,
	new: (fields: _StreamReadBytesRequestPartialFields?) -> StreamReadBytesRequest,
	encode: (self: StreamReadBytesRequest) -> buffer,
	decode: (input: buffer) -> StreamReadBytesRequest,
	jsonEncode: (self: StreamReadBytesRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamReadBytesRequest,
	descriptor: proto.Descriptor,
}

type _StreamReadBytesRequestFields = {
	handle: string,
}

export type _StreamReadBytesRequestPartialFields = {
	handle: string?,
}

export type StreamReadBytesRequest = typeof(setmetatable(
	{} :: _StreamReadBytesRequestFields,
	{} :: _StreamReadBytesRequestImpl
))
type _StreamReadBytesRequestMessage = proto.Message<StreamReadBytesRequest, _StreamReadBytesRequestPartialFields>

type _StreamReadBytesResponseImpl = {
	__index: _StreamReadBytesResponseImpl,
	new: (fields: _StreamReadBytesResponsePartialFields?) -> StreamReadBytesResponse,
	encode: (self: StreamReadBytesResponse) -> buffer,
	decode: (input: buffer) -> StreamReadBytesResponse,
	jsonEncode: (self: StreamReadBytesResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamReadBytesResponse,
	descriptor: proto.Descriptor,
}

type _StreamReadBytesResponseFields = {
	data: buffer,
}

export type _StreamReadBytesResponsePartialFields = {
	data: buffer?,
}

export type StreamReadBytesResponse = typeof(setmetatable(
	{} :: _StreamReadBytesResponseFields,
	{} :: _StreamReadBytesResponseImpl
))
type _StreamReadBytesResponseMessage = proto.Message<StreamReadBytesResponse, _StreamReadBytesResponsePartialFields>

type _StreamWriteBytesRequestImpl = {
	__index: _StreamWriteBytesRequestImpl,
	new: (fields: _StreamWriteBytesRequestPartialFields?) -> StreamWriteBytesRequest,
	encode: (self: StreamWriteBytesRequest) -> buffer,
	decode: (input: buffer) -> StreamWriteBytesRequest,
	jsonEncode: (self: StreamWriteBytesRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamWriteBytesRequest,
	descriptor: proto.Descriptor,
}

type _StreamWriteBytesRequestFields = {
	handle: string,
	data: buffer,
}

export type _StreamWriteBytesRequestPartialFields = {
	handle: string?,
	data: buffer?,
}

export type StreamWriteBytesRequest = typeof(setmetatable(
	{} :: _StreamWriteBytesRequestFields,
	{} :: _StreamWriteBytesRequestImpl
))
type _StreamWriteBytesRequestMessage = proto.Message<StreamWriteBytesRequest, _StreamWriteBytesRequestPartialFields>

type _StreamCloseRequestImpl = {
	__index: _StreamCloseRequestImpl,
	new: (fields: _StreamCloseRequestPartialFields?) -> StreamCloseRequest,
	encode: (self: StreamCloseRequest) -> buffer,
	decode: (input: buffer) -> StreamCloseRequest,
	jsonEncode: (self: StreamCloseRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StreamCloseRequest,
	descriptor: proto.Descriptor,
}

type _StreamCloseRequestFields = {
	handle: string,
}

export type _StreamCloseRequestPartialFields = {
	handle: string?,
}

export type StreamCloseRequest = typeof(setmetatable({} :: _StreamCloseRequestFields, {} :: _StreamCloseRequestImpl))
type _StreamCloseRequestMessage = proto.Message<StreamCloseRequest, _StreamCloseRequestPartialFields>

type _FsExistsRequestImpl = {
	__index: _FsExistsRequestImpl,
	new: (fields: _FsExistsRequestPartialFields?) -> FsExistsRequest,
	encode: (self: FsExistsRequest) -> buffer,
	decode: (input: buffer) -> FsExistsRequest,
	jsonEncode: (self: FsExistsRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsExistsRequest,
	descriptor: proto.Descriptor,
}

type _FsExistsRequestFields = {
	path: string,
}

export type _FsExistsRequestPartialFields = {
	path: string?,
}

export type FsExistsRequest = typeof(setmetatable({} :: _FsExistsRequestFields, {} :: _FsExistsRequestImpl))
type _FsExistsRequestMessage = proto.Message<FsExistsRequest, _FsExistsRequestPartialFields>

type _FsExistsResponseImpl = {
	__index: _FsExistsResponseImpl,
	new: (fields: _FsExistsResponsePartialFields?) -> FsExistsResponse,
	encode: (self: FsExistsResponse) -> buffer,
	decode: (input: buffer) -> FsExistsResponse,
	jsonEncode: (self: FsExistsResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsExistsResponse,
	descriptor: proto.Descriptor,
}

type _FsExistsResponseFields = {
	exists: boolean,
}

export type _FsExistsResponsePartialFields = {
	exists: boolean?,
}

export type FsExistsResponse = typeof(setmetatable({} :: _FsExistsResponseFields, {} :: _FsExistsResponseImpl))
type _FsExistsResponseMessage = proto.Message<FsExistsResponse, _FsExistsResponsePartialFields>

type _FsStatRequestImpl = {
	__index: _FsStatRequestImpl,
	new: (fields: _FsStatRequestPartialFields?) -> FsStatRequest,
	encode: (self: FsStatRequest) -> buffer,
	decode: (input: buffer) -> FsStatRequest,
	jsonEncode: (self: FsStatRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsStatRequest,
	descriptor: proto.Descriptor,
}

type _FsStatRequestFields = {
	path: string,
}

export type _FsStatRequestPartialFields = {
	path: string?,
}

export type FsStatRequest = typeof(setmetatable({} :: _FsStatRequestFields, {} :: _FsStatRequestImpl))
type _FsStatRequestMessage = proto.Message<FsStatRequest, _FsStatRequestPartialFields>

type _FsStatResponseImpl = {
	__index: _FsStatResponseImpl,
	new: (fields: _FsStatResponsePartialFields?) -> FsStatResponse,
	encode: (self: FsStatResponse) -> buffer,
	decode: (input: buffer) -> FsStatResponse,
	jsonEncode: (self: FsStatResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsStatResponse,
	descriptor: proto.Descriptor,
}

type _FsStatResponseFields = {
	type: string,
	size: number,
	createdMillis: number?,
	modifiedMillis: number?,
	accessedMillis: number?,
}

export type _FsStatResponsePartialFields = {
	type: string?,
	size: number?,
	createdMillis: number?,
	modifiedMillis: number?,
	accessedMillis: number?,
}

export type FsStatResponse = typeof(setmetatable({} :: _FsStatResponseFields, {} :: _FsStatResponseImpl))
type _FsStatResponseMessage = proto.Message<FsStatResponse, _FsStatResponsePartialFields>

type _FsTypeRequestImpl = {
	__index: _FsTypeRequestImpl,
	new: (fields: _FsTypeRequestPartialFields?) -> FsTypeRequest,
	encode: (self: FsTypeRequest) -> buffer,
	decode: (input: buffer) -> FsTypeRequest,
	jsonEncode: (self: FsTypeRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsTypeRequest,
	descriptor: proto.Descriptor,
}

type _FsTypeRequestFields = {
	path: string,
}

export type _FsTypeRequestPartialFields = {
	path: string?,
}

export type FsTypeRequest = typeof(setmetatable({} :: _FsTypeRequestFields, {} :: _FsTypeRequestImpl))
type _FsTypeRequestMessage = proto.Message<FsTypeRequest, _FsTypeRequestPartialFields>

type _FsTypeResponseImpl = {
	__index: _FsTypeResponseImpl,
	new: (fields: _FsTypeResponsePartialFields?) -> FsTypeResponse,
	encode: (self: FsTypeResponse) -> buffer,
	decode: (input: buffer) -> FsTypeResponse,
	jsonEncode: (self: FsTypeResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsTypeResponse,
	descriptor: proto.Descriptor,
}

type _FsTypeResponseFields = {
	type: string?,
}

export type _FsTypeResponsePartialFields = {
	type: string?,
}

export type FsTypeResponse = typeof(setmetatable({} :: _FsTypeResponseFields, {} :: _FsTypeResponseImpl))
type _FsTypeResponseMessage = proto.Message<FsTypeResponse, _FsTypeResponsePartialFields>

type _FsMkdirRequestImpl = {
	__index: _FsMkdirRequestImpl,
	new: (fields: _FsMkdirRequestPartialFields?) -> FsMkdirRequest,
	encode: (self: FsMkdirRequest) -> buffer,
	decode: (input: buffer) -> FsMkdirRequest,
	jsonEncode: (self: FsMkdirRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsMkdirRequest,
	descriptor: proto.Descriptor,
}

type _FsMkdirRequestFields = {
	path: string,
}

export type _FsMkdirRequestPartialFields = {
	path: string?,
}

export type FsMkdirRequest = typeof(setmetatable({} :: _FsMkdirRequestFields, {} :: _FsMkdirRequestImpl))
type _FsMkdirRequestMessage = proto.Message<FsMkdirRequest, _FsMkdirRequestPartialFields>

type _FsListdirRequestImpl = {
	__index: _FsListdirRequestImpl,
	new: (fields: _FsListdirRequestPartialFields?) -> FsListdirRequest,
	encode: (self: FsListdirRequest) -> buffer,
	decode: (input: buffer) -> FsListdirRequest,
	jsonEncode: (self: FsListdirRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsListdirRequest,
	descriptor: proto.Descriptor,
}

type _FsListdirRequestFields = {
	path: string,
}

export type _FsListdirRequestPartialFields = {
	path: string?,
}

export type FsListdirRequest = typeof(setmetatable({} :: _FsListdirRequestFields, {} :: _FsListdirRequestImpl))
type _FsListdirRequestMessage = proto.Message<FsListdirRequest, _FsListdirRequestPartialFields>

type _FsListdirResponseImpl = {
	__index: _FsListdirResponseImpl,
	new: (fields: _FsListdirResponsePartialFields?) -> FsListdirResponse,
	encode: (self: FsListdirResponse) -> buffer,
	decode: (input: buffer) -> FsListdirResponse,
	jsonEncode: (self: FsListdirResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsListdirResponse,
	descriptor: proto.Descriptor,
}

type _FsListdirResponseFields = {
	entries: { FsDirEntry },
}

export type _FsListdirResponsePartialFields = {
	entries: { FsDirEntry | _FsDirEntryPartialFields }?,
}

export type FsListdirResponse = typeof(setmetatable({} :: _FsListdirResponseFields, {} :: _FsListdirResponseImpl))
type _FsListdirResponseMessage = proto.Message<FsListdirResponse, _FsListdirResponsePartialFields>

type _FsDirEntryImpl = {
	__index: _FsDirEntryImpl,
	new: (fields: _FsDirEntryPartialFields?) -> FsDirEntry,
	encode: (self: FsDirEntry) -> buffer,
	decode: (input: buffer) -> FsDirEntry,
	jsonEncode: (self: FsDirEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsDirEntry,
	descriptor: proto.Descriptor,
}

type _FsDirEntryFields = {
	name: string,
	type: string,
}

export type _FsDirEntryPartialFields = {
	name: string?,
	type: string?,
}

export type FsDirEntry = typeof(setmetatable({} :: _FsDirEntryFields, {} :: _FsDirEntryImpl))
type _FsDirEntryMessage = proto.Message<FsDirEntry, _FsDirEntryPartialFields>

type _FsRemoveRequestImpl = {
	__index: _FsRemoveRequestImpl,
	new: (fields: _FsRemoveRequestPartialFields?) -> FsRemoveRequest,
	encode: (self: FsRemoveRequest) -> buffer,
	decode: (input: buffer) -> FsRemoveRequest,
	jsonEncode: (self: FsRemoveRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsRemoveRequest,
	descriptor: proto.Descriptor,
}

type _FsRemoveRequestFields = {
	path: string,
}

export type _FsRemoveRequestPartialFields = {
	path: string?,
}

export type FsRemoveRequest = typeof(setmetatable({} :: _FsRemoveRequestFields, {} :: _FsRemoveRequestImpl))
type _FsRemoveRequestMessage = proto.Message<FsRemoveRequest, _FsRemoveRequestPartialFields>

type _FsRmdirRequestImpl = {
	__index: _FsRmdirRequestImpl,
	new: (fields: _FsRmdirRequestPartialFields?) -> FsRmdirRequest,
	encode: (self: FsRmdirRequest) -> buffer,
	decode: (input: buffer) -> FsRmdirRequest,
	jsonEncode: (self: FsRmdirRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsRmdirRequest,
	descriptor: proto.Descriptor,
}

type _FsRmdirRequestFields = {
	path: string,
}

export type _FsRmdirRequestPartialFields = {
	path: string?,
}

export type FsRmdirRequest = typeof(setmetatable({} :: _FsRmdirRequestFields, {} :: _FsRmdirRequestImpl))
type _FsRmdirRequestMessage = proto.Message<FsRmdirRequest, _FsRmdirRequestPartialFields>

type _FsCopyRequestImpl = {
	__index: _FsCopyRequestImpl,
	new: (fields: _FsCopyRequestPartialFields?) -> FsCopyRequest,
	encode: (self: FsCopyRequest) -> buffer,
	decode: (input: buffer) -> FsCopyRequest,
	jsonEncode: (self: FsCopyRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FsCopyRequest,
	descriptor: proto.Descriptor,
}

type _FsCopyRequestFields = {
	src: string,
	dest: string,
}

export type _FsCopyRequestPartialFields = {
	src: string?,
	dest: string?,
}

export type FsCopyRequest = typeof(setmetatable({} :: _FsCopyRequestFields, {} :: _FsCopyRequestImpl))
type _FsCopyRequestMessage = proto.Message<FsCopyRequest, _FsCopyRequestPartialFields>

type _ProcessOptionsImpl = {
	__index: _ProcessOptionsImpl,
	new: (fields: _ProcessOptionsPartialFields?) -> ProcessOptions,
	encode: (self: ProcessOptions) -> buffer,
	decode: (input: buffer) -> ProcessOptions,
	jsonEncode: (self: ProcessOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessOptions,
	descriptor: proto.Descriptor,
}

type _ProcessOptionsFields = {
	cwd: string?,
	stdio: string?,
}

export type _ProcessOptionsPartialFields = {
	cwd: string?,
	stdio: string?,
}

export type ProcessOptions = typeof(setmetatable({} :: _ProcessOptionsFields, {} :: _ProcessOptionsImpl))
type _ProcessOptionsMessage = proto.Message<ProcessOptions, _ProcessOptionsPartialFields>

type _ProcessGetInfoRequestImpl = {
	__index: _ProcessGetInfoRequestImpl,
	new: (fields: _ProcessGetInfoRequestPartialFields?) -> ProcessGetInfoRequest,
	encode: (self: ProcessGetInfoRequest) -> buffer,
	decode: (input: buffer) -> ProcessGetInfoRequest,
	jsonEncode: (self: ProcessGetInfoRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessGetInfoRequest,
	descriptor: proto.Descriptor,
}

type _ProcessGetInfoRequestFields = {}

export type _ProcessGetInfoRequestPartialFields = {}

export type ProcessGetInfoRequest = typeof(setmetatable(
	{} :: _ProcessGetInfoRequestFields,
	{} :: _ProcessGetInfoRequestImpl
))
type _ProcessGetInfoRequestMessage = proto.Message<ProcessGetInfoRequest, _ProcessGetInfoRequestPartialFields>

type _ProcessGetInfoResponseImpl = {
	__index: _ProcessGetInfoResponseImpl,
	new: (fields: _ProcessGetInfoResponsePartialFields?) -> ProcessGetInfoResponse,
	encode: (self: ProcessGetInfoResponse) -> buffer,
	decode: (input: buffer) -> ProcessGetInfoResponse,
	jsonEncode: (self: ProcessGetInfoResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessGetInfoResponse,
	descriptor: proto.Descriptor,
}

type _ProcessGetInfoResponseFields = {
	cwd: string,
	homedir: string,
	execpath: string,
	env: { [string]: string },
	platform: string?,
	arch: string?,
	studioContentPath: string?,
}

export type _ProcessGetInfoResponsePartialFields = {
	cwd: string?,
	homedir: string?,
	execpath: string?,
	env: { [string]: string }?,
	platform: string?,
	arch: string?,
	studioContentPath: string?,
}

export type ProcessGetInfoResponse = typeof(setmetatable(
	{} :: _ProcessGetInfoResponseFields,
	{} :: _ProcessGetInfoResponseImpl
))
type _ProcessGetInfoResponseMessage = proto.Message<ProcessGetInfoResponse, _ProcessGetInfoResponsePartialFields>

type _ProcessGetInfoResponse_EnvEntryImpl = {
	__index: _ProcessGetInfoResponse_EnvEntryImpl,
	new: (fields: _ProcessGetInfoResponse_EnvEntryPartialFields?) -> ProcessGetInfoResponse_EnvEntry,
	encode: (self: ProcessGetInfoResponse_EnvEntry) -> buffer,
	decode: (input: buffer) -> ProcessGetInfoResponse_EnvEntry,
	jsonEncode: (self: ProcessGetInfoResponse_EnvEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessGetInfoResponse_EnvEntry,
	descriptor: proto.Descriptor,
}

type _ProcessGetInfoResponse_EnvEntryFields = {
	key: string,
	value: string,
}

export type _ProcessGetInfoResponse_EnvEntryPartialFields = {
	key: string?,
	value: string?,
}

export type ProcessGetInfoResponse_EnvEntry = typeof(setmetatable(
	{} :: _ProcessGetInfoResponse_EnvEntryFields,
	{} :: _ProcessGetInfoResponse_EnvEntryImpl
))
type _ProcessGetInfoResponse_EnvEntryMessage = proto.Message<
	ProcessGetInfoResponse_EnvEntry,
	_ProcessGetInfoResponse_EnvEntryPartialFields
>

type _ProcessExitRequestImpl = {
	__index: _ProcessExitRequestImpl,
	new: (fields: _ProcessExitRequestPartialFields?) -> ProcessExitRequest,
	encode: (self: ProcessExitRequest) -> buffer,
	decode: (input: buffer) -> ProcessExitRequest,
	jsonEncode: (self: ProcessExitRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessExitRequest,
	descriptor: proto.Descriptor,
}

type _ProcessExitRequestFields = {
	code: number,
}

export type _ProcessExitRequestPartialFields = {
	code: number?,
}

export type ProcessExitRequest = typeof(setmetatable({} :: _ProcessExitRequestFields, {} :: _ProcessExitRequestImpl))
type _ProcessExitRequestMessage = proto.Message<ProcessExitRequest, _ProcessExitRequestPartialFields>

type _ProcessRunRequestImpl = {
	__index: _ProcessRunRequestImpl,
	new: (fields: _ProcessRunRequestPartialFields?) -> ProcessRunRequest,
	encode: (self: ProcessRunRequest) -> buffer,
	decode: (input: buffer) -> ProcessRunRequest,
	jsonEncode: (self: ProcessRunRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessRunRequest,
	descriptor: proto.Descriptor,
}

type _ProcessRunRequestFields = {
	args: { string },
	options: ProcessOptions?,
}

export type _ProcessRunRequestPartialFields = {
	args: { string }?,
	options: (ProcessOptions | _ProcessOptionsPartialFields)?,
}

export type ProcessRunRequest = typeof(setmetatable({} :: _ProcessRunRequestFields, {} :: _ProcessRunRequestImpl))
type _ProcessRunRequestMessage = proto.Message<ProcessRunRequest, _ProcessRunRequestPartialFields>

type _ProcessRunResponseImpl = {
	__index: _ProcessRunResponseImpl,
	new: (fields: _ProcessRunResponsePartialFields?) -> ProcessRunResponse,
	encode: (self: ProcessRunResponse) -> buffer,
	decode: (input: buffer) -> ProcessRunResponse,
	jsonEncode: (self: ProcessRunResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessRunResponse,
	descriptor: proto.Descriptor,
}

type _ProcessRunResponseFields = {
	ok: boolean,
	exitcode: number,
	out: string,
	err: string,
}

export type _ProcessRunResponsePartialFields = {
	ok: boolean?,
	exitcode: number?,
	out: string?,
	err: string?,
}

export type ProcessRunResponse = typeof(setmetatable({} :: _ProcessRunResponseFields, {} :: _ProcessRunResponseImpl))
type _ProcessRunResponseMessage = proto.Message<ProcessRunResponse, _ProcessRunResponsePartialFields>

type _ProcessSystemRequestImpl = {
	__index: _ProcessSystemRequestImpl,
	new: (fields: _ProcessSystemRequestPartialFields?) -> ProcessSystemRequest,
	encode: (self: ProcessSystemRequest) -> buffer,
	decode: (input: buffer) -> ProcessSystemRequest,
	jsonEncode: (self: ProcessSystemRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessSystemRequest,
	descriptor: proto.Descriptor,
}

type _ProcessSystemRequestFields = {
	command: string,
	options: ProcessOptions?,
}

export type _ProcessSystemRequestPartialFields = {
	command: string?,
	options: (ProcessOptions | _ProcessOptionsPartialFields)?,
}

export type ProcessSystemRequest = typeof(setmetatable(
	{} :: _ProcessSystemRequestFields,
	{} :: _ProcessSystemRequestImpl
))
type _ProcessSystemRequestMessage = proto.Message<ProcessSystemRequest, _ProcessSystemRequestPartialFields>

type _ProcessCreateRequestImpl = {
	__index: _ProcessCreateRequestImpl,
	new: (fields: _ProcessCreateRequestPartialFields?) -> ProcessCreateRequest,
	encode: (self: ProcessCreateRequest) -> buffer,
	decode: (input: buffer) -> ProcessCreateRequest,
	jsonEncode: (self: ProcessCreateRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessCreateRequest,
	descriptor: proto.Descriptor,
}

type _ProcessCreateRequestFields = {
	args: { string },
	options: ProcessOptions?,
}

export type _ProcessCreateRequestPartialFields = {
	args: { string }?,
	options: (ProcessOptions | _ProcessOptionsPartialFields)?,
}

export type ProcessCreateRequest = typeof(setmetatable(
	{} :: _ProcessCreateRequestFields,
	{} :: _ProcessCreateRequestImpl
))
type _ProcessCreateRequestMessage = proto.Message<ProcessCreateRequest, _ProcessCreateRequestPartialFields>

type _ProcessCreateResponseImpl = {
	__index: _ProcessCreateResponseImpl,
	new: (fields: _ProcessCreateResponsePartialFields?) -> ProcessCreateResponse,
	encode: (self: ProcessCreateResponse) -> buffer,
	decode: (input: buffer) -> ProcessCreateResponse,
	jsonEncode: (self: ProcessCreateResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessCreateResponse,
	descriptor: proto.Descriptor,
}

type _ProcessCreateResponseFields = {
	pid: string,
	stdinHandle: string?,
	stdoutHandle: string?,
	stderrHandle: string?,
}

export type _ProcessCreateResponsePartialFields = {
	pid: string?,
	stdinHandle: string?,
	stdoutHandle: string?,
	stderrHandle: string?,
}

export type ProcessCreateResponse = typeof(setmetatable(
	{} :: _ProcessCreateResponseFields,
	{} :: _ProcessCreateResponseImpl
))
type _ProcessCreateResponseMessage = proto.Message<ProcessCreateResponse, _ProcessCreateResponsePartialFields>

type _ProcessRunHandleRequestImpl = {
	__index: _ProcessRunHandleRequestImpl,
	new: (fields: _ProcessRunHandleRequestPartialFields?) -> ProcessRunHandleRequest,
	encode: (self: ProcessRunHandleRequest) -> buffer,
	decode: (input: buffer) -> ProcessRunHandleRequest,
	jsonEncode: (self: ProcessRunHandleRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessRunHandleRequest,
	descriptor: proto.Descriptor,
}

type _ProcessRunHandleRequestFields = {
	pid: string,
}

export type _ProcessRunHandleRequestPartialFields = {
	pid: string?,
}

export type ProcessRunHandleRequest = typeof(setmetatable(
	{} :: _ProcessRunHandleRequestFields,
	{} :: _ProcessRunHandleRequestImpl
))
type _ProcessRunHandleRequestMessage = proto.Message<ProcessRunHandleRequest, _ProcessRunHandleRequestPartialFields>

type _ProcessKillRequestImpl = {
	__index: _ProcessKillRequestImpl,
	new: (fields: _ProcessKillRequestPartialFields?) -> ProcessKillRequest,
	encode: (self: ProcessKillRequest) -> buffer,
	decode: (input: buffer) -> ProcessKillRequest,
	jsonEncode: (self: ProcessKillRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessKillRequest,
	descriptor: proto.Descriptor,
}

type _ProcessKillRequestFields = {
	pid: string,
}

export type _ProcessKillRequestPartialFields = {
	pid: string?,
}

export type ProcessKillRequest = typeof(setmetatable({} :: _ProcessKillRequestFields, {} :: _ProcessKillRequestImpl))
type _ProcessKillRequestMessage = proto.Message<ProcessKillRequest, _ProcessKillRequestPartialFields>

type _McpCallRequestImpl = {
	__index: _McpCallRequestImpl,
	new: (fields: _McpCallRequestPartialFields?) -> McpCallRequest,
	encode: (self: McpCallRequest) -> buffer,
	decode: (input: buffer) -> McpCallRequest,
	jsonEncode: (self: McpCallRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> McpCallRequest,
	descriptor: proto.Descriptor,
}

type _McpCallRequestFields = {
	tool: string,
	argumentsJson: string,
	mcpStudioId: string,
}

export type _McpCallRequestPartialFields = {
	tool: string?,
	argumentsJson: string?,
	mcpStudioId: string?,
}

export type McpCallRequest = typeof(setmetatable({} :: _McpCallRequestFields, {} :: _McpCallRequestImpl))
type _McpCallRequestMessage = proto.Message<McpCallRequest, _McpCallRequestPartialFields>

type _McpCallResponseImpl = {
	__index: _McpCallResponseImpl,
	new: (fields: _McpCallResponsePartialFields?) -> McpCallResponse,
	encode: (self: McpCallResponse) -> buffer,
	decode: (input: buffer) -> McpCallResponse,
	jsonEncode: (self: McpCallResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> McpCallResponse,
	descriptor: proto.Descriptor,
}

type _McpCallResponseFields = {
	result: string,
}

export type _McpCallResponsePartialFields = {
	result: string?,
}

export type McpCallResponse = typeof(setmetatable({} :: _McpCallResponseFields, {} :: _McpCallResponseImpl))
type _McpCallResponseMessage = proto.Message<McpCallResponse, _McpCallResponsePartialFields>

type _ClientRpcCallImpl = {
	__index: _ClientRpcCallImpl,
	new: (fields: _ClientRpcCallPartialFields?) -> ClientRpcCall,
	encode: (self: ClientRpcCall) -> buffer,
	decode: (input: buffer) -> ClientRpcCall,
	jsonEncode: (self: ClientRpcCall) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ClientRpcCall,
	descriptor: proto.Descriptor,
}

type _ClientRpcCallFields = {
	id: string,
	executionId: string,
	req: (
		{ type: "fsExists", value: FsExistsRequest }
		| { type: "fsStat", value: FsStatRequest }
		| { type: "fsType", value: FsTypeRequest }
		| { type: "fsMkdir", value: FsMkdirRequest }
		| { type: "fsListdir", value: FsListdirRequest }
		| { type: "fsRemove", value: FsRemoveRequest }
		| { type: "fsRmdir", value: FsRmdirRequest }
		| { type: "fsCopy", value: FsCopyRequest }
		| { type: "streamOpen", value: StreamOpenRequest }
		| { type: "streamReadChunk", value: StreamReadChunkRequest }
		| { type: "streamReadLine", value: StreamReadLineRequest }
		| { type: "streamReadAll", value: StreamReadAllRequest }
		| { type: "streamWrite", value: StreamWriteRequest }
		| { type: "streamClose", value: StreamCloseRequest }
		| { type: "streamReadBytes", value: StreamReadBytesRequest }
		| { type: "streamWriteBytes", value: StreamWriteBytesRequest }
		| { type: "processGetInfo", value: ProcessGetInfoRequest }
		| { type: "processExit", value: ProcessExitRequest }
		| { type: "processRun", value: ProcessRunRequest }
		| { type: "processSystem", value: ProcessSystemRequest }
		| { type: "processCreate", value: ProcessCreateRequest }
		| { type: "processRunHandle", value: ProcessRunHandleRequest }
		| { type: "processKill", value: ProcessKillRequest }
		| { type: "mcpCall", value: McpCallRequest }
	)?,
}

export type _ClientRpcCallPartialFields = {
	id: string?,
	executionId: string?,
	req: (
		{ type: "fsExists", value: FsExistsRequest | _FsExistsRequestPartialFields }
		| { type: "fsStat", value: FsStatRequest | _FsStatRequestPartialFields }
		| { type: "fsType", value: FsTypeRequest | _FsTypeRequestPartialFields }
		| { type: "fsMkdir", value: FsMkdirRequest | _FsMkdirRequestPartialFields }
		| { type: "fsListdir", value: FsListdirRequest | _FsListdirRequestPartialFields }
		| { type: "fsRemove", value: FsRemoveRequest | _FsRemoveRequestPartialFields }
		| { type: "fsRmdir", value: FsRmdirRequest | _FsRmdirRequestPartialFields }
		| { type: "fsCopy", value: FsCopyRequest | _FsCopyRequestPartialFields }
		| { type: "streamOpen", value: StreamOpenRequest | _StreamOpenRequestPartialFields }
		| { type: "streamReadChunk", value: StreamReadChunkRequest | _StreamReadChunkRequestPartialFields }
		| { type: "streamReadLine", value: StreamReadLineRequest | _StreamReadLineRequestPartialFields }
		| { type: "streamReadAll", value: StreamReadAllRequest | _StreamReadAllRequestPartialFields }
		| { type: "streamWrite", value: StreamWriteRequest | _StreamWriteRequestPartialFields }
		| { type: "streamClose", value: StreamCloseRequest | _StreamCloseRequestPartialFields }
		| { type: "streamReadBytes", value: StreamReadBytesRequest | _StreamReadBytesRequestPartialFields }
		| { type: "streamWriteBytes", value: StreamWriteBytesRequest | _StreamWriteBytesRequestPartialFields }
		| { type: "processGetInfo", value: ProcessGetInfoRequest | _ProcessGetInfoRequestPartialFields }
		| { type: "processExit", value: ProcessExitRequest | _ProcessExitRequestPartialFields }
		| { type: "processRun", value: ProcessRunRequest | _ProcessRunRequestPartialFields }
		| { type: "processSystem", value: ProcessSystemRequest | _ProcessSystemRequestPartialFields }
		| { type: "processCreate", value: ProcessCreateRequest | _ProcessCreateRequestPartialFields }
		| { type: "processRunHandle", value: ProcessRunHandleRequest | _ProcessRunHandleRequestPartialFields }
		| { type: "processKill", value: ProcessKillRequest | _ProcessKillRequestPartialFields }
		| { type: "mcpCall", value: McpCallRequest | _McpCallRequestPartialFields }
	)?,
}

export type ClientRpcCall = typeof(setmetatable({} :: _ClientRpcCallFields, {} :: _ClientRpcCallImpl))
type _ClientRpcCallMessage = proto.Message<ClientRpcCall, _ClientRpcCallPartialFields>

type _ClientRpcResponseImpl = {
	__index: _ClientRpcResponseImpl,
	new: (fields: _ClientRpcResponsePartialFields?) -> ClientRpcResponse,
	encode: (self: ClientRpcResponse) -> buffer,
	decode: (input: buffer) -> ClientRpcResponse,
	jsonEncode: (self: ClientRpcResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ClientRpcResponse,
	descriptor: proto.Descriptor,
}

type _ClientRpcResponseFields = {
	id: string,
	executionId: string,
	res: (
		{ type: "error", value: string }
		| { type: "fsExists", value: FsExistsResponse }
		| { type: "fsStat", value: FsStatResponse }
		| { type: "fsType", value: FsTypeResponse }
		| { type: "fsMkdir", value: Ok }
		| { type: "fsListdir", value: FsListdirResponse }
		| { type: "fsRemove", value: Ok }
		| { type: "fsRmdir", value: Ok }
		| { type: "fsCopy", value: Ok }
		| { type: "streamOpen", value: StreamOpenResponse }
		| { type: "streamReadChunk", value: StreamReadChunkResponse }
		| { type: "streamReadLine", value: StreamReadLineResponse }
		| { type: "streamReadAll", value: StreamReadAllResponse }
		| { type: "streamWrite", value: Ok }
		| { type: "streamClose", value: Ok }
		| { type: "streamReadBytes", value: StreamReadBytesResponse }
		| { type: "streamWriteBytes", value: Ok }
		| { type: "processGetInfo", value: ProcessGetInfoResponse }
		| { type: "processExit", value: Ok }
		| { type: "processRun", value: ProcessRunResponse }
		| { type: "processSystem", value: ProcessRunResponse }
		| { type: "processCreate", value: ProcessCreateResponse }
		| { type: "processRunHandle", value: ProcessRunResponse }
		| { type: "processKill", value: Ok }
		| { type: "mcpCall", value: McpCallResponse }
	)?,
}

export type _ClientRpcResponsePartialFields = {
	id: string?,
	executionId: string?,
	res: (
		{ type: "error", value: string }
		| { type: "fsExists", value: FsExistsResponse | _FsExistsResponsePartialFields }
		| { type: "fsStat", value: FsStatResponse | _FsStatResponsePartialFields }
		| { type: "fsType", value: FsTypeResponse | _FsTypeResponsePartialFields }
		| { type: "fsMkdir", value: Ok | _OkPartialFields }
		| { type: "fsListdir", value: FsListdirResponse | _FsListdirResponsePartialFields }
		| { type: "fsRemove", value: Ok | _OkPartialFields }
		| { type: "fsRmdir", value: Ok | _OkPartialFields }
		| { type: "fsCopy", value: Ok | _OkPartialFields }
		| { type: "streamOpen", value: StreamOpenResponse | _StreamOpenResponsePartialFields }
		| { type: "streamReadChunk", value: StreamReadChunkResponse | _StreamReadChunkResponsePartialFields }
		| { type: "streamReadLine", value: StreamReadLineResponse | _StreamReadLineResponsePartialFields }
		| { type: "streamReadAll", value: StreamReadAllResponse | _StreamReadAllResponsePartialFields }
		| { type: "streamWrite", value: Ok | _OkPartialFields }
		| { type: "streamClose", value: Ok | _OkPartialFields }
		| { type: "streamReadBytes", value: StreamReadBytesResponse | _StreamReadBytesResponsePartialFields }
		| { type: "streamWriteBytes", value: Ok | _OkPartialFields }
		| { type: "processGetInfo", value: ProcessGetInfoResponse | _ProcessGetInfoResponsePartialFields }
		| { type: "processExit", value: Ok | _OkPartialFields }
		| { type: "processRun", value: ProcessRunResponse | _ProcessRunResponsePartialFields }
		| { type: "processSystem", value: ProcessRunResponse | _ProcessRunResponsePartialFields }
		| { type: "processCreate", value: ProcessCreateResponse | _ProcessCreateResponsePartialFields }
		| { type: "processRunHandle", value: ProcessRunResponse | _ProcessRunResponsePartialFields }
		| { type: "processKill", value: Ok | _OkPartialFields }
		| { type: "mcpCall", value: McpCallResponse | _McpCallResponsePartialFields }
	)?,
}

export type ClientRpcResponse = typeof(setmetatable({} :: _ClientRpcResponseFields, {} :: _ClientRpcResponseImpl))
type _ClientRpcResponseMessage = proto.Message<ClientRpcResponse, _ClientRpcResponsePartialFields>

do
	local _OkImpl = {}
	_OkImpl.__index = _OkImpl

	function _OkImpl.new(data: _OkPartialFields?): Ok
		return setmetatable({}, _OkImpl :: _OkImpl)
	end

	function _OkImpl.encode(self: Ok): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _OkImpl.decode(input: buffer): Ok
		local self = _OkImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _OkImpl.jsonEncode(self: Ok): any
		local output = {}

		return output
	end

	function _OkImpl.jsonDecode(input: { [string]: any }): Ok
		local self = _OkImpl.new()

		return self
	end

	_OkImpl.descriptor = {
		name = "Ok",
		fullName = "rodeo.runtime.Ok",
	}

	messages.Ok = _OkImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.Ok)
end

do
	local _StreamOpenRequestImpl = {}
	_StreamOpenRequestImpl.__index = _StreamOpenRequestImpl

	function _StreamOpenRequestImpl.new(data: _StreamOpenRequestPartialFields?): StreamOpenRequest
		return setmetatable({
			handle = if data == nil or data.handle == nil then "" else data.handle,
			path = if data == nil or data.path == nil then "" else data.path,
			mode = if data == nil or data.mode == nil then "" else data.mode,
		}, _StreamOpenRequestImpl :: _StreamOpenRequestImpl)
	end

	function _StreamOpenRequestImpl.encode(self: StreamOpenRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.handle ~= nil and self.handle ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.handle)
		end

		if self.path ~= nil and self.path ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		if self.mode ~= nil and self.mode ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mode)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamOpenRequestImpl.decode(input: buffer): StreamOpenRequest
		local self = _StreamOpenRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.handle = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mode = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamOpenRequestImpl.jsonEncode(self: StreamOpenRequest): any
		local output = {}

		if self.handle ~= nil and self.handle ~= "" then
			output.handle = self.handle
		end

		if self.path ~= nil and self.path ~= "" then
			output.path = self.path
		end

		if self.mode ~= nil and self.mode ~= "" then
			output.mode = self.mode
		end

		return output
	end

	function _StreamOpenRequestImpl.jsonDecode(input: { [string]: any }): StreamOpenRequest
		local self = _StreamOpenRequestImpl.new()

		if input.handle ~= nil then
			self.handle = input.handle
		end

		if input.path ~= nil then
			self.path = input.path
		end

		if input.mode ~= nil then
			self.mode = input.mode
		end

		return self
	end

	_StreamOpenRequestImpl.descriptor = {
		name = "StreamOpenRequest",
		fullName = "rodeo.runtime.StreamOpenRequest",
	}

	messages.StreamOpenRequest = _StreamOpenRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamOpenRequest)
end

do
	local _StreamOpenResponseImpl = {}
	_StreamOpenResponseImpl.__index = _StreamOpenResponseImpl

	function _StreamOpenResponseImpl.new(data: _StreamOpenResponsePartialFields?): StreamOpenResponse
		return setmetatable({
			handle = if data == nil or data.handle == nil then "" else data.handle,
		}, _StreamOpenResponseImpl :: _StreamOpenResponseImpl)
	end

	function _StreamOpenResponseImpl.encode(self: StreamOpenResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.handle ~= nil and self.handle ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.handle)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamOpenResponseImpl.decode(input: buffer): StreamOpenResponse
		local self = _StreamOpenResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.handle = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamOpenResponseImpl.jsonEncode(self: StreamOpenResponse): any
		local output = {}

		if self.handle ~= nil and self.handle ~= "" then
			output.handle = self.handle
		end

		return output
	end

	function _StreamOpenResponseImpl.jsonDecode(input: { [string]: any }): StreamOpenResponse
		local self = _StreamOpenResponseImpl.new()

		if input.handle ~= nil then
			self.handle = input.handle
		end

		return self
	end

	_StreamOpenResponseImpl.descriptor = {
		name = "StreamOpenResponse",
		fullName = "rodeo.runtime.StreamOpenResponse",
	}

	messages.StreamOpenResponse = _StreamOpenResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamOpenResponse)
end

do
	local _StreamReadChunkRequestImpl = {}
	_StreamReadChunkRequestImpl.__index = _StreamReadChunkRequestImpl

	function _StreamReadChunkRequestImpl.new(data: _StreamReadChunkRequestPartialFields?): StreamReadChunkRequest
		return setmetatable({
			handle = if data == nil or data.handle == nil then "" else data.handle,
			size = if data == nil or data.size == nil then nil else data.size,
		}, _StreamReadChunkRequestImpl :: _StreamReadChunkRequestImpl)
	end

	function _StreamReadChunkRequestImpl.encode(self: StreamReadChunkRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.handle ~= nil and self.handle ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.handle)
		end

		if self.size ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.size)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamReadChunkRequestImpl.decode(input: buffer): StreamReadChunkRequest
		local self = _StreamReadChunkRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.size = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.handle = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamReadChunkRequestImpl.jsonEncode(self: StreamReadChunkRequest): any
		local output = {}

		if self.handle ~= nil and self.handle ~= "" then
			output.handle = self.handle
		end

		if self.size ~= nil then
			output.size = self.size
		end

		return output
	end

	function _StreamReadChunkRequestImpl.jsonDecode(input: { [string]: any }): StreamReadChunkRequest
		local self = _StreamReadChunkRequestImpl.new()

		if input.handle ~= nil then
			self.handle = input.handle
		end

		if input.size ~= nil then
			self.size = input.size
		end

		return self
	end

	_StreamReadChunkRequestImpl.descriptor = {
		name = "StreamReadChunkRequest",
		fullName = "rodeo.runtime.StreamReadChunkRequest",
	}

	messages.StreamReadChunkRequest = _StreamReadChunkRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamReadChunkRequest)
end

do
	local _StreamReadChunkResponseImpl = {}
	_StreamReadChunkResponseImpl.__index = _StreamReadChunkResponseImpl

	function _StreamReadChunkResponseImpl.new(data: _StreamReadChunkResponsePartialFields?): StreamReadChunkResponse
		return setmetatable({
			data = if data == nil or data.data == nil then "" else data.data,
			eof = if data == nil or data.eof == nil then false else data.eof,
		}, _StreamReadChunkResponseImpl :: _StreamReadChunkResponseImpl)
	end

	function _StreamReadChunkResponseImpl.encode(self: StreamReadChunkResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.data ~= nil and self.data ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.data)
		end

		if self.eof then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.eof then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamReadChunkResponseImpl.decode(input: buffer): StreamReadChunkResponse
		local self = _StreamReadChunkResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.eof = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.data = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamReadChunkResponseImpl.jsonEncode(self: StreamReadChunkResponse): any
		local output = {}

		if self.data ~= nil and self.data ~= "" then
			output.data = self.data
		end

		if self.eof then
			output.eof = self.eof
		end

		return output
	end

	function _StreamReadChunkResponseImpl.jsonDecode(input: { [string]: any }): StreamReadChunkResponse
		local self = _StreamReadChunkResponseImpl.new()

		if input.data ~= nil then
			self.data = input.data
		end

		if input.eof ~= nil then
			self.eof = input.eof
		end

		return self
	end

	_StreamReadChunkResponseImpl.descriptor = {
		name = "StreamReadChunkResponse",
		fullName = "rodeo.runtime.StreamReadChunkResponse",
	}

	messages.StreamReadChunkResponse = _StreamReadChunkResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamReadChunkResponse)
end

do
	local _StreamReadLineRequestImpl = {}
	_StreamReadLineRequestImpl.__index = _StreamReadLineRequestImpl

	function _StreamReadLineRequestImpl.new(data: _StreamReadLineRequestPartialFields?): StreamReadLineRequest
		return setmetatable({
			handle = if data == nil or data.handle == nil then "" else data.handle,
		}, _StreamReadLineRequestImpl :: _StreamReadLineRequestImpl)
	end

	function _StreamReadLineRequestImpl.encode(self: StreamReadLineRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.handle ~= nil and self.handle ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.handle)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamReadLineRequestImpl.decode(input: buffer): StreamReadLineRequest
		local self = _StreamReadLineRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.handle = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamReadLineRequestImpl.jsonEncode(self: StreamReadLineRequest): any
		local output = {}

		if self.handle ~= nil and self.handle ~= "" then
			output.handle = self.handle
		end

		return output
	end

	function _StreamReadLineRequestImpl.jsonDecode(input: { [string]: any }): StreamReadLineRequest
		local self = _StreamReadLineRequestImpl.new()

		if input.handle ~= nil then
			self.handle = input.handle
		end

		return self
	end

	_StreamReadLineRequestImpl.descriptor = {
		name = "StreamReadLineRequest",
		fullName = "rodeo.runtime.StreamReadLineRequest",
	}

	messages.StreamReadLineRequest = _StreamReadLineRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamReadLineRequest)
end

do
	local _StreamReadLineResponseImpl = {}
	_StreamReadLineResponseImpl.__index = _StreamReadLineResponseImpl

	function _StreamReadLineResponseImpl.new(data: _StreamReadLineResponsePartialFields?): StreamReadLineResponse
		return setmetatable({
			data = if data == nil or data.data == nil then "" else data.data,
			eof = if data == nil or data.eof == nil then false else data.eof,
		}, _StreamReadLineResponseImpl :: _StreamReadLineResponseImpl)
	end

	function _StreamReadLineResponseImpl.encode(self: StreamReadLineResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.data ~= nil and self.data ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.data)
		end

		if self.eof then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.eof then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamReadLineResponseImpl.decode(input: buffer): StreamReadLineResponse
		local self = _StreamReadLineResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.eof = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.data = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamReadLineResponseImpl.jsonEncode(self: StreamReadLineResponse): any
		local output = {}

		if self.data ~= nil and self.data ~= "" then
			output.data = self.data
		end

		if self.eof then
			output.eof = self.eof
		end

		return output
	end

	function _StreamReadLineResponseImpl.jsonDecode(input: { [string]: any }): StreamReadLineResponse
		local self = _StreamReadLineResponseImpl.new()

		if input.data ~= nil then
			self.data = input.data
		end

		if input.eof ~= nil then
			self.eof = input.eof
		end

		return self
	end

	_StreamReadLineResponseImpl.descriptor = {
		name = "StreamReadLineResponse",
		fullName = "rodeo.runtime.StreamReadLineResponse",
	}

	messages.StreamReadLineResponse = _StreamReadLineResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamReadLineResponse)
end

do
	local _StreamReadAllRequestImpl = {}
	_StreamReadAllRequestImpl.__index = _StreamReadAllRequestImpl

	function _StreamReadAllRequestImpl.new(data: _StreamReadAllRequestPartialFields?): StreamReadAllRequest
		return setmetatable({
			handle = if data == nil or data.handle == nil then "" else data.handle,
		}, _StreamReadAllRequestImpl :: _StreamReadAllRequestImpl)
	end

	function _StreamReadAllRequestImpl.encode(self: StreamReadAllRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.handle ~= nil and self.handle ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.handle)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamReadAllRequestImpl.decode(input: buffer): StreamReadAllRequest
		local self = _StreamReadAllRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.handle = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamReadAllRequestImpl.jsonEncode(self: StreamReadAllRequest): any
		local output = {}

		if self.handle ~= nil and self.handle ~= "" then
			output.handle = self.handle
		end

		return output
	end

	function _StreamReadAllRequestImpl.jsonDecode(input: { [string]: any }): StreamReadAllRequest
		local self = _StreamReadAllRequestImpl.new()

		if input.handle ~= nil then
			self.handle = input.handle
		end

		return self
	end

	_StreamReadAllRequestImpl.descriptor = {
		name = "StreamReadAllRequest",
		fullName = "rodeo.runtime.StreamReadAllRequest",
	}

	messages.StreamReadAllRequest = _StreamReadAllRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamReadAllRequest)
end

do
	local _StreamReadAllResponseImpl = {}
	_StreamReadAllResponseImpl.__index = _StreamReadAllResponseImpl

	function _StreamReadAllResponseImpl.new(data: _StreamReadAllResponsePartialFields?): StreamReadAllResponse
		return setmetatable({
			data = if data == nil or data.data == nil then "" else data.data,
		}, _StreamReadAllResponseImpl :: _StreamReadAllResponseImpl)
	end

	function _StreamReadAllResponseImpl.encode(self: StreamReadAllResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.data ~= nil and self.data ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.data)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamReadAllResponseImpl.decode(input: buffer): StreamReadAllResponse
		local self = _StreamReadAllResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.data = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamReadAllResponseImpl.jsonEncode(self: StreamReadAllResponse): any
		local output = {}

		if self.data ~= nil and self.data ~= "" then
			output.data = self.data
		end

		return output
	end

	function _StreamReadAllResponseImpl.jsonDecode(input: { [string]: any }): StreamReadAllResponse
		local self = _StreamReadAllResponseImpl.new()

		if input.data ~= nil then
			self.data = input.data
		end

		return self
	end

	_StreamReadAllResponseImpl.descriptor = {
		name = "StreamReadAllResponse",
		fullName = "rodeo.runtime.StreamReadAllResponse",
	}

	messages.StreamReadAllResponse = _StreamReadAllResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamReadAllResponse)
end

do
	local _StreamWriteRequestImpl = {}
	_StreamWriteRequestImpl.__index = _StreamWriteRequestImpl

	function _StreamWriteRequestImpl.new(data: _StreamWriteRequestPartialFields?): StreamWriteRequest
		return setmetatable({
			handle = if data == nil or data.handle == nil then "" else data.handle,
			data = if data == nil or data.data == nil then "" else data.data,
		}, _StreamWriteRequestImpl :: _StreamWriteRequestImpl)
	end

	function _StreamWriteRequestImpl.encode(self: StreamWriteRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.handle ~= nil and self.handle ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.handle)
		end

		if self.data ~= nil and self.data ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.data)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamWriteRequestImpl.decode(input: buffer): StreamWriteRequest
		local self = _StreamWriteRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.handle = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.data = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamWriteRequestImpl.jsonEncode(self: StreamWriteRequest): any
		local output = {}

		if self.handle ~= nil and self.handle ~= "" then
			output.handle = self.handle
		end

		if self.data ~= nil and self.data ~= "" then
			output.data = self.data
		end

		return output
	end

	function _StreamWriteRequestImpl.jsonDecode(input: { [string]: any }): StreamWriteRequest
		local self = _StreamWriteRequestImpl.new()

		if input.handle ~= nil then
			self.handle = input.handle
		end

		if input.data ~= nil then
			self.data = input.data
		end

		return self
	end

	_StreamWriteRequestImpl.descriptor = {
		name = "StreamWriteRequest",
		fullName = "rodeo.runtime.StreamWriteRequest",
	}

	messages.StreamWriteRequest = _StreamWriteRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamWriteRequest)
end

do
	local _StreamReadBytesRequestImpl = {}
	_StreamReadBytesRequestImpl.__index = _StreamReadBytesRequestImpl

	function _StreamReadBytesRequestImpl.new(data: _StreamReadBytesRequestPartialFields?): StreamReadBytesRequest
		return setmetatable({
			handle = if data == nil or data.handle == nil then "" else data.handle,
		}, _StreamReadBytesRequestImpl :: _StreamReadBytesRequestImpl)
	end

	function _StreamReadBytesRequestImpl.encode(self: StreamReadBytesRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.handle ~= nil and self.handle ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.handle)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamReadBytesRequestImpl.decode(input: buffer): StreamReadBytesRequest
		local self = _StreamReadBytesRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.handle = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamReadBytesRequestImpl.jsonEncode(self: StreamReadBytesRequest): any
		local output = {}

		if self.handle ~= nil and self.handle ~= "" then
			output.handle = self.handle
		end

		return output
	end

	function _StreamReadBytesRequestImpl.jsonDecode(input: { [string]: any }): StreamReadBytesRequest
		local self = _StreamReadBytesRequestImpl.new()

		if input.handle ~= nil then
			self.handle = input.handle
		end

		return self
	end

	_StreamReadBytesRequestImpl.descriptor = {
		name = "StreamReadBytesRequest",
		fullName = "rodeo.runtime.StreamReadBytesRequest",
	}

	messages.StreamReadBytesRequest = _StreamReadBytesRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamReadBytesRequest)
end

do
	local _StreamReadBytesResponseImpl = {}
	_StreamReadBytesResponseImpl.__index = _StreamReadBytesResponseImpl

	function _StreamReadBytesResponseImpl.new(data: _StreamReadBytesResponsePartialFields?): StreamReadBytesResponse
		return setmetatable({
			data = if data == nil or data.data == nil then buffer.create(0) else data.data,
		}, _StreamReadBytesResponseImpl :: _StreamReadBytesResponseImpl)
	end

	function _StreamReadBytesResponseImpl.encode(self: StreamReadBytesResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.data ~= nil and buffer.len(self.data) > 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, self.data, buffer.len(self.data))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamReadBytesResponseImpl.decode(input: buffer): StreamReadBytesResponse
		local self = _StreamReadBytesResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.data = value
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamReadBytesResponseImpl.jsonEncode(self: StreamReadBytesResponse): any
		local output = {}

		if self.data ~= nil and buffer.len(self.data) > 0 then
			output.data = proto.json.serializeBuffer(self.data)
		end

		return output
	end

	function _StreamReadBytesResponseImpl.jsonDecode(input: { [string]: any }): StreamReadBytesResponse
		local self = _StreamReadBytesResponseImpl.new()

		if input.data ~= nil then
			self.data = proto.json.deserializeBuffer(input.data)
		end

		return self
	end

	_StreamReadBytesResponseImpl.descriptor = {
		name = "StreamReadBytesResponse",
		fullName = "rodeo.runtime.StreamReadBytesResponse",
	}

	messages.StreamReadBytesResponse = _StreamReadBytesResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamReadBytesResponse)
end

do
	local _StreamWriteBytesRequestImpl = {}
	_StreamWriteBytesRequestImpl.__index = _StreamWriteBytesRequestImpl

	function _StreamWriteBytesRequestImpl.new(data: _StreamWriteBytesRequestPartialFields?): StreamWriteBytesRequest
		return setmetatable({
			handle = if data == nil or data.handle == nil then "" else data.handle,
			data = if data == nil or data.data == nil then buffer.create(0) else data.data,
		}, _StreamWriteBytesRequestImpl :: _StreamWriteBytesRequestImpl)
	end

	function _StreamWriteBytesRequestImpl.encode(self: StreamWriteBytesRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.handle ~= nil and self.handle ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.handle)
		end

		if self.data ~= nil and buffer.len(self.data) > 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, self.data, buffer.len(self.data))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamWriteBytesRequestImpl.decode(input: buffer): StreamWriteBytesRequest
		local self = _StreamWriteBytesRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.handle = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.data = value
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamWriteBytesRequestImpl.jsonEncode(self: StreamWriteBytesRequest): any
		local output = {}

		if self.handle ~= nil and self.handle ~= "" then
			output.handle = self.handle
		end

		if self.data ~= nil and buffer.len(self.data) > 0 then
			output.data = proto.json.serializeBuffer(self.data)
		end

		return output
	end

	function _StreamWriteBytesRequestImpl.jsonDecode(input: { [string]: any }): StreamWriteBytesRequest
		local self = _StreamWriteBytesRequestImpl.new()

		if input.handle ~= nil then
			self.handle = input.handle
		end

		if input.data ~= nil then
			self.data = proto.json.deserializeBuffer(input.data)
		end

		return self
	end

	_StreamWriteBytesRequestImpl.descriptor = {
		name = "StreamWriteBytesRequest",
		fullName = "rodeo.runtime.StreamWriteBytesRequest",
	}

	messages.StreamWriteBytesRequest = _StreamWriteBytesRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamWriteBytesRequest)
end

do
	local _StreamCloseRequestImpl = {}
	_StreamCloseRequestImpl.__index = _StreamCloseRequestImpl

	function _StreamCloseRequestImpl.new(data: _StreamCloseRequestPartialFields?): StreamCloseRequest
		return setmetatable({
			handle = if data == nil or data.handle == nil then "" else data.handle,
		}, _StreamCloseRequestImpl :: _StreamCloseRequestImpl)
	end

	function _StreamCloseRequestImpl.encode(self: StreamCloseRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.handle ~= nil and self.handle ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.handle)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StreamCloseRequestImpl.decode(input: buffer): StreamCloseRequest
		local self = _StreamCloseRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.handle = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StreamCloseRequestImpl.jsonEncode(self: StreamCloseRequest): any
		local output = {}

		if self.handle ~= nil and self.handle ~= "" then
			output.handle = self.handle
		end

		return output
	end

	function _StreamCloseRequestImpl.jsonDecode(input: { [string]: any }): StreamCloseRequest
		local self = _StreamCloseRequestImpl.new()

		if input.handle ~= nil then
			self.handle = input.handle
		end

		return self
	end

	_StreamCloseRequestImpl.descriptor = {
		name = "StreamCloseRequest",
		fullName = "rodeo.runtime.StreamCloseRequest",
	}

	messages.StreamCloseRequest = _StreamCloseRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StreamCloseRequest)
end

do
	local _FsExistsRequestImpl = {}
	_FsExistsRequestImpl.__index = _FsExistsRequestImpl

	function _FsExistsRequestImpl.new(data: _FsExistsRequestPartialFields?): FsExistsRequest
		return setmetatable({
			path = if data == nil or data.path == nil then "" else data.path,
		}, _FsExistsRequestImpl :: _FsExistsRequestImpl)
	end

	function _FsExistsRequestImpl.encode(self: FsExistsRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.path ~= nil and self.path ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsExistsRequestImpl.decode(input: buffer): FsExistsRequest
		local self = _FsExistsRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsExistsRequestImpl.jsonEncode(self: FsExistsRequest): any
		local output = {}

		if self.path ~= nil and self.path ~= "" then
			output.path = self.path
		end

		return output
	end

	function _FsExistsRequestImpl.jsonDecode(input: { [string]: any }): FsExistsRequest
		local self = _FsExistsRequestImpl.new()

		if input.path ~= nil then
			self.path = input.path
		end

		return self
	end

	_FsExistsRequestImpl.descriptor = {
		name = "FsExistsRequest",
		fullName = "rodeo.runtime.FsExistsRequest",
	}

	messages.FsExistsRequest = _FsExistsRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsExistsRequest)
end

do
	local _FsExistsResponseImpl = {}
	_FsExistsResponseImpl.__index = _FsExistsResponseImpl

	function _FsExistsResponseImpl.new(data: _FsExistsResponsePartialFields?): FsExistsResponse
		return setmetatable({
			exists = if data == nil or data.exists == nil then false else data.exists,
		}, _FsExistsResponseImpl :: _FsExistsResponseImpl)
	end

	function _FsExistsResponseImpl.encode(self: FsExistsResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.exists then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.exists then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsExistsResponseImpl.decode(input: buffer): FsExistsResponse
		local self = _FsExistsResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.exists = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsExistsResponseImpl.jsonEncode(self: FsExistsResponse): any
		local output = {}

		if self.exists then
			output.exists = self.exists
		end

		return output
	end

	function _FsExistsResponseImpl.jsonDecode(input: { [string]: any }): FsExistsResponse
		local self = _FsExistsResponseImpl.new()

		if input.exists ~= nil then
			self.exists = input.exists
		end

		return self
	end

	_FsExistsResponseImpl.descriptor = {
		name = "FsExistsResponse",
		fullName = "rodeo.runtime.FsExistsResponse",
	}

	messages.FsExistsResponse = _FsExistsResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsExistsResponse)
end

do
	local _FsStatRequestImpl = {}
	_FsStatRequestImpl.__index = _FsStatRequestImpl

	function _FsStatRequestImpl.new(data: _FsStatRequestPartialFields?): FsStatRequest
		return setmetatable({
			path = if data == nil or data.path == nil then "" else data.path,
		}, _FsStatRequestImpl :: _FsStatRequestImpl)
	end

	function _FsStatRequestImpl.encode(self: FsStatRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.path ~= nil and self.path ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsStatRequestImpl.decode(input: buffer): FsStatRequest
		local self = _FsStatRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsStatRequestImpl.jsonEncode(self: FsStatRequest): any
		local output = {}

		if self.path ~= nil and self.path ~= "" then
			output.path = self.path
		end

		return output
	end

	function _FsStatRequestImpl.jsonDecode(input: { [string]: any }): FsStatRequest
		local self = _FsStatRequestImpl.new()

		if input.path ~= nil then
			self.path = input.path
		end

		return self
	end

	_FsStatRequestImpl.descriptor = {
		name = "FsStatRequest",
		fullName = "rodeo.runtime.FsStatRequest",
	}

	messages.FsStatRequest = _FsStatRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsStatRequest)
end

do
	local _FsStatResponseImpl = {}
	_FsStatResponseImpl.__index = _FsStatResponseImpl

	function _FsStatResponseImpl.new(data: _FsStatResponsePartialFields?): FsStatResponse
		return setmetatable({
			type = if data == nil or data.type == nil then "" else data.type,
			size = if data == nil or data.size == nil then 0 else data.size,
			createdMillis = if data == nil or data.createdMillis == nil then nil else data.createdMillis,
			modifiedMillis = if data == nil or data.modifiedMillis == nil then nil else data.modifiedMillis,
			accessedMillis = if data == nil or data.accessedMillis == nil then nil else data.accessedMillis,
		}, _FsStatResponseImpl :: _FsStatResponseImpl)
	end

	function _FsStatResponseImpl.encode(self: FsStatResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.type ~= nil and self.type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.type)
		end

		if self.size ~= nil and self.size ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.size)
		end

		if self.createdMillis ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.createdMillis)
		end

		if self.modifiedMillis ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.modifiedMillis)
		end

		if self.accessedMillis ~= nil then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.accessedMillis)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsStatResponseImpl.decode(input: buffer): FsStatResponse
		local self = _FsStatResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.size = value
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.createdMillis = value
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.modifiedMillis = value
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.accessedMillis = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.type = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsStatResponseImpl.jsonEncode(self: FsStatResponse): any
		local output = {}

		if self.type ~= nil and self.type ~= "" then
			output.type = self.type
		end

		if self.size ~= nil and self.size ~= 0 then
			output.size = self.size
		end

		if self.createdMillis ~= nil then
			output.createdMillis = self.createdMillis
		end

		if self.modifiedMillis ~= nil then
			output.modifiedMillis = self.modifiedMillis
		end

		if self.accessedMillis ~= nil then
			output.accessedMillis = self.accessedMillis
		end

		return output
	end

	function _FsStatResponseImpl.jsonDecode(input: { [string]: any }): FsStatResponse
		local self = _FsStatResponseImpl.new()

		if input.type ~= nil then
			self.type = input.type
		end

		if input.size ~= nil then
			self.size = input.size
		end

		if input.createdMillis ~= nil then
			self.createdMillis = input.createdMillis
		end

		if input.modifiedMillis ~= nil then
			self.modifiedMillis = input.modifiedMillis
		end

		if input.accessedMillis ~= nil then
			self.accessedMillis = input.accessedMillis
		end

		return self
	end

	_FsStatResponseImpl.descriptor = {
		name = "FsStatResponse",
		fullName = "rodeo.runtime.FsStatResponse",
	}

	messages.FsStatResponse = _FsStatResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsStatResponse)
end

do
	local _FsTypeRequestImpl = {}
	_FsTypeRequestImpl.__index = _FsTypeRequestImpl

	function _FsTypeRequestImpl.new(data: _FsTypeRequestPartialFields?): FsTypeRequest
		return setmetatable({
			path = if data == nil or data.path == nil then "" else data.path,
		}, _FsTypeRequestImpl :: _FsTypeRequestImpl)
	end

	function _FsTypeRequestImpl.encode(self: FsTypeRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.path ~= nil and self.path ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsTypeRequestImpl.decode(input: buffer): FsTypeRequest
		local self = _FsTypeRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsTypeRequestImpl.jsonEncode(self: FsTypeRequest): any
		local output = {}

		if self.path ~= nil and self.path ~= "" then
			output.path = self.path
		end

		return output
	end

	function _FsTypeRequestImpl.jsonDecode(input: { [string]: any }): FsTypeRequest
		local self = _FsTypeRequestImpl.new()

		if input.path ~= nil then
			self.path = input.path
		end

		return self
	end

	_FsTypeRequestImpl.descriptor = {
		name = "FsTypeRequest",
		fullName = "rodeo.runtime.FsTypeRequest",
	}

	messages.FsTypeRequest = _FsTypeRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsTypeRequest)
end

do
	local _FsTypeResponseImpl = {}
	_FsTypeResponseImpl.__index = _FsTypeResponseImpl

	function _FsTypeResponseImpl.new(data: _FsTypeResponsePartialFields?): FsTypeResponse
		return setmetatable({
			type = if data == nil or data.type == nil then nil else data.type,
		}, _FsTypeResponseImpl :: _FsTypeResponseImpl)
	end

	function _FsTypeResponseImpl.encode(self: FsTypeResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.type ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.type)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsTypeResponseImpl.decode(input: buffer): FsTypeResponse
		local self = _FsTypeResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.type = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsTypeResponseImpl.jsonEncode(self: FsTypeResponse): any
		local output = {}

		if self.type ~= nil then
			output.type = self.type
		end

		return output
	end

	function _FsTypeResponseImpl.jsonDecode(input: { [string]: any }): FsTypeResponse
		local self = _FsTypeResponseImpl.new()

		if input.type ~= nil then
			self.type = input.type
		end

		return self
	end

	_FsTypeResponseImpl.descriptor = {
		name = "FsTypeResponse",
		fullName = "rodeo.runtime.FsTypeResponse",
	}

	messages.FsTypeResponse = _FsTypeResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsTypeResponse)
end

do
	local _FsMkdirRequestImpl = {}
	_FsMkdirRequestImpl.__index = _FsMkdirRequestImpl

	function _FsMkdirRequestImpl.new(data: _FsMkdirRequestPartialFields?): FsMkdirRequest
		return setmetatable({
			path = if data == nil or data.path == nil then "" else data.path,
		}, _FsMkdirRequestImpl :: _FsMkdirRequestImpl)
	end

	function _FsMkdirRequestImpl.encode(self: FsMkdirRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.path ~= nil and self.path ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsMkdirRequestImpl.decode(input: buffer): FsMkdirRequest
		local self = _FsMkdirRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsMkdirRequestImpl.jsonEncode(self: FsMkdirRequest): any
		local output = {}

		if self.path ~= nil and self.path ~= "" then
			output.path = self.path
		end

		return output
	end

	function _FsMkdirRequestImpl.jsonDecode(input: { [string]: any }): FsMkdirRequest
		local self = _FsMkdirRequestImpl.new()

		if input.path ~= nil then
			self.path = input.path
		end

		return self
	end

	_FsMkdirRequestImpl.descriptor = {
		name = "FsMkdirRequest",
		fullName = "rodeo.runtime.FsMkdirRequest",
	}

	messages.FsMkdirRequest = _FsMkdirRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsMkdirRequest)
end

do
	local _FsListdirRequestImpl = {}
	_FsListdirRequestImpl.__index = _FsListdirRequestImpl

	function _FsListdirRequestImpl.new(data: _FsListdirRequestPartialFields?): FsListdirRequest
		return setmetatable({
			path = if data == nil or data.path == nil then "" else data.path,
		}, _FsListdirRequestImpl :: _FsListdirRequestImpl)
	end

	function _FsListdirRequestImpl.encode(self: FsListdirRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.path ~= nil and self.path ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsListdirRequestImpl.decode(input: buffer): FsListdirRequest
		local self = _FsListdirRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsListdirRequestImpl.jsonEncode(self: FsListdirRequest): any
		local output = {}

		if self.path ~= nil and self.path ~= "" then
			output.path = self.path
		end

		return output
	end

	function _FsListdirRequestImpl.jsonDecode(input: { [string]: any }): FsListdirRequest
		local self = _FsListdirRequestImpl.new()

		if input.path ~= nil then
			self.path = input.path
		end

		return self
	end

	_FsListdirRequestImpl.descriptor = {
		name = "FsListdirRequest",
		fullName = "rodeo.runtime.FsListdirRequest",
	}

	messages.FsListdirRequest = _FsListdirRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsListdirRequest)
end

do
	local _FsListdirResponseImpl = {}
	_FsListdirResponseImpl.__index = _FsListdirResponseImpl

	function _FsListdirResponseImpl.new(data: _FsListdirResponsePartialFields?): FsListdirResponse
		return setmetatable({
			entries = (function()
				local src = if data == nil then nil else data.entries
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.FsDirEntry.new(v :: any)
						else (v :: any) :: FsDirEntry
				end
				return out
			end)(),
		}, _FsListdirResponseImpl :: _FsListdirResponseImpl)
	end

	function _FsListdirResponseImpl.encode(self: FsListdirResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.entries ~= nil and #self.entries > 0 then
			for _, value in self.entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsListdirResponseImpl.decode(input: buffer): FsListdirResponse
		local self = _FsListdirResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.entries, messages.FsDirEntry.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsListdirResponseImpl.jsonEncode(self: FsListdirResponse): any
		local output = {}

		if self.entries ~= nil and #self.entries > 0 then
			local newOutput = {}
			for _, value in self.entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.entries = newOutput
		end

		return output
	end

	function _FsListdirResponseImpl.jsonDecode(input: { [string]: any }): FsListdirResponse
		local self = _FsListdirResponseImpl.new()

		if input.entries ~= nil then
			local newOutput: { FsDirEntry } = {}
			for _, value in input.entries do
				table.insert(newOutput, messages.FsDirEntry.jsonDecode(value))
			end

			self.entries = newOutput
		end

		return self
	end

	_FsListdirResponseImpl.descriptor = {
		name = "FsListdirResponse",
		fullName = "rodeo.runtime.FsListdirResponse",
	}

	messages.FsListdirResponse = _FsListdirResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsListdirResponse)
end

do
	local _FsDirEntryImpl = {}
	_FsDirEntryImpl.__index = _FsDirEntryImpl

	function _FsDirEntryImpl.new(data: _FsDirEntryPartialFields?): FsDirEntry
		return setmetatable({
			name = if data == nil or data.name == nil then "" else data.name,
			type = if data == nil or data.type == nil then "" else data.type,
		}, _FsDirEntryImpl :: _FsDirEntryImpl)
	end

	function _FsDirEntryImpl.encode(self: FsDirEntry): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.type ~= nil and self.type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.type)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsDirEntryImpl.decode(input: buffer): FsDirEntry
		local self = _FsDirEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.type = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsDirEntryImpl.jsonEncode(self: FsDirEntry): any
		local output = {}

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.type ~= nil and self.type ~= "" then
			output.type = self.type
		end

		return output
	end

	function _FsDirEntryImpl.jsonDecode(input: { [string]: any }): FsDirEntry
		local self = _FsDirEntryImpl.new()

		if input.name ~= nil then
			self.name = input.name
		end

		if input.type ~= nil then
			self.type = input.type
		end

		return self
	end

	_FsDirEntryImpl.descriptor = {
		name = "FsDirEntry",
		fullName = "rodeo.runtime.FsDirEntry",
	}

	messages.FsDirEntry = _FsDirEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsDirEntry)
end

do
	local _FsRemoveRequestImpl = {}
	_FsRemoveRequestImpl.__index = _FsRemoveRequestImpl

	function _FsRemoveRequestImpl.new(data: _FsRemoveRequestPartialFields?): FsRemoveRequest
		return setmetatable({
			path = if data == nil or data.path == nil then "" else data.path,
		}, _FsRemoveRequestImpl :: _FsRemoveRequestImpl)
	end

	function _FsRemoveRequestImpl.encode(self: FsRemoveRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.path ~= nil and self.path ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsRemoveRequestImpl.decode(input: buffer): FsRemoveRequest
		local self = _FsRemoveRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsRemoveRequestImpl.jsonEncode(self: FsRemoveRequest): any
		local output = {}

		if self.path ~= nil and self.path ~= "" then
			output.path = self.path
		end

		return output
	end

	function _FsRemoveRequestImpl.jsonDecode(input: { [string]: any }): FsRemoveRequest
		local self = _FsRemoveRequestImpl.new()

		if input.path ~= nil then
			self.path = input.path
		end

		return self
	end

	_FsRemoveRequestImpl.descriptor = {
		name = "FsRemoveRequest",
		fullName = "rodeo.runtime.FsRemoveRequest",
	}

	messages.FsRemoveRequest = _FsRemoveRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsRemoveRequest)
end

do
	local _FsRmdirRequestImpl = {}
	_FsRmdirRequestImpl.__index = _FsRmdirRequestImpl

	function _FsRmdirRequestImpl.new(data: _FsRmdirRequestPartialFields?): FsRmdirRequest
		return setmetatable({
			path = if data == nil or data.path == nil then "" else data.path,
		}, _FsRmdirRequestImpl :: _FsRmdirRequestImpl)
	end

	function _FsRmdirRequestImpl.encode(self: FsRmdirRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.path ~= nil and self.path ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsRmdirRequestImpl.decode(input: buffer): FsRmdirRequest
		local self = _FsRmdirRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsRmdirRequestImpl.jsonEncode(self: FsRmdirRequest): any
		local output = {}

		if self.path ~= nil and self.path ~= "" then
			output.path = self.path
		end

		return output
	end

	function _FsRmdirRequestImpl.jsonDecode(input: { [string]: any }): FsRmdirRequest
		local self = _FsRmdirRequestImpl.new()

		if input.path ~= nil then
			self.path = input.path
		end

		return self
	end

	_FsRmdirRequestImpl.descriptor = {
		name = "FsRmdirRequest",
		fullName = "rodeo.runtime.FsRmdirRequest",
	}

	messages.FsRmdirRequest = _FsRmdirRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsRmdirRequest)
end

do
	local _FsCopyRequestImpl = {}
	_FsCopyRequestImpl.__index = _FsCopyRequestImpl

	function _FsCopyRequestImpl.new(data: _FsCopyRequestPartialFields?): FsCopyRequest
		return setmetatable({
			src = if data == nil or data.src == nil then "" else data.src,
			dest = if data == nil or data.dest == nil then "" else data.dest,
		}, _FsCopyRequestImpl :: _FsCopyRequestImpl)
	end

	function _FsCopyRequestImpl.encode(self: FsCopyRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.src ~= nil and self.src ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.src)
		end

		if self.dest ~= nil and self.dest ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.dest)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FsCopyRequestImpl.decode(input: buffer): FsCopyRequest
		local self = _FsCopyRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.src = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.dest = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FsCopyRequestImpl.jsonEncode(self: FsCopyRequest): any
		local output = {}

		if self.src ~= nil and self.src ~= "" then
			output.src = self.src
		end

		if self.dest ~= nil and self.dest ~= "" then
			output.dest = self.dest
		end

		return output
	end

	function _FsCopyRequestImpl.jsonDecode(input: { [string]: any }): FsCopyRequest
		local self = _FsCopyRequestImpl.new()

		if input.src ~= nil then
			self.src = input.src
		end

		if input.dest ~= nil then
			self.dest = input.dest
		end

		return self
	end

	_FsCopyRequestImpl.descriptor = {
		name = "FsCopyRequest",
		fullName = "rodeo.runtime.FsCopyRequest",
	}

	messages.FsCopyRequest = _FsCopyRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FsCopyRequest)
end

do
	local _ProcessOptionsImpl = {}
	_ProcessOptionsImpl.__index = _ProcessOptionsImpl

	function _ProcessOptionsImpl.new(data: _ProcessOptionsPartialFields?): ProcessOptions
		return setmetatable({
			cwd = if data == nil or data.cwd == nil then nil else data.cwd,
			stdio = if data == nil or data.stdio == nil then nil else data.stdio,
		}, _ProcessOptionsImpl :: _ProcessOptionsImpl)
	end

	function _ProcessOptionsImpl.encode(self: ProcessOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.cwd ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.cwd)
		end

		if self.stdio ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.stdio)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessOptionsImpl.decode(input: buffer): ProcessOptions
		local self = _ProcessOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.cwd = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.stdio = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessOptionsImpl.jsonEncode(self: ProcessOptions): any
		local output = {}

		if self.cwd ~= nil then
			output.cwd = self.cwd
		end

		if self.stdio ~= nil then
			output.stdio = self.stdio
		end

		return output
	end

	function _ProcessOptionsImpl.jsonDecode(input: { [string]: any }): ProcessOptions
		local self = _ProcessOptionsImpl.new()

		if input.cwd ~= nil then
			self.cwd = input.cwd
		end

		if input.stdio ~= nil then
			self.stdio = input.stdio
		end

		return self
	end

	_ProcessOptionsImpl.descriptor = {
		name = "ProcessOptions",
		fullName = "rodeo.runtime.ProcessOptions",
	}

	messages.ProcessOptions = _ProcessOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessOptions)
end

do
	local _ProcessGetInfoRequestImpl = {}
	_ProcessGetInfoRequestImpl.__index = _ProcessGetInfoRequestImpl

	function _ProcessGetInfoRequestImpl.new(data: _ProcessGetInfoRequestPartialFields?): ProcessGetInfoRequest
		return setmetatable({}, _ProcessGetInfoRequestImpl :: _ProcessGetInfoRequestImpl)
	end

	function _ProcessGetInfoRequestImpl.encode(self: ProcessGetInfoRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessGetInfoRequestImpl.decode(input: buffer): ProcessGetInfoRequest
		local self = _ProcessGetInfoRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessGetInfoRequestImpl.jsonEncode(self: ProcessGetInfoRequest): any
		local output = {}

		return output
	end

	function _ProcessGetInfoRequestImpl.jsonDecode(input: { [string]: any }): ProcessGetInfoRequest
		local self = _ProcessGetInfoRequestImpl.new()

		return self
	end

	_ProcessGetInfoRequestImpl.descriptor = {
		name = "ProcessGetInfoRequest",
		fullName = "rodeo.runtime.ProcessGetInfoRequest",
	}

	messages.ProcessGetInfoRequest = _ProcessGetInfoRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessGetInfoRequest)
end

do
	local _ProcessGetInfoResponseImpl = {}
	_ProcessGetInfoResponseImpl.__index = _ProcessGetInfoResponseImpl

	function _ProcessGetInfoResponseImpl.new(data: _ProcessGetInfoResponsePartialFields?): ProcessGetInfoResponse
		return setmetatable({
			cwd = if data == nil or data.cwd == nil then "" else data.cwd,
			homedir = if data == nil or data.homedir == nil then "" else data.homedir,
			execpath = if data == nil or data.execpath == nil then "" else data.execpath,
			env = if data == nil or data.env == nil then {} else data.env,
			platform = if data == nil or data.platform == nil then nil else data.platform,
			arch = if data == nil or data.arch == nil then nil else data.arch,
			studioContentPath = if data == nil or data.studioContentPath == nil then nil else data.studioContentPath,
		}, _ProcessGetInfoResponseImpl :: _ProcessGetInfoResponseImpl)
	end

	function _ProcessGetInfoResponseImpl.encode(self: ProcessGetInfoResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.cwd ~= nil and self.cwd ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.cwd)
		end

		if self.homedir ~= nil and self.homedir ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.homedir)
		end

		if self.execpath ~= nil and self.execpath ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.execpath)
		end

		if self.env ~= nil and next(self.env) ~= nil then
			for key, value in self.env do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.platform ~= nil then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.platform)
		end

		if self.arch ~= nil then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.arch)
		end

		if self.studioContentPath ~= nil then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.studioContentPath)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessGetInfoResponseImpl.decode(input: buffer): ProcessGetInfoResponse
		local self = _ProcessGetInfoResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.cwd = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.homedir = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.execpath = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.ProcessGetInfoResponse_EnvEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.env[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.platform = proto.decodeString(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.arch = proto.decodeString(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.studioContentPath = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessGetInfoResponseImpl.jsonEncode(self: ProcessGetInfoResponse): any
		local output = {}

		if self.cwd ~= nil and self.cwd ~= "" then
			output.cwd = self.cwd
		end

		if self.homedir ~= nil and self.homedir ~= "" then
			output.homedir = self.homedir
		end

		if self.execpath ~= nil and self.execpath ~= "" then
			output.execpath = self.execpath
		end

		if self.env ~= nil and next(self.env) ~= nil then
			local newOutput = {}
			for key, value in self.env do
				newOutput[key] = value
			end
			output.env = newOutput
		end

		if self.platform ~= nil then
			output.platform = self.platform
		end

		if self.arch ~= nil then
			output.arch = self.arch
		end

		if self.studioContentPath ~= nil then
			output.studioContentPath = self.studioContentPath
		end

		return output
	end

	function _ProcessGetInfoResponseImpl.jsonDecode(input: { [string]: any }): ProcessGetInfoResponse
		local self = _ProcessGetInfoResponseImpl.new()

		if input.cwd ~= nil then
			self.cwd = input.cwd
		end

		if input.homedir ~= nil then
			self.homedir = input.homedir
		end

		if input.execpath ~= nil then
			self.execpath = input.execpath
		end

		if input.env ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.env do
				newOutput[key] = value
			end

			self.env = newOutput
		end

		if input.platform ~= nil then
			self.platform = input.platform
		end

		if input.arch ~= nil then
			self.arch = input.arch
		end

		if input.studioContentPath ~= nil then
			self.studioContentPath = input.studioContentPath
		end

		return self
	end

	_ProcessGetInfoResponseImpl.descriptor = {
		name = "ProcessGetInfoResponse",
		fullName = "rodeo.runtime.ProcessGetInfoResponse",
	}

	messages.ProcessGetInfoResponse = _ProcessGetInfoResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessGetInfoResponse)
end

do
	local _ProcessGetInfoResponse_EnvEntryImpl = {}
	_ProcessGetInfoResponse_EnvEntryImpl.__index = _ProcessGetInfoResponse_EnvEntryImpl

	function _ProcessGetInfoResponse_EnvEntryImpl.new(
		data: _ProcessGetInfoResponse_EnvEntryPartialFields?
	): ProcessGetInfoResponse_EnvEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then "" else data.value,
		}, _ProcessGetInfoResponse_EnvEntryImpl :: _ProcessGetInfoResponse_EnvEntryImpl)
	end

	function _ProcessGetInfoResponse_EnvEntryImpl.encode(self: ProcessGetInfoResponse_EnvEntry): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessGetInfoResponse_EnvEntryImpl.decode(input: buffer): ProcessGetInfoResponse_EnvEntry
		local self = _ProcessGetInfoResponse_EnvEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessGetInfoResponse_EnvEntryImpl.jsonEncode(self: ProcessGetInfoResponse_EnvEntry): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _ProcessGetInfoResponse_EnvEntryImpl.jsonDecode(input: { [string]: any }): ProcessGetInfoResponse_EnvEntry
		local self = _ProcessGetInfoResponse_EnvEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_ProcessGetInfoResponse_EnvEntryImpl.descriptor = {
		name = "ProcessGetInfoResponse_EnvEntry",
		fullName = "rodeo.runtime.EnvEntry",
	}

	messages.ProcessGetInfoResponse_EnvEntry = _ProcessGetInfoResponse_EnvEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessGetInfoResponse_EnvEntry)
end

do
	local _ProcessExitRequestImpl = {}
	_ProcessExitRequestImpl.__index = _ProcessExitRequestImpl

	function _ProcessExitRequestImpl.new(data: _ProcessExitRequestPartialFields?): ProcessExitRequest
		return setmetatable({
			code = if data == nil or data.code == nil then 0 else data.code,
		}, _ProcessExitRequestImpl :: _ProcessExitRequestImpl)
	end

	function _ProcessExitRequestImpl.encode(self: ProcessExitRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.code ~= nil and self.code ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.code)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessExitRequestImpl.decode(input: buffer): ProcessExitRequest
		local self = _ProcessExitRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.code = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessExitRequestImpl.jsonEncode(self: ProcessExitRequest): any
		local output = {}

		if self.code ~= nil and self.code ~= 0 then
			output.code = self.code
		end

		return output
	end

	function _ProcessExitRequestImpl.jsonDecode(input: { [string]: any }): ProcessExitRequest
		local self = _ProcessExitRequestImpl.new()

		if input.code ~= nil then
			self.code = input.code
		end

		return self
	end

	_ProcessExitRequestImpl.descriptor = {
		name = "ProcessExitRequest",
		fullName = "rodeo.runtime.ProcessExitRequest",
	}

	messages.ProcessExitRequest = _ProcessExitRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessExitRequest)
end

do
	local _ProcessRunRequestImpl = {}
	_ProcessRunRequestImpl.__index = _ProcessRunRequestImpl

	function _ProcessRunRequestImpl.new(data: _ProcessRunRequestPartialFields?): ProcessRunRequest
		return setmetatable({
			args = if data == nil or data.args == nil then {} else data.args,
			options = if data == nil or data.options == nil
				then nil
				elseif getmetatable(data.options :: any) == nil then messages.ProcessOptions.new(
					data.options :: any
				)
				else (data.options :: any) :: ProcessOptions,
		}, _ProcessRunRequestImpl :: _ProcessRunRequestImpl)
	end

	function _ProcessRunRequestImpl.encode(self: ProcessRunRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.args ~= nil and #self.args > 0 then
			for _, value in self.args do
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.options ~= nil then
			local encoded = self.options:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessRunRequestImpl.decode(input: buffer): ProcessRunRequest
		local self = _ProcessRunRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.args, proto.decodeString(value))
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.options = messages.ProcessOptions.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessRunRequestImpl.jsonEncode(self: ProcessRunRequest): any
		local output = {}

		if self.args ~= nil and #self.args > 0 then
			local newOutput = {}
			for _, value in self.args do
				table.insert(newOutput, value)
			end
			output.args = newOutput
		end

		if self.options ~= nil then
			output.options = self.options:jsonEncode()
		end

		return output
	end

	function _ProcessRunRequestImpl.jsonDecode(input: { [string]: any }): ProcessRunRequest
		local self = _ProcessRunRequestImpl.new()

		if input.args ~= nil then
			local newOutput: { string } = {}
			for _, value in input.args do
				table.insert(newOutput, value)
			end

			self.args = newOutput
		end

		if input.options ~= nil then
			self.options = messages.ProcessOptions.jsonDecode(input.options)
		end

		return self
	end

	_ProcessRunRequestImpl.descriptor = {
		name = "ProcessRunRequest",
		fullName = "rodeo.runtime.ProcessRunRequest",
	}

	messages.ProcessRunRequest = _ProcessRunRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessRunRequest)
end

do
	local _ProcessRunResponseImpl = {}
	_ProcessRunResponseImpl.__index = _ProcessRunResponseImpl

	function _ProcessRunResponseImpl.new(data: _ProcessRunResponsePartialFields?): ProcessRunResponse
		return setmetatable({
			ok = if data == nil or data.ok == nil then false else data.ok,
			exitcode = if data == nil or data.exitcode == nil then 0 else data.exitcode,
			out = if data == nil or data.out == nil then "" else data.out,
			err = if data == nil or data.err == nil then "" else data.err,
		}, _ProcessRunResponseImpl :: _ProcessRunResponseImpl)
	end

	function _ProcessRunResponseImpl.encode(self: ProcessRunResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.ok then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.ok then 1 else 0)
		end

		if self.exitcode ~= nil and self.exitcode ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.exitcode)
		end

		if self.out ~= nil and self.out ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.out)
		end

		if self.err ~= nil and self.err ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.err)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessRunResponseImpl.decode(input: buffer): ProcessRunResponse
		local self = _ProcessRunResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.ok = value ~= 0
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.exitcode = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.out = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.err = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessRunResponseImpl.jsonEncode(self: ProcessRunResponse): any
		local output = {}

		if self.ok then
			output.ok = self.ok
		end

		if self.exitcode ~= nil and self.exitcode ~= 0 then
			output.exitcode = self.exitcode
		end

		if self.out ~= nil and self.out ~= "" then
			output.out = self.out
		end

		if self.err ~= nil and self.err ~= "" then
			output.err = self.err
		end

		return output
	end

	function _ProcessRunResponseImpl.jsonDecode(input: { [string]: any }): ProcessRunResponse
		local self = _ProcessRunResponseImpl.new()

		if input.ok ~= nil then
			self.ok = input.ok
		end

		if input.exitcode ~= nil then
			self.exitcode = input.exitcode
		end

		if input.out ~= nil then
			self.out = input.out
		end

		if input.err ~= nil then
			self.err = input.err
		end

		return self
	end

	_ProcessRunResponseImpl.descriptor = {
		name = "ProcessRunResponse",
		fullName = "rodeo.runtime.ProcessRunResponse",
	}

	messages.ProcessRunResponse = _ProcessRunResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessRunResponse)
end

do
	local _ProcessSystemRequestImpl = {}
	_ProcessSystemRequestImpl.__index = _ProcessSystemRequestImpl

	function _ProcessSystemRequestImpl.new(data: _ProcessSystemRequestPartialFields?): ProcessSystemRequest
		return setmetatable({
			command = if data == nil or data.command == nil then "" else data.command,
			options = if data == nil or data.options == nil
				then nil
				elseif getmetatable(data.options :: any) == nil then messages.ProcessOptions.new(
					data.options :: any
				)
				else (data.options :: any) :: ProcessOptions,
		}, _ProcessSystemRequestImpl :: _ProcessSystemRequestImpl)
	end

	function _ProcessSystemRequestImpl.encode(self: ProcessSystemRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.command ~= nil and self.command ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.command)
		end

		if self.options ~= nil then
			local encoded = self.options:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessSystemRequestImpl.decode(input: buffer): ProcessSystemRequest
		local self = _ProcessSystemRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.command = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.options = messages.ProcessOptions.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessSystemRequestImpl.jsonEncode(self: ProcessSystemRequest): any
		local output = {}

		if self.command ~= nil and self.command ~= "" then
			output.command = self.command
		end

		if self.options ~= nil then
			output.options = self.options:jsonEncode()
		end

		return output
	end

	function _ProcessSystemRequestImpl.jsonDecode(input: { [string]: any }): ProcessSystemRequest
		local self = _ProcessSystemRequestImpl.new()

		if input.command ~= nil then
			self.command = input.command
		end

		if input.options ~= nil then
			self.options = messages.ProcessOptions.jsonDecode(input.options)
		end

		return self
	end

	_ProcessSystemRequestImpl.descriptor = {
		name = "ProcessSystemRequest",
		fullName = "rodeo.runtime.ProcessSystemRequest",
	}

	messages.ProcessSystemRequest = _ProcessSystemRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessSystemRequest)
end

do
	local _ProcessCreateRequestImpl = {}
	_ProcessCreateRequestImpl.__index = _ProcessCreateRequestImpl

	function _ProcessCreateRequestImpl.new(data: _ProcessCreateRequestPartialFields?): ProcessCreateRequest
		return setmetatable({
			args = if data == nil or data.args == nil then {} else data.args,
			options = if data == nil or data.options == nil
				then nil
				elseif getmetatable(data.options :: any) == nil then messages.ProcessOptions.new(
					data.options :: any
				)
				else (data.options :: any) :: ProcessOptions,
		}, _ProcessCreateRequestImpl :: _ProcessCreateRequestImpl)
	end

	function _ProcessCreateRequestImpl.encode(self: ProcessCreateRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.args ~= nil and #self.args > 0 then
			for _, value in self.args do
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.options ~= nil then
			local encoded = self.options:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessCreateRequestImpl.decode(input: buffer): ProcessCreateRequest
		local self = _ProcessCreateRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.args, proto.decodeString(value))
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.options = messages.ProcessOptions.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessCreateRequestImpl.jsonEncode(self: ProcessCreateRequest): any
		local output = {}

		if self.args ~= nil and #self.args > 0 then
			local newOutput = {}
			for _, value in self.args do
				table.insert(newOutput, value)
			end
			output.args = newOutput
		end

		if self.options ~= nil then
			output.options = self.options:jsonEncode()
		end

		return output
	end

	function _ProcessCreateRequestImpl.jsonDecode(input: { [string]: any }): ProcessCreateRequest
		local self = _ProcessCreateRequestImpl.new()

		if input.args ~= nil then
			local newOutput: { string } = {}
			for _, value in input.args do
				table.insert(newOutput, value)
			end

			self.args = newOutput
		end

		if input.options ~= nil then
			self.options = messages.ProcessOptions.jsonDecode(input.options)
		end

		return self
	end

	_ProcessCreateRequestImpl.descriptor = {
		name = "ProcessCreateRequest",
		fullName = "rodeo.runtime.ProcessCreateRequest",
	}

	messages.ProcessCreateRequest = _ProcessCreateRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessCreateRequest)
end

do
	local _ProcessCreateResponseImpl = {}
	_ProcessCreateResponseImpl.__index = _ProcessCreateResponseImpl

	function _ProcessCreateResponseImpl.new(data: _ProcessCreateResponsePartialFields?): ProcessCreateResponse
		return setmetatable({
			pid = if data == nil or data.pid == nil then "" else data.pid,
			stdinHandle = if data == nil or data.stdinHandle == nil then nil else data.stdinHandle,
			stdoutHandle = if data == nil or data.stdoutHandle == nil then nil else data.stdoutHandle,
			stderrHandle = if data == nil or data.stderrHandle == nil then nil else data.stderrHandle,
		}, _ProcessCreateResponseImpl :: _ProcessCreateResponseImpl)
	end

	function _ProcessCreateResponseImpl.encode(self: ProcessCreateResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.pid ~= nil and self.pid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.pid)
		end

		if self.stdinHandle ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.stdinHandle)
		end

		if self.stdoutHandle ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.stdoutHandle)
		end

		if self.stderrHandle ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.stderrHandle)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessCreateResponseImpl.decode(input: buffer): ProcessCreateResponse
		local self = _ProcessCreateResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.pid = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.stdinHandle = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.stdoutHandle = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.stderrHandle = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessCreateResponseImpl.jsonEncode(self: ProcessCreateResponse): any
		local output = {}

		if self.pid ~= nil and self.pid ~= "" then
			output.pid = self.pid
		end

		if self.stdinHandle ~= nil then
			output.stdinHandle = self.stdinHandle
		end

		if self.stdoutHandle ~= nil then
			output.stdoutHandle = self.stdoutHandle
		end

		if self.stderrHandle ~= nil then
			output.stderrHandle = self.stderrHandle
		end

		return output
	end

	function _ProcessCreateResponseImpl.jsonDecode(input: { [string]: any }): ProcessCreateResponse
		local self = _ProcessCreateResponseImpl.new()

		if input.pid ~= nil then
			self.pid = input.pid
		end

		if input.stdinHandle ~= nil then
			self.stdinHandle = input.stdinHandle
		end

		if input.stdoutHandle ~= nil then
			self.stdoutHandle = input.stdoutHandle
		end

		if input.stderrHandle ~= nil then
			self.stderrHandle = input.stderrHandle
		end

		return self
	end

	_ProcessCreateResponseImpl.descriptor = {
		name = "ProcessCreateResponse",
		fullName = "rodeo.runtime.ProcessCreateResponse",
	}

	messages.ProcessCreateResponse = _ProcessCreateResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessCreateResponse)
end

do
	local _ProcessRunHandleRequestImpl = {}
	_ProcessRunHandleRequestImpl.__index = _ProcessRunHandleRequestImpl

	function _ProcessRunHandleRequestImpl.new(data: _ProcessRunHandleRequestPartialFields?): ProcessRunHandleRequest
		return setmetatable({
			pid = if data == nil or data.pid == nil then "" else data.pid,
		}, _ProcessRunHandleRequestImpl :: _ProcessRunHandleRequestImpl)
	end

	function _ProcessRunHandleRequestImpl.encode(self: ProcessRunHandleRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.pid ~= nil and self.pid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.pid)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessRunHandleRequestImpl.decode(input: buffer): ProcessRunHandleRequest
		local self = _ProcessRunHandleRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.pid = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessRunHandleRequestImpl.jsonEncode(self: ProcessRunHandleRequest): any
		local output = {}

		if self.pid ~= nil and self.pid ~= "" then
			output.pid = self.pid
		end

		return output
	end

	function _ProcessRunHandleRequestImpl.jsonDecode(input: { [string]: any }): ProcessRunHandleRequest
		local self = _ProcessRunHandleRequestImpl.new()

		if input.pid ~= nil then
			self.pid = input.pid
		end

		return self
	end

	_ProcessRunHandleRequestImpl.descriptor = {
		name = "ProcessRunHandleRequest",
		fullName = "rodeo.runtime.ProcessRunHandleRequest",
	}

	messages.ProcessRunHandleRequest = _ProcessRunHandleRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessRunHandleRequest)
end

do
	local _ProcessKillRequestImpl = {}
	_ProcessKillRequestImpl.__index = _ProcessKillRequestImpl

	function _ProcessKillRequestImpl.new(data: _ProcessKillRequestPartialFields?): ProcessKillRequest
		return setmetatable({
			pid = if data == nil or data.pid == nil then "" else data.pid,
		}, _ProcessKillRequestImpl :: _ProcessKillRequestImpl)
	end

	function _ProcessKillRequestImpl.encode(self: ProcessKillRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.pid ~= nil and self.pid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.pid)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessKillRequestImpl.decode(input: buffer): ProcessKillRequest
		local self = _ProcessKillRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.pid = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessKillRequestImpl.jsonEncode(self: ProcessKillRequest): any
		local output = {}

		if self.pid ~= nil and self.pid ~= "" then
			output.pid = self.pid
		end

		return output
	end

	function _ProcessKillRequestImpl.jsonDecode(input: { [string]: any }): ProcessKillRequest
		local self = _ProcessKillRequestImpl.new()

		if input.pid ~= nil then
			self.pid = input.pid
		end

		return self
	end

	_ProcessKillRequestImpl.descriptor = {
		name = "ProcessKillRequest",
		fullName = "rodeo.runtime.ProcessKillRequest",
	}

	messages.ProcessKillRequest = _ProcessKillRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessKillRequest)
end

do
	local _McpCallRequestImpl = {}
	_McpCallRequestImpl.__index = _McpCallRequestImpl

	function _McpCallRequestImpl.new(data: _McpCallRequestPartialFields?): McpCallRequest
		return setmetatable({
			tool = if data == nil or data.tool == nil then "" else data.tool,
			argumentsJson = if data == nil or data.argumentsJson == nil then "" else data.argumentsJson,
			mcpStudioId = if data == nil or data.mcpStudioId == nil then "" else data.mcpStudioId,
		}, _McpCallRequestImpl :: _McpCallRequestImpl)
	end

	function _McpCallRequestImpl.encode(self: McpCallRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.tool ~= nil and self.tool ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.tool)
		end

		if self.argumentsJson ~= nil and self.argumentsJson ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.argumentsJson)
		end

		if self.mcpStudioId ~= nil and self.mcpStudioId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mcpStudioId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _McpCallRequestImpl.decode(input: buffer): McpCallRequest
		local self = _McpCallRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.tool = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.argumentsJson = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mcpStudioId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _McpCallRequestImpl.jsonEncode(self: McpCallRequest): any
		local output = {}

		if self.tool ~= nil and self.tool ~= "" then
			output.tool = self.tool
		end

		if self.argumentsJson ~= nil and self.argumentsJson ~= "" then
			output.argumentsJson = self.argumentsJson
		end

		if self.mcpStudioId ~= nil and self.mcpStudioId ~= "" then
			output.mcpStudioId = self.mcpStudioId
		end

		return output
	end

	function _McpCallRequestImpl.jsonDecode(input: { [string]: any }): McpCallRequest
		local self = _McpCallRequestImpl.new()

		if input.tool ~= nil then
			self.tool = input.tool
		end

		if input.argumentsJson ~= nil then
			self.argumentsJson = input.argumentsJson
		end

		if input.mcpStudioId ~= nil then
			self.mcpStudioId = input.mcpStudioId
		end

		return self
	end

	_McpCallRequestImpl.descriptor = {
		name = "McpCallRequest",
		fullName = "rodeo.runtime.McpCallRequest",
	}

	messages.McpCallRequest = _McpCallRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.McpCallRequest)
end

do
	local _McpCallResponseImpl = {}
	_McpCallResponseImpl.__index = _McpCallResponseImpl

	function _McpCallResponseImpl.new(data: _McpCallResponsePartialFields?): McpCallResponse
		return setmetatable({
			result = if data == nil or data.result == nil then "" else data.result,
		}, _McpCallResponseImpl :: _McpCallResponseImpl)
	end

	function _McpCallResponseImpl.encode(self: McpCallResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.result ~= nil and self.result ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.result)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _McpCallResponseImpl.decode(input: buffer): McpCallResponse
		local self = _McpCallResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.result = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _McpCallResponseImpl.jsonEncode(self: McpCallResponse): any
		local output = {}

		if self.result ~= nil and self.result ~= "" then
			output.result = self.result
		end

		return output
	end

	function _McpCallResponseImpl.jsonDecode(input: { [string]: any }): McpCallResponse
		local self = _McpCallResponseImpl.new()

		if input.result ~= nil then
			self.result = input.result
		end

		return self
	end

	_McpCallResponseImpl.descriptor = {
		name = "McpCallResponse",
		fullName = "rodeo.runtime.McpCallResponse",
	}

	messages.McpCallResponse = _McpCallResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.McpCallResponse)
end

do
	local _ClientRpcCallImpl = {}
	_ClientRpcCallImpl.__index = _ClientRpcCallImpl

	function _ClientRpcCallImpl.new(data: _ClientRpcCallPartialFields?): ClientRpcCall
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
			req = (function()
				if data == nil or data.req == nil then
					return nil
				end
				local v = data.req
				if
					(v :: any).type == "fsExists"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsExistsRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsStat"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsStatRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsType"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsTypeRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsMkdir"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsMkdirRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsListdir"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsListdirRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsRemove"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsRemoveRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsRmdir"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsRmdirRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsCopy"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsCopyRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "streamOpen"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StreamOpenRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "streamReadChunk"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.StreamReadChunkRequest.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "streamReadLine"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.StreamReadLineRequest.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "streamReadAll"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.StreamReadAllRequest.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "streamWrite"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StreamWriteRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "streamClose"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StreamCloseRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "streamReadBytes"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.StreamReadBytesRequest.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "streamWriteBytes"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.StreamWriteBytesRequest.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "processGetInfo"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.ProcessGetInfoRequest.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "processExit"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ProcessExitRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "processRun"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ProcessRunRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "processSystem"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.ProcessSystemRequest.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "processCreate"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.ProcessCreateRequest.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "processRunHandle"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.ProcessRunHandleRequest.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "processKill"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ProcessKillRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "mcpCall"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.McpCallRequest.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _ClientRpcCallImpl :: _ClientRpcCallImpl)
	end

	function _ClientRpcCallImpl.encode(self: ClientRpcCall): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		if self.req ~= nil then
			if self.req.type == "fsExists" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "fsStat" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "fsType" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "fsMkdir" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "fsListdir" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "fsRemove" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "fsRmdir" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "fsCopy" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 17, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "streamOpen" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 20, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "streamReadChunk" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 21, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "streamReadLine" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 22, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "streamReadAll" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 23, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "streamWrite" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 24, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "streamClose" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 25, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "streamReadBytes" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 26, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "streamWriteBytes" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 27, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "processGetInfo" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 30, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "processExit" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 31, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "processRun" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 32, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "processSystem" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 33, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "processCreate" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 34, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "processRunHandle" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 35, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "processKill" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 36, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.req.type == "mcpCall" then
				local encoded = self.req.value:encode()
				output, cursor = proto.writeTag(output, cursor, 40, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ClientRpcCallImpl.decode(input: buffer): ClientRpcCall
		local self = _ClientRpcCallImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "fsExists", value = messages.FsExistsRequest.decode(value) }
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "fsStat", value = messages.FsStatRequest.decode(value) }
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "fsType", value = messages.FsTypeRequest.decode(value) }
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "fsMkdir", value = messages.FsMkdirRequest.decode(value) }
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "fsListdir", value = messages.FsListdirRequest.decode(value) }
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "fsRemove", value = messages.FsRemoveRequest.decode(value) }
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "fsRmdir", value = messages.FsRmdirRequest.decode(value) }
					continue
				elseif field == 17 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "fsCopy", value = messages.FsCopyRequest.decode(value) }
					continue
				elseif field == 20 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "streamOpen", value = messages.StreamOpenRequest.decode(value) }
					continue
				elseif field == 21 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "streamReadChunk", value = messages.StreamReadChunkRequest.decode(value) }
					continue
				elseif field == 22 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "streamReadLine", value = messages.StreamReadLineRequest.decode(value) }
					continue
				elseif field == 23 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "streamReadAll", value = messages.StreamReadAllRequest.decode(value) }
					continue
				elseif field == 24 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "streamWrite", value = messages.StreamWriteRequest.decode(value) }
					continue
				elseif field == 25 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "streamClose", value = messages.StreamCloseRequest.decode(value) }
					continue
				elseif field == 26 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "streamReadBytes", value = messages.StreamReadBytesRequest.decode(value) }
					continue
				elseif field == 27 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "streamWriteBytes", value = messages.StreamWriteBytesRequest.decode(value) }
					continue
				elseif field == 30 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "processGetInfo", value = messages.ProcessGetInfoRequest.decode(value) }
					continue
				elseif field == 31 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "processExit", value = messages.ProcessExitRequest.decode(value) }
					continue
				elseif field == 32 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "processRun", value = messages.ProcessRunRequest.decode(value) }
					continue
				elseif field == 33 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "processSystem", value = messages.ProcessSystemRequest.decode(value) }
					continue
				elseif field == 34 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "processCreate", value = messages.ProcessCreateRequest.decode(value) }
					continue
				elseif field == 35 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "processRunHandle", value = messages.ProcessRunHandleRequest.decode(value) }
					continue
				elseif field == 36 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "processKill", value = messages.ProcessKillRequest.decode(value) }
					continue
				elseif field == 40 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.req = { type = "mcpCall", value = messages.McpCallRequest.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ClientRpcCallImpl.jsonEncode(self: ClientRpcCall): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		if self.req ~= nil then
			if self.req.type == "fsExists" then
				output.fsExists = self.req.value:jsonEncode()
			elseif self.req.type == "fsStat" then
				output.fsStat = self.req.value:jsonEncode()
			elseif self.req.type == "fsType" then
				output.fsType = self.req.value:jsonEncode()
			elseif self.req.type == "fsMkdir" then
				output.fsMkdir = self.req.value:jsonEncode()
			elseif self.req.type == "fsListdir" then
				output.fsListdir = self.req.value:jsonEncode()
			elseif self.req.type == "fsRemove" then
				output.fsRemove = self.req.value:jsonEncode()
			elseif self.req.type == "fsRmdir" then
				output.fsRmdir = self.req.value:jsonEncode()
			elseif self.req.type == "fsCopy" then
				output.fsCopy = self.req.value:jsonEncode()
			elseif self.req.type == "streamOpen" then
				output.streamOpen = self.req.value:jsonEncode()
			elseif self.req.type == "streamReadChunk" then
				output.streamReadChunk = self.req.value:jsonEncode()
			elseif self.req.type == "streamReadLine" then
				output.streamReadLine = self.req.value:jsonEncode()
			elseif self.req.type == "streamReadAll" then
				output.streamReadAll = self.req.value:jsonEncode()
			elseif self.req.type == "streamWrite" then
				output.streamWrite = self.req.value:jsonEncode()
			elseif self.req.type == "streamClose" then
				output.streamClose = self.req.value:jsonEncode()
			elseif self.req.type == "streamReadBytes" then
				output.streamReadBytes = self.req.value:jsonEncode()
			elseif self.req.type == "streamWriteBytes" then
				output.streamWriteBytes = self.req.value:jsonEncode()
			elseif self.req.type == "processGetInfo" then
				output.processGetInfo = self.req.value:jsonEncode()
			elseif self.req.type == "processExit" then
				output.processExit = self.req.value:jsonEncode()
			elseif self.req.type == "processRun" then
				output.processRun = self.req.value:jsonEncode()
			elseif self.req.type == "processSystem" then
				output.processSystem = self.req.value:jsonEncode()
			elseif self.req.type == "processCreate" then
				output.processCreate = self.req.value:jsonEncode()
			elseif self.req.type == "processRunHandle" then
				output.processRunHandle = self.req.value:jsonEncode()
			elseif self.req.type == "processKill" then
				output.processKill = self.req.value:jsonEncode()
			elseif self.req.type == "mcpCall" then
				output.mcpCall = self.req.value:jsonEncode()
			end
		end

		return output
	end

	function _ClientRpcCallImpl.jsonDecode(input: { [string]: any }): ClientRpcCall
		local self = _ClientRpcCallImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		if input.fsExists ~= nil then
			self.req = { type = "fsExists", value = messages.FsExistsRequest.jsonDecode(input.fsExists) }
		end

		if input.fsStat ~= nil then
			self.req = { type = "fsStat", value = messages.FsStatRequest.jsonDecode(input.fsStat) }
		end

		if input.fsType ~= nil then
			self.req = { type = "fsType", value = messages.FsTypeRequest.jsonDecode(input.fsType) }
		end

		if input.fsMkdir ~= nil then
			self.req = { type = "fsMkdir", value = messages.FsMkdirRequest.jsonDecode(input.fsMkdir) }
		end

		if input.fsListdir ~= nil then
			self.req = { type = "fsListdir", value = messages.FsListdirRequest.jsonDecode(input.fsListdir) }
		end

		if input.fsRemove ~= nil then
			self.req = { type = "fsRemove", value = messages.FsRemoveRequest.jsonDecode(input.fsRemove) }
		end

		if input.fsRmdir ~= nil then
			self.req = { type = "fsRmdir", value = messages.FsRmdirRequest.jsonDecode(input.fsRmdir) }
		end

		if input.fsCopy ~= nil then
			self.req = { type = "fsCopy", value = messages.FsCopyRequest.jsonDecode(input.fsCopy) }
		end

		if input.streamOpen ~= nil then
			self.req = { type = "streamOpen", value = messages.StreamOpenRequest.jsonDecode(input.streamOpen) }
		end

		if input.streamReadChunk ~= nil then
			self.req =
				{ type = "streamReadChunk", value = messages.StreamReadChunkRequest.jsonDecode(input.streamReadChunk) }
		end

		if input.streamReadLine ~= nil then
			self.req =
				{ type = "streamReadLine", value = messages.StreamReadLineRequest.jsonDecode(input.streamReadLine) }
		end

		if input.streamReadAll ~= nil then
			self.req = { type = "streamReadAll", value = messages.StreamReadAllRequest.jsonDecode(input.streamReadAll) }
		end

		if input.streamWrite ~= nil then
			self.req = { type = "streamWrite", value = messages.StreamWriteRequest.jsonDecode(input.streamWrite) }
		end

		if input.streamClose ~= nil then
			self.req = { type = "streamClose", value = messages.StreamCloseRequest.jsonDecode(input.streamClose) }
		end

		if input.streamReadBytes ~= nil then
			self.req =
				{ type = "streamReadBytes", value = messages.StreamReadBytesRequest.jsonDecode(input.streamReadBytes) }
		end

		if input.streamWriteBytes ~= nil then
			self.req = {
				type = "streamWriteBytes",
				value = messages.StreamWriteBytesRequest.jsonDecode(input.streamWriteBytes),
			}
		end

		if input.processGetInfo ~= nil then
			self.req =
				{ type = "processGetInfo", value = messages.ProcessGetInfoRequest.jsonDecode(input.processGetInfo) }
		end

		if input.processExit ~= nil then
			self.req = { type = "processExit", value = messages.ProcessExitRequest.jsonDecode(input.processExit) }
		end

		if input.processRun ~= nil then
			self.req = { type = "processRun", value = messages.ProcessRunRequest.jsonDecode(input.processRun) }
		end

		if input.processSystem ~= nil then
			self.req = { type = "processSystem", value = messages.ProcessSystemRequest.jsonDecode(input.processSystem) }
		end

		if input.processCreate ~= nil then
			self.req = { type = "processCreate", value = messages.ProcessCreateRequest.jsonDecode(input.processCreate) }
		end

		if input.processRunHandle ~= nil then
			self.req = {
				type = "processRunHandle",
				value = messages.ProcessRunHandleRequest.jsonDecode(input.processRunHandle),
			}
		end

		if input.processKill ~= nil then
			self.req = { type = "processKill", value = messages.ProcessKillRequest.jsonDecode(input.processKill) }
		end

		if input.mcpCall ~= nil then
			self.req = { type = "mcpCall", value = messages.McpCallRequest.jsonDecode(input.mcpCall) }
		end

		return self
	end

	_ClientRpcCallImpl.descriptor = {
		name = "ClientRpcCall",
		fullName = "rodeo.runtime.ClientRpcCall",
	}

	messages.ClientRpcCall = _ClientRpcCallImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ClientRpcCall)
end

do
	local _ClientRpcResponseImpl = {}
	_ClientRpcResponseImpl.__index = _ClientRpcResponseImpl

	function _ClientRpcResponseImpl.new(data: _ClientRpcResponsePartialFields?): ClientRpcResponse
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
			res = (function()
				if data == nil or data.res == nil then
					return nil
				end
				local v = data.res
				if
					(v :: any).type == "fsExists"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsExistsResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsStat"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsStatResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsType"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsTypeResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsMkdir"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.Ok.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsListdir"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FsListdirResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsRemove"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.Ok.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsRmdir"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.Ok.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fsCopy"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.Ok.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "streamOpen"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StreamOpenResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "streamReadChunk"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.StreamReadChunkResponse.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "streamReadLine"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.StreamReadLineResponse.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "streamReadAll"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.StreamReadAllResponse.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "streamWrite"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.Ok.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "streamClose"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.Ok.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "streamReadBytes"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.StreamReadBytesResponse.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "streamWriteBytes"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.Ok.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "processGetInfo"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.ProcessGetInfoResponse.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "processExit"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.Ok.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "processRun"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ProcessRunResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "processSystem"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ProcessRunResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "processCreate"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return {
							type = (v :: any).type,
							value = messages.ProcessCreateResponse.new((v :: any).value),
						} :: any
				elseif
					(v :: any).type == "processRunHandle"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ProcessRunResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "processKill"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.Ok.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "mcpCall"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.McpCallResponse.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _ClientRpcResponseImpl :: _ClientRpcResponseImpl)
	end

	function _ClientRpcResponseImpl.encode(self: ClientRpcResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		if self.res ~= nil then
			if self.res.type == "error" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.res.value)
			elseif self.res.type == "fsExists" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "fsStat" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "fsType" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "fsMkdir" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "fsListdir" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "fsRemove" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "fsRmdir" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "fsCopy" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 17, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "streamOpen" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 20, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "streamReadChunk" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 21, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "streamReadLine" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 22, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "streamReadAll" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 23, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "streamWrite" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 24, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "streamClose" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 25, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "streamReadBytes" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 26, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "streamWriteBytes" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 27, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "processGetInfo" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 30, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "processExit" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 31, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "processRun" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 32, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "processSystem" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 33, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "processCreate" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 34, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "processRunHandle" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 35, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "processKill" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 36, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.res.type == "mcpCall" then
				local encoded = self.res.value:encode()
				output, cursor = proto.writeTag(output, cursor, 40, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ClientRpcResponseImpl.decode(input: buffer): ClientRpcResponse
		local self = _ClientRpcResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "error", value = proto.decodeString(value) }
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "fsExists", value = messages.FsExistsResponse.decode(value) }
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "fsStat", value = messages.FsStatResponse.decode(value) }
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "fsType", value = messages.FsTypeResponse.decode(value) }
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "fsMkdir", value = messages.Ok.decode(value) }
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "fsListdir", value = messages.FsListdirResponse.decode(value) }
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "fsRemove", value = messages.Ok.decode(value) }
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "fsRmdir", value = messages.Ok.decode(value) }
					continue
				elseif field == 17 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "fsCopy", value = messages.Ok.decode(value) }
					continue
				elseif field == 20 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "streamOpen", value = messages.StreamOpenResponse.decode(value) }
					continue
				elseif field == 21 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "streamReadChunk", value = messages.StreamReadChunkResponse.decode(value) }
					continue
				elseif field == 22 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "streamReadLine", value = messages.StreamReadLineResponse.decode(value) }
					continue
				elseif field == 23 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "streamReadAll", value = messages.StreamReadAllResponse.decode(value) }
					continue
				elseif field == 24 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "streamWrite", value = messages.Ok.decode(value) }
					continue
				elseif field == 25 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "streamClose", value = messages.Ok.decode(value) }
					continue
				elseif field == 26 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "streamReadBytes", value = messages.StreamReadBytesResponse.decode(value) }
					continue
				elseif field == 27 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "streamWriteBytes", value = messages.Ok.decode(value) }
					continue
				elseif field == 30 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "processGetInfo", value = messages.ProcessGetInfoResponse.decode(value) }
					continue
				elseif field == 31 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "processExit", value = messages.Ok.decode(value) }
					continue
				elseif field == 32 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "processRun", value = messages.ProcessRunResponse.decode(value) }
					continue
				elseif field == 33 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "processSystem", value = messages.ProcessRunResponse.decode(value) }
					continue
				elseif field == 34 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "processCreate", value = messages.ProcessCreateResponse.decode(value) }
					continue
				elseif field == 35 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "processRunHandle", value = messages.ProcessRunResponse.decode(value) }
					continue
				elseif field == 36 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "processKill", value = messages.Ok.decode(value) }
					continue
				elseif field == 40 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.res = { type = "mcpCall", value = messages.McpCallResponse.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ClientRpcResponseImpl.jsonEncode(self: ClientRpcResponse): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		if self.res ~= nil then
			if self.res.type == "error" then
				output.error = self.res.value
			elseif self.res.type == "fsExists" then
				output.fsExists = self.res.value:jsonEncode()
			elseif self.res.type == "fsStat" then
				output.fsStat = self.res.value:jsonEncode()
			elseif self.res.type == "fsType" then
				output.fsType = self.res.value:jsonEncode()
			elseif self.res.type == "fsMkdir" then
				output.fsMkdir = self.res.value:jsonEncode()
			elseif self.res.type == "fsListdir" then
				output.fsListdir = self.res.value:jsonEncode()
			elseif self.res.type == "fsRemove" then
				output.fsRemove = self.res.value:jsonEncode()
			elseif self.res.type == "fsRmdir" then
				output.fsRmdir = self.res.value:jsonEncode()
			elseif self.res.type == "fsCopy" then
				output.fsCopy = self.res.value:jsonEncode()
			elseif self.res.type == "streamOpen" then
				output.streamOpen = self.res.value:jsonEncode()
			elseif self.res.type == "streamReadChunk" then
				output.streamReadChunk = self.res.value:jsonEncode()
			elseif self.res.type == "streamReadLine" then
				output.streamReadLine = self.res.value:jsonEncode()
			elseif self.res.type == "streamReadAll" then
				output.streamReadAll = self.res.value:jsonEncode()
			elseif self.res.type == "streamWrite" then
				output.streamWrite = self.res.value:jsonEncode()
			elseif self.res.type == "streamClose" then
				output.streamClose = self.res.value:jsonEncode()
			elseif self.res.type == "streamReadBytes" then
				output.streamReadBytes = self.res.value:jsonEncode()
			elseif self.res.type == "streamWriteBytes" then
				output.streamWriteBytes = self.res.value:jsonEncode()
			elseif self.res.type == "processGetInfo" then
				output.processGetInfo = self.res.value:jsonEncode()
			elseif self.res.type == "processExit" then
				output.processExit = self.res.value:jsonEncode()
			elseif self.res.type == "processRun" then
				output.processRun = self.res.value:jsonEncode()
			elseif self.res.type == "processSystem" then
				output.processSystem = self.res.value:jsonEncode()
			elseif self.res.type == "processCreate" then
				output.processCreate = self.res.value:jsonEncode()
			elseif self.res.type == "processRunHandle" then
				output.processRunHandle = self.res.value:jsonEncode()
			elseif self.res.type == "processKill" then
				output.processKill = self.res.value:jsonEncode()
			elseif self.res.type == "mcpCall" then
				output.mcpCall = self.res.value:jsonEncode()
			end
		end

		return output
	end

	function _ClientRpcResponseImpl.jsonDecode(input: { [string]: any }): ClientRpcResponse
		local self = _ClientRpcResponseImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		if input.error ~= nil then
			self.res = { type = "error", value = input.error }
		end

		if input.fsExists ~= nil then
			self.res = { type = "fsExists", value = messages.FsExistsResponse.jsonDecode(input.fsExists) }
		end

		if input.fsStat ~= nil then
			self.res = { type = "fsStat", value = messages.FsStatResponse.jsonDecode(input.fsStat) }
		end

		if input.fsType ~= nil then
			self.res = { type = "fsType", value = messages.FsTypeResponse.jsonDecode(input.fsType) }
		end

		if input.fsMkdir ~= nil then
			self.res = { type = "fsMkdir", value = messages.Ok.jsonDecode(input.fsMkdir) }
		end

		if input.fsListdir ~= nil then
			self.res = { type = "fsListdir", value = messages.FsListdirResponse.jsonDecode(input.fsListdir) }
		end

		if input.fsRemove ~= nil then
			self.res = { type = "fsRemove", value = messages.Ok.jsonDecode(input.fsRemove) }
		end

		if input.fsRmdir ~= nil then
			self.res = { type = "fsRmdir", value = messages.Ok.jsonDecode(input.fsRmdir) }
		end

		if input.fsCopy ~= nil then
			self.res = { type = "fsCopy", value = messages.Ok.jsonDecode(input.fsCopy) }
		end

		if input.streamOpen ~= nil then
			self.res = { type = "streamOpen", value = messages.StreamOpenResponse.jsonDecode(input.streamOpen) }
		end

		if input.streamReadChunk ~= nil then
			self.res =
				{ type = "streamReadChunk", value = messages.StreamReadChunkResponse.jsonDecode(input.streamReadChunk) }
		end

		if input.streamReadLine ~= nil then
			self.res =
				{ type = "streamReadLine", value = messages.StreamReadLineResponse.jsonDecode(input.streamReadLine) }
		end

		if input.streamReadAll ~= nil then
			self.res =
				{ type = "streamReadAll", value = messages.StreamReadAllResponse.jsonDecode(input.streamReadAll) }
		end

		if input.streamWrite ~= nil then
			self.res = { type = "streamWrite", value = messages.Ok.jsonDecode(input.streamWrite) }
		end

		if input.streamClose ~= nil then
			self.res = { type = "streamClose", value = messages.Ok.jsonDecode(input.streamClose) }
		end

		if input.streamReadBytes ~= nil then
			self.res =
				{ type = "streamReadBytes", value = messages.StreamReadBytesResponse.jsonDecode(input.streamReadBytes) }
		end

		if input.streamWriteBytes ~= nil then
			self.res = { type = "streamWriteBytes", value = messages.Ok.jsonDecode(input.streamWriteBytes) }
		end

		if input.processGetInfo ~= nil then
			self.res =
				{ type = "processGetInfo", value = messages.ProcessGetInfoResponse.jsonDecode(input.processGetInfo) }
		end

		if input.processExit ~= nil then
			self.res = { type = "processExit", value = messages.Ok.jsonDecode(input.processExit) }
		end

		if input.processRun ~= nil then
			self.res = { type = "processRun", value = messages.ProcessRunResponse.jsonDecode(input.processRun) }
		end

		if input.processSystem ~= nil then
			self.res = { type = "processSystem", value = messages.ProcessRunResponse.jsonDecode(input.processSystem) }
		end

		if input.processCreate ~= nil then
			self.res =
				{ type = "processCreate", value = messages.ProcessCreateResponse.jsonDecode(input.processCreate) }
		end

		if input.processRunHandle ~= nil then
			self.res =
				{ type = "processRunHandle", value = messages.ProcessRunResponse.jsonDecode(input.processRunHandle) }
		end

		if input.processKill ~= nil then
			self.res = { type = "processKill", value = messages.Ok.jsonDecode(input.processKill) }
		end

		if input.mcpCall ~= nil then
			self.res = { type = "mcpCall", value = messages.McpCallResponse.jsonDecode(input.mcpCall) }
		end

		return self
	end

	_ClientRpcResponseImpl.descriptor = {
		name = "ClientRpcResponse",
		fullName = "rodeo.runtime.ClientRpcResponse",
	}

	messages.ClientRpcResponse = _ClientRpcResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ClientRpcResponse)
end

return {
	Ok = messages.Ok,
	StreamOpenRequest = messages.StreamOpenRequest,
	StreamOpenResponse = messages.StreamOpenResponse,
	StreamReadChunkRequest = messages.StreamReadChunkRequest,
	StreamReadChunkResponse = messages.StreamReadChunkResponse,
	StreamReadLineRequest = messages.StreamReadLineRequest,
	StreamReadLineResponse = messages.StreamReadLineResponse,
	StreamReadAllRequest = messages.StreamReadAllRequest,
	StreamReadAllResponse = messages.StreamReadAllResponse,
	StreamWriteRequest = messages.StreamWriteRequest,
	StreamReadBytesRequest = messages.StreamReadBytesRequest,
	StreamReadBytesResponse = messages.StreamReadBytesResponse,
	StreamWriteBytesRequest = messages.StreamWriteBytesRequest,
	StreamCloseRequest = messages.StreamCloseRequest,
	FsExistsRequest = messages.FsExistsRequest,
	FsExistsResponse = messages.FsExistsResponse,
	FsStatRequest = messages.FsStatRequest,
	FsStatResponse = messages.FsStatResponse,
	FsTypeRequest = messages.FsTypeRequest,
	FsTypeResponse = messages.FsTypeResponse,
	FsMkdirRequest = messages.FsMkdirRequest,
	FsListdirRequest = messages.FsListdirRequest,
	FsListdirResponse = messages.FsListdirResponse,
	FsDirEntry = messages.FsDirEntry,
	FsRemoveRequest = messages.FsRemoveRequest,
	FsRmdirRequest = messages.FsRmdirRequest,
	FsCopyRequest = messages.FsCopyRequest,
	ProcessOptions = messages.ProcessOptions,
	ProcessGetInfoRequest = messages.ProcessGetInfoRequest,
	ProcessGetInfoResponse = messages.ProcessGetInfoResponse,
	ProcessExitRequest = messages.ProcessExitRequest,
	ProcessRunRequest = messages.ProcessRunRequest,
	ProcessRunResponse = messages.ProcessRunResponse,
	ProcessSystemRequest = messages.ProcessSystemRequest,
	ProcessCreateRequest = messages.ProcessCreateRequest,
	ProcessCreateResponse = messages.ProcessCreateResponse,
	ProcessRunHandleRequest = messages.ProcessRunHandleRequest,
	ProcessKillRequest = messages.ProcessKillRequest,
	McpCallRequest = messages.McpCallRequest,
	McpCallResponse = messages.McpCallResponse,
	ClientRpcCall = messages.ClientRpcCall,
	ClientRpcResponse = messages.ClientRpcResponse,
}
