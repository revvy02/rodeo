--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require("./proto")
local typeRegistry = require("./proto/typeRegistry")

type _Messages = {
	BackendMessage: _BackendMessageMessage,
	FileChunk: _FileChunkMessage,
	FileAck: _FileAckMessage,
	FilesComplete: _FilesCompleteMessage,
	RegisterRequest: _RegisterRequestMessage,
	DomConnect: _DomConnectMessage,
	DomDisconnect: _DomDisconnectMessage,
	DomPluginMessage: _DomPluginMessageMessage,
	DomServerMessage: _DomServerMessageMessage,
	StateSnapshot: _StateSnapshotMessage,
	StudioInstanceState: _StudioInstanceStateMessage,
	DomSnapshot: _DomSnapshotMessage,
	StudioState: _StudioStateMessage,
	StudioDom: _StudioDomMessage,
	StudioSnapshot: _StudioSnapshotMessage,
	PlayerStatus: _PlayerStatusMessage,
	MasterMessage: _MasterMessageMessage,
	CloseStudioCommand: _CloseStudioCommandMessage,
	RunCompleted: _RunCompletedMessage,
	RegisterResponse: _RegisterResponseMessage,
	SaveCommand: _SaveCommandMessage,
	SaveResult: _SaveResultMessage,
	LaunchStudioCommand: _LaunchStudioCommandMessage,
	SessionExited: _SessionExitedMessage,
	RunClientMessage: _RunClientMessageMessage,
	SubmitRequest: _SubmitRequestMessage,
	RunEvent: _RunEventMessage,
	RunComplete: _RunCompleteMessage,
	ProcessCreated: _ProcessCreatedMessage,
	HealthRequest: _HealthRequestMessage,
	GetStateRequest: _GetStateRequestMessage,
	ListProcessesRequest: _ListProcessesRequestMessage,
	KillProcessRequest: _KillProcessRequestMessage,
	LaunchStudioRequest: _LaunchStudioRequestMessage,
	LaunchStudioEvent: _LaunchStudioEventMessage,
	StudioLaunching: _StudioLaunchingMessage,
	StudioReady: _StudioReadyMessage,
	StudioLaunchError: _StudioLaunchErrorMessage,
	SetStudioModeRequest: _SetStudioModeRequestMessage,
	SetStudioModeResponse: _SetStudioModeResponseMessage,
	CloseStudioRequest: _CloseStudioRequestMessage,
	CloseStudioEvent: _CloseStudioEventMessage,
	StudioClosing: _StudioClosingMessage,
	StudioClosed: _StudioClosedMessage,
	SavePlaceRequest: _SavePlaceRequestMessage,
	SavePlaceResponse: _SavePlaceResponseMessage,
	ListBackendsRequest: _ListBackendsRequestMessage,
	ListBackendsResponse: _ListBackendsResponseMessage,
	BackendInfo: _BackendInfoMessage,
	RodeoSnapshot: _RodeoSnapshotMessage,
	LogFilter: _LogFilterMessage,
	HealthResponse: _HealthResponseMessage,
	ContextInfo: _ContextInfoMessage,
	DomInfo: _DomInfoMessage,
	ProcessInfo: _ProcessInfoMessage,
	ProcessListResponse: _ProcessListResponseMessage,
	KillResponse: _KillResponseMessage,
	DisconnectMsg: _DisconnectMsgMessage,
	PluginMessage: _PluginMessageMessage,
	ServerMessage: _ServerMessageMessage,
	SetTargetModeMsg: _SetTargetModeMsgMessage,
	StudioStateMsg: _StudioStateMsgMessage,
	ClientInfo: _ClientInfoMessage,
	WelcomeMsg: _WelcomeMsgMessage,
	RunCommand: _RunCommandMessage,
	KillCommand: _KillCommandMessage,
	ExecutionDone: _ExecutionDoneMessage,
	ExecutionKilled: _ExecutionKilledMessage,
	ProcessState: _ProcessStateMessage,
}
local messages: _Messages = {} :: _Messages

local _runtime = require("./runtime")

type _BackendMessageImpl = {
	__index: _BackendMessageImpl,
	new: (fields: _BackendMessagePartialFields?) -> BackendMessage,
	encode: (self: BackendMessage) -> buffer,
	decode: (input: buffer) -> BackendMessage,
	jsonEncode: (self: BackendMessage) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BackendMessage,
	descriptor: proto.Descriptor,
}

type _BackendMessageFields = {
	msg: (
		{ type: "register", value: RegisterRequest }
		| { type: "domConnect", value: DomConnect }
		| { type: "domDisconnect", value: DomDisconnect }
		| { type: "domPluginMessage", value: DomPluginMessage }
		| { type: "stateSnapshot", value: StateSnapshot }
		| { type: "playerStatus", value: PlayerStatus }
		| { type: "filesComplete", value: FilesComplete }
		| { type: "sessionExited", value: SessionExited }
		| { type: "saveResult", value: SaveResult }
	)?,
}

export type _BackendMessagePartialFields = {
	msg: (
		{ type: "register", value: RegisterRequest | _RegisterRequestPartialFields }
		| { type: "domConnect", value: DomConnect | _DomConnectPartialFields }
		| { type: "domDisconnect", value: DomDisconnect | _DomDisconnectPartialFields }
		| { type: "domPluginMessage", value: DomPluginMessage | _DomPluginMessagePartialFields }
		| { type: "stateSnapshot", value: StateSnapshot | _StateSnapshotPartialFields }
		| { type: "playerStatus", value: PlayerStatus | _PlayerStatusPartialFields }
		| { type: "filesComplete", value: FilesComplete | _FilesCompletePartialFields }
		| { type: "sessionExited", value: SessionExited | _SessionExitedPartialFields }
		| { type: "saveResult", value: SaveResult | _SaveResultPartialFields }
	)?,
}

export type BackendMessage = typeof(setmetatable({} :: _BackendMessageFields, {} :: _BackendMessageImpl))
type _BackendMessageMessage = proto.Message<BackendMessage, _BackendMessagePartialFields>

type _FileChunkImpl = {
	__index: _FileChunkImpl,
	new: (fields: _FileChunkPartialFields?) -> FileChunk,
	encode: (self: FileChunk) -> buffer,
	decode: (input: buffer) -> FileChunk,
	jsonEncode: (self: FileChunk) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FileChunk,
	descriptor: proto.Descriptor,
}

type _FileChunkFields = {
	filename: string,
	executionId: string,
	data: buffer,
	isLast: boolean,
}

export type _FileChunkPartialFields = {
	filename: string?,
	executionId: string?,
	data: buffer?,
	isLast: boolean?,
}

export type FileChunk = typeof(setmetatable({} :: _FileChunkFields, {} :: _FileChunkImpl))
type _FileChunkMessage = proto.Message<FileChunk, _FileChunkPartialFields>

type _FileAckImpl = {
	__index: _FileAckImpl,
	new: (fields: _FileAckPartialFields?) -> FileAck,
	encode: (self: FileAck) -> buffer,
	decode: (input: buffer) -> FileAck,
	jsonEncode: (self: FileAck) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FileAck,
	descriptor: proto.Descriptor,
}

type _FileAckFields = {}

export type _FileAckPartialFields = {}

export type FileAck = typeof(setmetatable({} :: _FileAckFields, {} :: _FileAckImpl))
type _FileAckMessage = proto.Message<FileAck, _FileAckPartialFields>

type _FilesCompleteImpl = {
	__index: _FilesCompleteImpl,
	new: (fields: _FilesCompletePartialFields?) -> FilesComplete,
	encode: (self: FilesComplete) -> buffer,
	decode: (input: buffer) -> FilesComplete,
	jsonEncode: (self: FilesComplete) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FilesComplete,
	descriptor: proto.Descriptor,
}

type _FilesCompleteFields = {
	executionId: string,
}

export type _FilesCompletePartialFields = {
	executionId: string?,
}

export type FilesComplete = typeof(setmetatable({} :: _FilesCompleteFields, {} :: _FilesCompleteImpl))
type _FilesCompleteMessage = proto.Message<FilesComplete, _FilesCompletePartialFields>

type _RegisterRequestImpl = {
	__index: _RegisterRequestImpl,
	new: (fields: _RegisterRequestPartialFields?) -> RegisterRequest,
	encode: (self: RegisterRequest) -> buffer,
	decode: (input: buffer) -> RegisterRequest,
	jsonEncode: (self: RegisterRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> RegisterRequest,
	descriptor: proto.Descriptor,
}

type _RegisterRequestFields = {
	kind: string,
	name: string,
	port: number?,
}

export type _RegisterRequestPartialFields = {
	kind: string?,
	name: string?,
	port: number?,
}

export type RegisterRequest = typeof(setmetatable({} :: _RegisterRequestFields, {} :: _RegisterRequestImpl))
type _RegisterRequestMessage = proto.Message<RegisterRequest, _RegisterRequestPartialFields>

type _DomConnectImpl = {
	__index: _DomConnectImpl,
	new: (fields: _DomConnectPartialFields?) -> DomConnect,
	encode: (self: DomConnect) -> buffer,
	decode: (input: buffer) -> DomConnect,
	jsonEncode: (self: DomConnect) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DomConnect,
	descriptor: proto.Descriptor,
}

type _DomConnectFields = {
	domId: string,
	stateJson: string,
}

export type _DomConnectPartialFields = {
	domId: string?,
	stateJson: string?,
}

export type DomConnect = typeof(setmetatable({} :: _DomConnectFields, {} :: _DomConnectImpl))
type _DomConnectMessage = proto.Message<DomConnect, _DomConnectPartialFields>

type _DomDisconnectImpl = {
	__index: _DomDisconnectImpl,
	new: (fields: _DomDisconnectPartialFields?) -> DomDisconnect,
	encode: (self: DomDisconnect) -> buffer,
	decode: (input: buffer) -> DomDisconnect,
	jsonEncode: (self: DomDisconnect) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DomDisconnect,
	descriptor: proto.Descriptor,
}

type _DomDisconnectFields = {
	domId: string,
}

export type _DomDisconnectPartialFields = {
	domId: string?,
}

export type DomDisconnect = typeof(setmetatable({} :: _DomDisconnectFields, {} :: _DomDisconnectImpl))
type _DomDisconnectMessage = proto.Message<DomDisconnect, _DomDisconnectPartialFields>

type _DomPluginMessageImpl = {
	__index: _DomPluginMessageImpl,
	new: (fields: _DomPluginMessagePartialFields?) -> DomPluginMessage,
	encode: (self: DomPluginMessage) -> buffer,
	decode: (input: buffer) -> DomPluginMessage,
	jsonEncode: (self: DomPluginMessage) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DomPluginMessage,
	descriptor: proto.Descriptor,
}

type _DomPluginMessageFields = {
	domId: string,
	message: PluginMessage?,
}

export type _DomPluginMessagePartialFields = {
	domId: string?,
	message: (PluginMessage | _PluginMessagePartialFields)?,
}

export type DomPluginMessage = typeof(setmetatable({} :: _DomPluginMessageFields, {} :: _DomPluginMessageImpl))
type _DomPluginMessageMessage = proto.Message<DomPluginMessage, _DomPluginMessagePartialFields>

type _DomServerMessageImpl = {
	__index: _DomServerMessageImpl,
	new: (fields: _DomServerMessagePartialFields?) -> DomServerMessage,
	encode: (self: DomServerMessage) -> buffer,
	decode: (input: buffer) -> DomServerMessage,
	jsonEncode: (self: DomServerMessage) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DomServerMessage,
	descriptor: proto.Descriptor,
}

type _DomServerMessageFields = {
	domId: string,
	message: ServerMessage?,
}

export type _DomServerMessagePartialFields = {
	domId: string?,
	message: (ServerMessage | _ServerMessagePartialFields)?,
}

export type DomServerMessage = typeof(setmetatable({} :: _DomServerMessageFields, {} :: _DomServerMessageImpl))
type _DomServerMessageMessage = proto.Message<DomServerMessage, _DomServerMessagePartialFields>

type _StateSnapshotImpl = {
	__index: _StateSnapshotImpl,
	new: (fields: _StateSnapshotPartialFields?) -> StateSnapshot,
	encode: (self: StateSnapshot) -> buffer,
	decode: (input: buffer) -> StateSnapshot,
	jsonEncode: (self: StateSnapshot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StateSnapshot,
	descriptor: proto.Descriptor,
}

type _StateSnapshotFields = {
	doms: { DomSnapshot },
	studios: { StudioSnapshot },
	studioInstances: { StudioInstanceState },
}

export type _StateSnapshotPartialFields = {
	doms: { DomSnapshot | _DomSnapshotPartialFields }?,
	studios: { StudioSnapshot | _StudioSnapshotPartialFields }?,
	studioInstances: { StudioInstanceState | _StudioInstanceStatePartialFields }?,
}

export type StateSnapshot = typeof(setmetatable({} :: _StateSnapshotFields, {} :: _StateSnapshotImpl))
type _StateSnapshotMessage = proto.Message<StateSnapshot, _StateSnapshotPartialFields>

type _StudioInstanceStateImpl = {
	__index: _StudioInstanceStateImpl,
	new: (fields: _StudioInstanceStatePartialFields?) -> StudioInstanceState,
	encode: (self: StudioInstanceState) -> buffer,
	decode: (input: buffer) -> StudioInstanceState,
	jsonEncode: (self: StudioInstanceState) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioInstanceState,
	descriptor: proto.Descriptor,
}

type _StudioInstanceStateFields = {
	sessionGuid: string,
	status: string,
	error: string?,
}

export type _StudioInstanceStatePartialFields = {
	sessionGuid: string?,
	status: string?,
	error: string?,
}

export type StudioInstanceState = typeof(setmetatable({} :: _StudioInstanceStateFields, {} :: _StudioInstanceStateImpl))
type _StudioInstanceStateMessage = proto.Message<StudioInstanceState, _StudioInstanceStatePartialFields>

type _DomSnapshotImpl = {
	__index: _DomSnapshotImpl,
	new: (fields: _DomSnapshotPartialFields?) -> DomSnapshot,
	encode: (self: DomSnapshot) -> buffer,
	decode: (input: buffer) -> DomSnapshot,
	jsonEncode: (self: DomSnapshot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DomSnapshot,
	descriptor: proto.Descriptor,
}

type _DomSnapshotFields = {
	domId: string,
	backendId: string?,
	mode: string?,
	domKind: string?,
	sessionGuid: string?,
	studioId: string?,
	placeId: number?,
	gameName: string?,
	activeRuns: number,
	connected: boolean,
	userName: string?,
	userId: number?,
}

export type _DomSnapshotPartialFields = {
	domId: string?,
	backendId: string?,
	mode: string?,
	domKind: string?,
	sessionGuid: string?,
	studioId: string?,
	placeId: number?,
	gameName: string?,
	activeRuns: number?,
	connected: boolean?,
	userName: string?,
	userId: number?,
}

export type DomSnapshot = typeof(setmetatable({} :: _DomSnapshotFields, {} :: _DomSnapshotImpl))
type _DomSnapshotMessage = proto.Message<DomSnapshot, _DomSnapshotPartialFields>

type _StudioStateImpl = {
	__index: _StudioStateImpl,
	new: (fields: _StudioStatePartialFields?) -> StudioState,
	encode: (self: StudioState) -> buffer,
	decode: (input: buffer) -> StudioState,
	jsonEncode: (self: StudioState) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioState,
	descriptor: proto.Descriptor,
}

type _StudioStateFields = {
	studioId: string,
	backendId: string,
	sessionId: string?,
	placeName: string,
	placeId: number,
	status: string,
	studioMode: string,
	editDomId: string?,
	doms: { StudioDom },
}

export type _StudioStatePartialFields = {
	studioId: string?,
	backendId: string?,
	sessionId: string?,
	placeName: string?,
	placeId: number?,
	status: string?,
	studioMode: string?,
	editDomId: string?,
	doms: { StudioDom | _StudioDomPartialFields }?,
}

export type StudioState = typeof(setmetatable({} :: _StudioStateFields, {} :: _StudioStateImpl))
type _StudioStateMessage = proto.Message<StudioState, _StudioStatePartialFields>

type _StudioDomImpl = {
	__index: _StudioDomImpl,
	new: (fields: _StudioDomPartialFields?) -> StudioDom,
	encode: (self: StudioDom) -> buffer,
	decode: (input: buffer) -> StudioDom,
	jsonEncode: (self: StudioDom) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioDom,
	descriptor: proto.Descriptor,
}

type _StudioDomFields = {
	domId: string,
	domKind: string,
	userName: string?,
	userId: number?,
}

export type _StudioDomPartialFields = {
	domId: string?,
	domKind: string?,
	userName: string?,
	userId: number?,
}

export type StudioDom = typeof(setmetatable({} :: _StudioDomFields, {} :: _StudioDomImpl))
type _StudioDomMessage = proto.Message<StudioDom, _StudioDomPartialFields>

type _StudioSnapshotImpl = {
	__index: _StudioSnapshotImpl,
	new: (fields: _StudioSnapshotPartialFields?) -> StudioSnapshot,
	encode: (self: StudioSnapshot) -> buffer,
	decode: (input: buffer) -> StudioSnapshot,
	jsonEncode: (self: StudioSnapshot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioSnapshot,
	descriptor: proto.Descriptor,
}

type _StudioSnapshotFields = {
	sessionGuid: string,
	mode: string,
	name: string,
	placeId: number,
}

export type _StudioSnapshotPartialFields = {
	sessionGuid: string?,
	mode: string?,
	name: string?,
	placeId: number?,
}

export type StudioSnapshot = typeof(setmetatable({} :: _StudioSnapshotFields, {} :: _StudioSnapshotImpl))
type _StudioSnapshotMessage = proto.Message<StudioSnapshot, _StudioSnapshotPartialFields>

type _PlayerStatusImpl = {
	__index: _PlayerStatusImpl,
	new: (fields: _PlayerStatusPartialFields?) -> PlayerStatus,
	encode: (self: PlayerStatus) -> buffer,
	decode: (input: buffer) -> PlayerStatus,
	jsonEncode: (self: PlayerStatus) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PlayerStatus,
	descriptor: proto.Descriptor,
}

type _PlayerStatusFields = {
	state: string,
	placeId: number?,
	error: string?,
}

export type _PlayerStatusPartialFields = {
	state: string?,
	placeId: number?,
	error: string?,
}

export type PlayerStatus = typeof(setmetatable({} :: _PlayerStatusFields, {} :: _PlayerStatusImpl))
type _PlayerStatusMessage = proto.Message<PlayerStatus, _PlayerStatusPartialFields>

type _MasterMessageImpl = {
	__index: _MasterMessageImpl,
	new: (fields: _MasterMessagePartialFields?) -> MasterMessage,
	encode: (self: MasterMessage) -> buffer,
	decode: (input: buffer) -> MasterMessage,
	jsonEncode: (self: MasterMessage) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MasterMessage,
	descriptor: proto.Descriptor,
}

type _MasterMessageFields = {
	msg: (
		{ type: "registered", value: RegisterResponse }
		| { type: "domServerMessage", value: DomServerMessage }
		| { type: "save", value: SaveCommand }
		| { type: "runCompleted", value: RunCompleted }
		| { type: "launchStudio", value: LaunchStudioCommand }
		| { type: "closeStudio", value: CloseStudioCommand }
	)?,
}

export type _MasterMessagePartialFields = {
	msg: (
		{ type: "registered", value: RegisterResponse | _RegisterResponsePartialFields }
		| { type: "domServerMessage", value: DomServerMessage | _DomServerMessagePartialFields }
		| { type: "save", value: SaveCommand | _SaveCommandPartialFields }
		| { type: "runCompleted", value: RunCompleted | _RunCompletedPartialFields }
		| { type: "launchStudio", value: LaunchStudioCommand | _LaunchStudioCommandPartialFields }
		| { type: "closeStudio", value: CloseStudioCommand | _CloseStudioCommandPartialFields }
	)?,
}

export type MasterMessage = typeof(setmetatable({} :: _MasterMessageFields, {} :: _MasterMessageImpl))
type _MasterMessageMessage = proto.Message<MasterMessage, _MasterMessagePartialFields>

type _CloseStudioCommandImpl = {
	__index: _CloseStudioCommandImpl,
	new: (fields: _CloseStudioCommandPartialFields?) -> CloseStudioCommand,
	encode: (self: CloseStudioCommand) -> buffer,
	decode: (input: buffer) -> CloseStudioCommand,
	jsonEncode: (self: CloseStudioCommand) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CloseStudioCommand,
	descriptor: proto.Descriptor,
}

type _CloseStudioCommandFields = {
	sessionGuid: string,
}

export type _CloseStudioCommandPartialFields = {
	sessionGuid: string?,
}

export type CloseStudioCommand = typeof(setmetatable({} :: _CloseStudioCommandFields, {} :: _CloseStudioCommandImpl))
type _CloseStudioCommandMessage = proto.Message<CloseStudioCommand, _CloseStudioCommandPartialFields>

type _RunCompletedImpl = {
	__index: _RunCompletedImpl,
	new: (fields: _RunCompletedPartialFields?) -> RunCompleted,
	encode: (self: RunCompleted) -> buffer,
	decode: (input: buffer) -> RunCompleted,
	jsonEncode: (self: RunCompleted) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> RunCompleted,
	descriptor: proto.Descriptor,
}

type _RunCompletedFields = {
	executionId: string,
}

export type _RunCompletedPartialFields = {
	executionId: string?,
}

export type RunCompleted = typeof(setmetatable({} :: _RunCompletedFields, {} :: _RunCompletedImpl))
type _RunCompletedMessage = proto.Message<RunCompleted, _RunCompletedPartialFields>

type _RegisterResponseImpl = {
	__index: _RegisterResponseImpl,
	new: (fields: _RegisterResponsePartialFields?) -> RegisterResponse,
	encode: (self: RegisterResponse) -> buffer,
	decode: (input: buffer) -> RegisterResponse,
	jsonEncode: (self: RegisterResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> RegisterResponse,
	descriptor: proto.Descriptor,
}

type _RegisterResponseFields = {
	id: string,
	masterId: string,
}

export type _RegisterResponsePartialFields = {
	id: string?,
	masterId: string?,
}

export type RegisterResponse = typeof(setmetatable({} :: _RegisterResponseFields, {} :: _RegisterResponseImpl))
type _RegisterResponseMessage = proto.Message<RegisterResponse, _RegisterResponsePartialFields>

type _SaveCommandImpl = {
	__index: _SaveCommandImpl,
	new: (fields: _SaveCommandPartialFields?) -> SaveCommand,
	encode: (self: SaveCommand) -> buffer,
	decode: (input: buffer) -> SaveCommand,
	jsonEncode: (self: SaveCommand) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SaveCommand,
	descriptor: proto.Descriptor,
}

type _SaveCommandFields = {
	requestId: string,
	sessionGuid: string?,
}

export type _SaveCommandPartialFields = {
	requestId: string?,
	sessionGuid: string?,
}

export type SaveCommand = typeof(setmetatable({} :: _SaveCommandFields, {} :: _SaveCommandImpl))
type _SaveCommandMessage = proto.Message<SaveCommand, _SaveCommandPartialFields>

type _SaveResultImpl = {
	__index: _SaveResultImpl,
	new: (fields: _SaveResultPartialFields?) -> SaveResult,
	encode: (self: SaveResult) -> buffer,
	decode: (input: buffer) -> SaveResult,
	jsonEncode: (self: SaveResult) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SaveResult,
	descriptor: proto.Descriptor,
}

type _SaveResultFields = {
	requestId: string,
	sessionGuid: string?,
	saved: boolean,
	path: string?,
	error: string?,
}

export type _SaveResultPartialFields = {
	requestId: string?,
	sessionGuid: string?,
	saved: boolean?,
	path: string?,
	error: string?,
}

export type SaveResult = typeof(setmetatable({} :: _SaveResultFields, {} :: _SaveResultImpl))
type _SaveResultMessage = proto.Message<SaveResult, _SaveResultPartialFields>

type _LaunchStudioCommandImpl = {
	__index: _LaunchStudioCommandImpl,
	new: (fields: _LaunchStudioCommandPartialFields?) -> LaunchStudioCommand,
	encode: (self: LaunchStudioCommand) -> buffer,
	decode: (input: buffer) -> LaunchStudioCommand,
	jsonEncode: (self: LaunchStudioCommand) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> LaunchStudioCommand,
	descriptor: proto.Descriptor,
}

type _LaunchStudioCommandFields = {
	placeFile: string?,
	placeId: number?,
	fflags: { string },
	background: boolean,
	detached: boolean,
	savePath: string?,
	fflagFile: string?,
	sessionGuid: string,
	showWidgets: string,
	profile: boolean,
}

export type _LaunchStudioCommandPartialFields = {
	placeFile: string?,
	placeId: number?,
	fflags: { string }?,
	background: boolean?,
	detached: boolean?,
	savePath: string?,
	fflagFile: string?,
	sessionGuid: string?,
	showWidgets: string?,
	profile: boolean?,
}

export type LaunchStudioCommand = typeof(setmetatable({} :: _LaunchStudioCommandFields, {} :: _LaunchStudioCommandImpl))
type _LaunchStudioCommandMessage = proto.Message<LaunchStudioCommand, _LaunchStudioCommandPartialFields>

type _SessionExitedImpl = {
	__index: _SessionExitedImpl,
	new: (fields: _SessionExitedPartialFields?) -> SessionExited,
	encode: (self: SessionExited) -> buffer,
	decode: (input: buffer) -> SessionExited,
	jsonEncode: (self: SessionExited) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SessionExited,
	descriptor: proto.Descriptor,
}

type _SessionExitedFields = {
	sessionGuid: string,
	reason: string,
}

export type _SessionExitedPartialFields = {
	sessionGuid: string?,
	reason: string?,
}

export type SessionExited = typeof(setmetatable({} :: _SessionExitedFields, {} :: _SessionExitedImpl))
type _SessionExitedMessage = proto.Message<SessionExited, _SessionExitedPartialFields>

type _RunClientMessageImpl = {
	__index: _RunClientMessageImpl,
	new: (fields: _RunClientMessagePartialFields?) -> RunClientMessage,
	encode: (self: RunClientMessage) -> buffer,
	decode: (input: buffer) -> RunClientMessage,
	jsonEncode: (self: RunClientMessage) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> RunClientMessage,
	descriptor: proto.Descriptor,
}

type _RunClientMessageFields = {
	msg: (
		{ type: "submit", value: SubmitRequest }
		| { type: "rpcResponse", value: _runtime.ClientRpcResponse }
		| { type: "kill", value: KillCommand }
	)?,
}

export type _RunClientMessagePartialFields = {
	msg: (
		{ type: "submit", value: SubmitRequest | _SubmitRequestPartialFields }
		| { type: "rpcResponse", value: _runtime.ClientRpcResponse | _runtime._ClientRpcResponsePartialFields }
		| { type: "kill", value: KillCommand | _KillCommandPartialFields }
	)?,
}

export type RunClientMessage = typeof(setmetatable({} :: _RunClientMessageFields, {} :: _RunClientMessageImpl))
type _RunClientMessageMessage = proto.Message<RunClientMessage, _RunClientMessagePartialFields>

type _SubmitRequestImpl = {
	__index: _SubmitRequestImpl,
	new: (fields: _SubmitRequestPartialFields?) -> SubmitRequest,
	encode: (self: SubmitRequest) -> buffer,
	decode: (input: buffer) -> SubmitRequest,
	jsonEncode: (self: SubmitRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SubmitRequest,
	descriptor: proto.Descriptor,
}

type _SubmitRequestFields = {
	script: string,
	mode: string?,
	session: string?,
	domId: string?,
	domKind: string?,
	context: string?,
	cacheRequires: boolean?,
	scriptArgs: { string },
	returnFile: string?,
	showReturn: boolean?,
	outputFile: string?,
	verbose: boolean?,
	instancePath: string?,
	scriptPath: string?,
	profile: boolean?,
	logFilter: LogFilter?,
}

export type _SubmitRequestPartialFields = {
	script: string?,
	mode: string?,
	session: string?,
	domId: string?,
	domKind: string?,
	context: string?,
	cacheRequires: boolean?,
	scriptArgs: { string }?,
	returnFile: string?,
	showReturn: boolean?,
	outputFile: string?,
	verbose: boolean?,
	instancePath: string?,
	scriptPath: string?,
	profile: boolean?,
	logFilter: (LogFilter | _LogFilterPartialFields)?,
}

export type SubmitRequest = typeof(setmetatable({} :: _SubmitRequestFields, {} :: _SubmitRequestImpl))
type _SubmitRequestMessage = proto.Message<SubmitRequest, _SubmitRequestPartialFields>

type _RunEventImpl = {
	__index: _RunEventImpl,
	new: (fields: _RunEventPartialFields?) -> RunEvent,
	encode: (self: RunEvent) -> buffer,
	decode: (input: buffer) -> RunEvent,
	jsonEncode: (self: RunEvent) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> RunEvent,
	descriptor: proto.Descriptor,
}

type _RunEventFields = {
	event: (
		{ type: "created", value: ProcessCreated }
		| { type: "disconnect", value: string }
		| { type: "fileChunk", value: FileChunk }
		| { type: "complete", value: RunComplete }
		| { type: "rpcCall", value: _runtime.ClientRpcCall }
		| { type: "executionDone", value: ExecutionDone }
		| { type: "executionKilled", value: ExecutionKilled }
	)?,
}

export type _RunEventPartialFields = {
	event: (
		{ type: "created", value: ProcessCreated | _ProcessCreatedPartialFields }
		| { type: "disconnect", value: string }
		| { type: "fileChunk", value: FileChunk | _FileChunkPartialFields }
		| { type: "complete", value: RunComplete | _RunCompletePartialFields }
		| { type: "rpcCall", value: _runtime.ClientRpcCall | _runtime._ClientRpcCallPartialFields }
		| { type: "executionDone", value: ExecutionDone | _ExecutionDonePartialFields }
		| { type: "executionKilled", value: ExecutionKilled | _ExecutionKilledPartialFields }
	)?,
}

export type RunEvent = typeof(setmetatable({} :: _RunEventFields, {} :: _RunEventImpl))
type _RunEventMessage = proto.Message<RunEvent, _RunEventPartialFields>

type _RunCompleteImpl = {
	__index: _RunCompleteImpl,
	new: (fields: _RunCompletePartialFields?) -> RunComplete,
	encode: (self: RunComplete) -> buffer,
	decode: (input: buffer) -> RunComplete,
	jsonEncode: (self: RunComplete) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> RunComplete,
	descriptor: proto.Descriptor,
}

type _RunCompleteFields = {}

export type _RunCompletePartialFields = {}

export type RunComplete = typeof(setmetatable({} :: _RunCompleteFields, {} :: _RunCompleteImpl))
type _RunCompleteMessage = proto.Message<RunComplete, _RunCompletePartialFields>

type _ProcessCreatedImpl = {
	__index: _ProcessCreatedImpl,
	new: (fields: _ProcessCreatedPartialFields?) -> ProcessCreated,
	encode: (self: ProcessCreated) -> buffer,
	decode: (input: buffer) -> ProcessCreated,
	jsonEncode: (self: ProcessCreated) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessCreated,
	descriptor: proto.Descriptor,
}

type _ProcessCreatedFields = {
	executionId: string,
}

export type _ProcessCreatedPartialFields = {
	executionId: string?,
}

export type ProcessCreated = typeof(setmetatable({} :: _ProcessCreatedFields, {} :: _ProcessCreatedImpl))
type _ProcessCreatedMessage = proto.Message<ProcessCreated, _ProcessCreatedPartialFields>

type _HealthRequestImpl = {
	__index: _HealthRequestImpl,
	new: (fields: _HealthRequestPartialFields?) -> HealthRequest,
	encode: (self: HealthRequest) -> buffer,
	decode: (input: buffer) -> HealthRequest,
	jsonEncode: (self: HealthRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HealthRequest,
	descriptor: proto.Descriptor,
}

type _HealthRequestFields = {}

export type _HealthRequestPartialFields = {}

export type HealthRequest = typeof(setmetatable({} :: _HealthRequestFields, {} :: _HealthRequestImpl))
type _HealthRequestMessage = proto.Message<HealthRequest, _HealthRequestPartialFields>

type _GetStateRequestImpl = {
	__index: _GetStateRequestImpl,
	new: (fields: _GetStateRequestPartialFields?) -> GetStateRequest,
	encode: (self: GetStateRequest) -> buffer,
	decode: (input: buffer) -> GetStateRequest,
	jsonEncode: (self: GetStateRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetStateRequest,
	descriptor: proto.Descriptor,
}

type _GetStateRequestFields = {}

export type _GetStateRequestPartialFields = {}

export type GetStateRequest = typeof(setmetatable({} :: _GetStateRequestFields, {} :: _GetStateRequestImpl))
type _GetStateRequestMessage = proto.Message<GetStateRequest, _GetStateRequestPartialFields>

type _ListProcessesRequestImpl = {
	__index: _ListProcessesRequestImpl,
	new: (fields: _ListProcessesRequestPartialFields?) -> ListProcessesRequest,
	encode: (self: ListProcessesRequest) -> buffer,
	decode: (input: buffer) -> ListProcessesRequest,
	jsonEncode: (self: ListProcessesRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ListProcessesRequest,
	descriptor: proto.Descriptor,
}

type _ListProcessesRequestFields = {}

export type _ListProcessesRequestPartialFields = {}

export type ListProcessesRequest = typeof(setmetatable(
	{} :: _ListProcessesRequestFields,
	{} :: _ListProcessesRequestImpl
))
type _ListProcessesRequestMessage = proto.Message<ListProcessesRequest, _ListProcessesRequestPartialFields>

type _KillProcessRequestImpl = {
	__index: _KillProcessRequestImpl,
	new: (fields: _KillProcessRequestPartialFields?) -> KillProcessRequest,
	encode: (self: KillProcessRequest) -> buffer,
	decode: (input: buffer) -> KillProcessRequest,
	jsonEncode: (self: KillProcessRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> KillProcessRequest,
	descriptor: proto.Descriptor,
}

type _KillProcessRequestFields = {
	executionId: string,
}

export type _KillProcessRequestPartialFields = {
	executionId: string?,
}

export type KillProcessRequest = typeof(setmetatable({} :: _KillProcessRequestFields, {} :: _KillProcessRequestImpl))
type _KillProcessRequestMessage = proto.Message<KillProcessRequest, _KillProcessRequestPartialFields>

type _LaunchStudioRequestImpl = {
	__index: _LaunchStudioRequestImpl,
	new: (fields: _LaunchStudioRequestPartialFields?) -> LaunchStudioRequest,
	encode: (self: LaunchStudioRequest) -> buffer,
	decode: (input: buffer) -> LaunchStudioRequest,
	jsonEncode: (self: LaunchStudioRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> LaunchStudioRequest,
	descriptor: proto.Descriptor,
}

type _LaunchStudioRequestFields = {
	backend: string,
	placeFile: string?,
	placeId: number?,
	fflags: { string },
	background: boolean,
	detached: boolean,
	savePath: string?,
	fflagFile: string?,
	showWidgets: string,
	profile: boolean,
}

export type _LaunchStudioRequestPartialFields = {
	backend: string?,
	placeFile: string?,
	placeId: number?,
	fflags: { string }?,
	background: boolean?,
	detached: boolean?,
	savePath: string?,
	fflagFile: string?,
	showWidgets: string?,
	profile: boolean?,
}

export type LaunchStudioRequest = typeof(setmetatable({} :: _LaunchStudioRequestFields, {} :: _LaunchStudioRequestImpl))
type _LaunchStudioRequestMessage = proto.Message<LaunchStudioRequest, _LaunchStudioRequestPartialFields>

type _LaunchStudioEventImpl = {
	__index: _LaunchStudioEventImpl,
	new: (fields: _LaunchStudioEventPartialFields?) -> LaunchStudioEvent,
	encode: (self: LaunchStudioEvent) -> buffer,
	decode: (input: buffer) -> LaunchStudioEvent,
	jsonEncode: (self: LaunchStudioEvent) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> LaunchStudioEvent,
	descriptor: proto.Descriptor,
}

type _LaunchStudioEventFields = {
	event: (
		{ type: "launching", value: StudioLaunching }
		| { type: "ready", value: StudioReady }
		| { type: "error", value: StudioLaunchError }
	)?,
}

export type _LaunchStudioEventPartialFields = {
	event: (
		{ type: "launching", value: StudioLaunching | _StudioLaunchingPartialFields }
		| { type: "ready", value: StudioReady | _StudioReadyPartialFields }
		| { type: "error", value: StudioLaunchError | _StudioLaunchErrorPartialFields }
	)?,
}

export type LaunchStudioEvent = typeof(setmetatable({} :: _LaunchStudioEventFields, {} :: _LaunchStudioEventImpl))
type _LaunchStudioEventMessage = proto.Message<LaunchStudioEvent, _LaunchStudioEventPartialFields>

type _StudioLaunchingImpl = {
	__index: _StudioLaunchingImpl,
	new: (fields: _StudioLaunchingPartialFields?) -> StudioLaunching,
	encode: (self: StudioLaunching) -> buffer,
	decode: (input: buffer) -> StudioLaunching,
	jsonEncode: (self: StudioLaunching) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioLaunching,
	descriptor: proto.Descriptor,
}

type _StudioLaunchingFields = {}

export type _StudioLaunchingPartialFields = {}

export type StudioLaunching = typeof(setmetatable({} :: _StudioLaunchingFields, {} :: _StudioLaunchingImpl))
type _StudioLaunchingMessage = proto.Message<StudioLaunching, _StudioLaunchingPartialFields>

type _StudioReadyImpl = {
	__index: _StudioReadyImpl,
	new: (fields: _StudioReadyPartialFields?) -> StudioReady,
	encode: (self: StudioReady) -> buffer,
	decode: (input: buffer) -> StudioReady,
	jsonEncode: (self: StudioReady) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioReady,
	descriptor: proto.Descriptor,
}

type _StudioReadyFields = {
	backendId: string,
	sessionGuid: string,
}

export type _StudioReadyPartialFields = {
	backendId: string?,
	sessionGuid: string?,
}

export type StudioReady = typeof(setmetatable({} :: _StudioReadyFields, {} :: _StudioReadyImpl))
type _StudioReadyMessage = proto.Message<StudioReady, _StudioReadyPartialFields>

type _StudioLaunchErrorImpl = {
	__index: _StudioLaunchErrorImpl,
	new: (fields: _StudioLaunchErrorPartialFields?) -> StudioLaunchError,
	encode: (self: StudioLaunchError) -> buffer,
	decode: (input: buffer) -> StudioLaunchError,
	jsonEncode: (self: StudioLaunchError) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioLaunchError,
	descriptor: proto.Descriptor,
}

type _StudioLaunchErrorFields = {
	message: string,
}

export type _StudioLaunchErrorPartialFields = {
	message: string?,
}

export type StudioLaunchError = typeof(setmetatable({} :: _StudioLaunchErrorFields, {} :: _StudioLaunchErrorImpl))
type _StudioLaunchErrorMessage = proto.Message<StudioLaunchError, _StudioLaunchErrorPartialFields>

type _SetStudioModeRequestImpl = {
	__index: _SetStudioModeRequestImpl,
	new: (fields: _SetStudioModeRequestPartialFields?) -> SetStudioModeRequest,
	encode: (self: SetStudioModeRequest) -> buffer,
	decode: (input: buffer) -> SetStudioModeRequest,
	jsonEncode: (self: SetStudioModeRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SetStudioModeRequest,
	descriptor: proto.Descriptor,
}

type _SetStudioModeRequestFields = {
	sessionGuid: string,
	mode: string,
}

export type _SetStudioModeRequestPartialFields = {
	sessionGuid: string?,
	mode: string?,
}

export type SetStudioModeRequest = typeof(setmetatable(
	{} :: _SetStudioModeRequestFields,
	{} :: _SetStudioModeRequestImpl
))
type _SetStudioModeRequestMessage = proto.Message<SetStudioModeRequest, _SetStudioModeRequestPartialFields>

type _SetStudioModeResponseImpl = {
	__index: _SetStudioModeResponseImpl,
	new: (fields: _SetStudioModeResponsePartialFields?) -> SetStudioModeResponse,
	encode: (self: SetStudioModeResponse) -> buffer,
	decode: (input: buffer) -> SetStudioModeResponse,
	jsonEncode: (self: SetStudioModeResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SetStudioModeResponse,
	descriptor: proto.Descriptor,
}

type _SetStudioModeResponseFields = {
	ok: boolean,
	mode: string,
}

export type _SetStudioModeResponsePartialFields = {
	ok: boolean?,
	mode: string?,
}

export type SetStudioModeResponse = typeof(setmetatable(
	{} :: _SetStudioModeResponseFields,
	{} :: _SetStudioModeResponseImpl
))
type _SetStudioModeResponseMessage = proto.Message<SetStudioModeResponse, _SetStudioModeResponsePartialFields>

type _CloseStudioRequestImpl = {
	__index: _CloseStudioRequestImpl,
	new: (fields: _CloseStudioRequestPartialFields?) -> CloseStudioRequest,
	encode: (self: CloseStudioRequest) -> buffer,
	decode: (input: buffer) -> CloseStudioRequest,
	jsonEncode: (self: CloseStudioRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CloseStudioRequest,
	descriptor: proto.Descriptor,
}

type _CloseStudioRequestFields = {
	sessionGuid: string,
}

export type _CloseStudioRequestPartialFields = {
	sessionGuid: string?,
}

export type CloseStudioRequest = typeof(setmetatable({} :: _CloseStudioRequestFields, {} :: _CloseStudioRequestImpl))
type _CloseStudioRequestMessage = proto.Message<CloseStudioRequest, _CloseStudioRequestPartialFields>

type _CloseStudioEventImpl = {
	__index: _CloseStudioEventImpl,
	new: (fields: _CloseStudioEventPartialFields?) -> CloseStudioEvent,
	encode: (self: CloseStudioEvent) -> buffer,
	decode: (input: buffer) -> CloseStudioEvent,
	jsonEncode: (self: CloseStudioEvent) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CloseStudioEvent,
	descriptor: proto.Descriptor,
}

type _CloseStudioEventFields = {
	event: ({ type: "closing", value: StudioClosing } | { type: "closed", value: StudioClosed })?,
}

export type _CloseStudioEventPartialFields = {
	event: (
		{ type: "closing", value: StudioClosing | _StudioClosingPartialFields }
		| { type: "closed", value: StudioClosed | _StudioClosedPartialFields }
	)?,
}

export type CloseStudioEvent = typeof(setmetatable({} :: _CloseStudioEventFields, {} :: _CloseStudioEventImpl))
type _CloseStudioEventMessage = proto.Message<CloseStudioEvent, _CloseStudioEventPartialFields>

type _StudioClosingImpl = {
	__index: _StudioClosingImpl,
	new: (fields: _StudioClosingPartialFields?) -> StudioClosing,
	encode: (self: StudioClosing) -> buffer,
	decode: (input: buffer) -> StudioClosing,
	jsonEncode: (self: StudioClosing) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioClosing,
	descriptor: proto.Descriptor,
}

type _StudioClosingFields = {}

export type _StudioClosingPartialFields = {}

export type StudioClosing = typeof(setmetatable({} :: _StudioClosingFields, {} :: _StudioClosingImpl))
type _StudioClosingMessage = proto.Message<StudioClosing, _StudioClosingPartialFields>

type _StudioClosedImpl = {
	__index: _StudioClosedImpl,
	new: (fields: _StudioClosedPartialFields?) -> StudioClosed,
	encode: (self: StudioClosed) -> buffer,
	decode: (input: buffer) -> StudioClosed,
	jsonEncode: (self: StudioClosed) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioClosed,
	descriptor: proto.Descriptor,
}

type _StudioClosedFields = {}

export type _StudioClosedPartialFields = {}

export type StudioClosed = typeof(setmetatable({} :: _StudioClosedFields, {} :: _StudioClosedImpl))
type _StudioClosedMessage = proto.Message<StudioClosed, _StudioClosedPartialFields>

type _SavePlaceRequestImpl = {
	__index: _SavePlaceRequestImpl,
	new: (fields: _SavePlaceRequestPartialFields?) -> SavePlaceRequest,
	encode: (self: SavePlaceRequest) -> buffer,
	decode: (input: buffer) -> SavePlaceRequest,
	jsonEncode: (self: SavePlaceRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SavePlaceRequest,
	descriptor: proto.Descriptor,
}

type _SavePlaceRequestFields = {
	backend: string?,
	sessionGuid: string?,
}

export type _SavePlaceRequestPartialFields = {
	backend: string?,
	sessionGuid: string?,
}

export type SavePlaceRequest = typeof(setmetatable({} :: _SavePlaceRequestFields, {} :: _SavePlaceRequestImpl))
type _SavePlaceRequestMessage = proto.Message<SavePlaceRequest, _SavePlaceRequestPartialFields>

type _SavePlaceResponseImpl = {
	__index: _SavePlaceResponseImpl,
	new: (fields: _SavePlaceResponsePartialFields?) -> SavePlaceResponse,
	encode: (self: SavePlaceResponse) -> buffer,
	decode: (input: buffer) -> SavePlaceResponse,
	jsonEncode: (self: SavePlaceResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SavePlaceResponse,
	descriptor: proto.Descriptor,
}

type _SavePlaceResponseFields = {
	saved: boolean,
	path: string?,
	error: string?,
}

export type _SavePlaceResponsePartialFields = {
	saved: boolean?,
	path: string?,
	error: string?,
}

export type SavePlaceResponse = typeof(setmetatable({} :: _SavePlaceResponseFields, {} :: _SavePlaceResponseImpl))
type _SavePlaceResponseMessage = proto.Message<SavePlaceResponse, _SavePlaceResponsePartialFields>

type _ListBackendsRequestImpl = {
	__index: _ListBackendsRequestImpl,
	new: (fields: _ListBackendsRequestPartialFields?) -> ListBackendsRequest,
	encode: (self: ListBackendsRequest) -> buffer,
	decode: (input: buffer) -> ListBackendsRequest,
	jsonEncode: (self: ListBackendsRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ListBackendsRequest,
	descriptor: proto.Descriptor,
}

type _ListBackendsRequestFields = {
	kind: string?,
}

export type _ListBackendsRequestPartialFields = {
	kind: string?,
}

export type ListBackendsRequest = typeof(setmetatable({} :: _ListBackendsRequestFields, {} :: _ListBackendsRequestImpl))
type _ListBackendsRequestMessage = proto.Message<ListBackendsRequest, _ListBackendsRequestPartialFields>

type _ListBackendsResponseImpl = {
	__index: _ListBackendsResponseImpl,
	new: (fields: _ListBackendsResponsePartialFields?) -> ListBackendsResponse,
	encode: (self: ListBackendsResponse) -> buffer,
	decode: (input: buffer) -> ListBackendsResponse,
	jsonEncode: (self: ListBackendsResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ListBackendsResponse,
	descriptor: proto.Descriptor,
}

type _ListBackendsResponseFields = {
	backends: { BackendInfo },
}

export type _ListBackendsResponsePartialFields = {
	backends: { BackendInfo | _BackendInfoPartialFields }?,
}

export type ListBackendsResponse = typeof(setmetatable(
	{} :: _ListBackendsResponseFields,
	{} :: _ListBackendsResponseImpl
))
type _ListBackendsResponseMessage = proto.Message<ListBackendsResponse, _ListBackendsResponsePartialFields>

type _BackendInfoImpl = {
	__index: _BackendInfoImpl,
	new: (fields: _BackendInfoPartialFields?) -> BackendInfo,
	encode: (self: BackendInfo) -> buffer,
	decode: (input: buffer) -> BackendInfo,
	jsonEncode: (self: BackendInfo) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BackendInfo,
	descriptor: proto.Descriptor,
}

type _BackendInfoFields = {
	id: string,
	kind: string,
	name: string,
}

export type _BackendInfoPartialFields = {
	id: string?,
	kind: string?,
	name: string?,
}

export type BackendInfo = typeof(setmetatable({} :: _BackendInfoFields, {} :: _BackendInfoImpl))
type _BackendInfoMessage = proto.Message<BackendInfo, _BackendInfoPartialFields>

type _RodeoSnapshotImpl = {
	__index: _RodeoSnapshotImpl,
	new: (fields: _RodeoSnapshotPartialFields?) -> RodeoSnapshot,
	encode: (self: RodeoSnapshot) -> buffer,
	decode: (input: buffer) -> RodeoSnapshot,
	jsonEncode: (self: RodeoSnapshot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> RodeoSnapshot,
	descriptor: proto.Descriptor,
}

type _RodeoSnapshotFields = {
	backends: { BackendInfo },
	processes: { ProcessInfo },
	studios: { StudioState },
}

export type _RodeoSnapshotPartialFields = {
	backends: { BackendInfo | _BackendInfoPartialFields }?,
	processes: { ProcessInfo | _ProcessInfoPartialFields }?,
	studios: { StudioState | _StudioStatePartialFields }?,
}

export type RodeoSnapshot = typeof(setmetatable({} :: _RodeoSnapshotFields, {} :: _RodeoSnapshotImpl))
type _RodeoSnapshotMessage = proto.Message<RodeoSnapshot, _RodeoSnapshotPartialFields>

type _LogFilterImpl = {
	__index: _LogFilterImpl,
	new: (fields: _LogFilterPartialFields?) -> LogFilter,
	encode: (self: LogFilter) -> buffer,
	decode: (input: buffer) -> LogFilter,
	jsonEncode: (self: LogFilter) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> LogFilter,
	descriptor: proto.Descriptor,
}

type _LogFilterFields = {
	enableWarn: boolean,
	enableError: boolean,
	enableInfo: boolean,
	enableOutput: boolean,
	enableLogs: boolean,
}

export type _LogFilterPartialFields = {
	enableWarn: boolean?,
	enableError: boolean?,
	enableInfo: boolean?,
	enableOutput: boolean?,
	enableLogs: boolean?,
}

export type LogFilter = typeof(setmetatable({} :: _LogFilterFields, {} :: _LogFilterImpl))
type _LogFilterMessage = proto.Message<LogFilter, _LogFilterPartialFields>

type _HealthResponseImpl = {
	__index: _HealthResponseImpl,
	new: (fields: _HealthResponsePartialFields?) -> HealthResponse,
	encode: (self: HealthResponse) -> buffer,
	decode: (input: buffer) -> HealthResponse,
	jsonEncode: (self: HealthResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HealthResponse,
	descriptor: proto.Descriptor,
}

type _HealthResponseFields = {
	launched: boolean,
	contextCount: number,
	totalDoms: number,
	totalQueued: number,
	contexts: { ContextInfo },
}

export type _HealthResponsePartialFields = {
	launched: boolean?,
	contextCount: number?,
	totalDoms: number?,
	totalQueued: number?,
	contexts: { ContextInfo | _ContextInfoPartialFields }?,
}

export type HealthResponse = typeof(setmetatable({} :: _HealthResponseFields, {} :: _HealthResponseImpl))
type _HealthResponseMessage = proto.Message<HealthResponse, _HealthResponsePartialFields>

type _ContextInfoImpl = {
	__index: _ContextInfoImpl,
	new: (fields: _ContextInfoPartialFields?) -> ContextInfo,
	encode: (self: ContextInfo) -> buffer,
	decode: (input: buffer) -> ContextInfo,
	jsonEncode: (self: ContextInfo) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ContextInfo,
	descriptor: proto.Descriptor,
}

type _ContextInfoFields = {
	bitset: number,
	domCount: number,
	totalQueued: number,
	doms: { DomInfo },
}

export type _ContextInfoPartialFields = {
	bitset: number?,
	domCount: number?,
	totalQueued: number?,
	doms: { DomInfo | _DomInfoPartialFields }?,
}

export type ContextInfo = typeof(setmetatable({} :: _ContextInfoFields, {} :: _ContextInfoImpl))
type _ContextInfoMessage = proto.Message<ContextInfo, _ContextInfoPartialFields>

type _DomInfoImpl = {
	__index: _DomInfoImpl,
	new: (fields: _DomInfoPartialFields?) -> DomInfo,
	encode: (self: DomInfo) -> buffer,
	decode: (input: buffer) -> DomInfo,
	jsonEncode: (self: DomInfo) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DomInfo,
	descriptor: proto.Descriptor,
}

type _DomInfoFields = {
	rodeoId: string,
	activeCount: number,
	isIdle: boolean,
}

export type _DomInfoPartialFields = {
	rodeoId: string?,
	activeCount: number?,
	isIdle: boolean?,
}

export type DomInfo = typeof(setmetatable({} :: _DomInfoFields, {} :: _DomInfoImpl))
type _DomInfoMessage = proto.Message<DomInfo, _DomInfoPartialFields>

type _ProcessInfoImpl = {
	__index: _ProcessInfoImpl,
	new: (fields: _ProcessInfoPartialFields?) -> ProcessInfo,
	encode: (self: ProcessInfo) -> buffer,
	decode: (input: buffer) -> ProcessInfo,
	jsonEncode: (self: ProcessInfo) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessInfo,
	descriptor: proto.Descriptor,
}

type _ProcessInfoFields = {
	executionId: string,
	state: string,
	mode: string,
	context: string,
	domKind: string,
	studioId: string?,
	sessionId: string?,
	domId: string?,
	createdAt: number,
}

export type _ProcessInfoPartialFields = {
	executionId: string?,
	state: string?,
	mode: string?,
	context: string?,
	domKind: string?,
	studioId: string?,
	sessionId: string?,
	domId: string?,
	createdAt: number?,
}

export type ProcessInfo = typeof(setmetatable({} :: _ProcessInfoFields, {} :: _ProcessInfoImpl))
type _ProcessInfoMessage = proto.Message<ProcessInfo, _ProcessInfoPartialFields>

type _ProcessListResponseImpl = {
	__index: _ProcessListResponseImpl,
	new: (fields: _ProcessListResponsePartialFields?) -> ProcessListResponse,
	encode: (self: ProcessListResponse) -> buffer,
	decode: (input: buffer) -> ProcessListResponse,
	jsonEncode: (self: ProcessListResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProcessListResponse,
	descriptor: proto.Descriptor,
}

type _ProcessListResponseFields = {
	processes: { ProcessInfo },
}

export type _ProcessListResponsePartialFields = {
	processes: { ProcessInfo | _ProcessInfoPartialFields }?,
}

export type ProcessListResponse = typeof(setmetatable({} :: _ProcessListResponseFields, {} :: _ProcessListResponseImpl))
type _ProcessListResponseMessage = proto.Message<ProcessListResponse, _ProcessListResponsePartialFields>

type _KillResponseImpl = {
	__index: _KillResponseImpl,
	new: (fields: _KillResponsePartialFields?) -> KillResponse,
	encode: (self: KillResponse) -> buffer,
	decode: (input: buffer) -> KillResponse,
	jsonEncode: (self: KillResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> KillResponse,
	descriptor: proto.Descriptor,
}

type _KillResponseFields = {
	killed: boolean,
	executionId: string,
}

export type _KillResponsePartialFields = {
	killed: boolean?,
	executionId: string?,
}

export type KillResponse = typeof(setmetatable({} :: _KillResponseFields, {} :: _KillResponseImpl))
type _KillResponseMessage = proto.Message<KillResponse, _KillResponsePartialFields>

type _DisconnectMsgImpl = {
	__index: _DisconnectMsgImpl,
	new: (fields: _DisconnectMsgPartialFields?) -> DisconnectMsg,
	encode: (self: DisconnectMsg) -> buffer,
	decode: (input: buffer) -> DisconnectMsg,
	jsonEncode: (self: DisconnectMsg) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DisconnectMsg,
	descriptor: proto.Descriptor,
}

type _DisconnectMsgFields = {
	executionId: string,
}

export type _DisconnectMsgPartialFields = {
	executionId: string?,
}

export type DisconnectMsg = typeof(setmetatable({} :: _DisconnectMsgFields, {} :: _DisconnectMsgImpl))
type _DisconnectMsgMessage = proto.Message<DisconnectMsg, _DisconnectMsgPartialFields>

type _PluginMessageImpl = {
	__index: _PluginMessageImpl,
	new: (fields: _PluginMessagePartialFields?) -> PluginMessage,
	encode: (self: PluginMessage) -> buffer,
	decode: (input: buffer) -> PluginMessage,
	jsonEncode: (self: PluginMessage) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PluginMessage,
	descriptor: proto.Descriptor,
}

type _PluginMessageFields = {
	msg: (
		{ type: "studioState", value: StudioStateMsg }
		| { type: "rpc", value: _runtime.ClientRpcCall }
		| { type: "done", value: ExecutionDone }
		| { type: "killed", value: ExecutionKilled }
	)?,
}

export type _PluginMessagePartialFields = {
	msg: (
		{ type: "studioState", value: StudioStateMsg | _StudioStateMsgPartialFields }
		| { type: "rpc", value: _runtime.ClientRpcCall | _runtime._ClientRpcCallPartialFields }
		| { type: "done", value: ExecutionDone | _ExecutionDonePartialFields }
		| { type: "killed", value: ExecutionKilled | _ExecutionKilledPartialFields }
	)?,
}

export type PluginMessage = typeof(setmetatable({} :: _PluginMessageFields, {} :: _PluginMessageImpl))
type _PluginMessageMessage = proto.Message<PluginMessage, _PluginMessagePartialFields>

type _ServerMessageImpl = {
	__index: _ServerMessageImpl,
	new: (fields: _ServerMessagePartialFields?) -> ServerMessage,
	encode: (self: ServerMessage) -> buffer,
	decode: (input: buffer) -> ServerMessage,
	jsonEncode: (self: ServerMessage) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ServerMessage,
	descriptor: proto.Descriptor,
}

type _ServerMessageFields = {
	msg: (
		{ type: "welcome", value: WelcomeMsg }
		| { type: "run", value: RunCommand }
		| { type: "rpcResponse", value: _runtime.ClientRpcResponse }
		| { type: "kill", value: KillCommand }
		| { type: "setTargetMode", value: SetTargetModeMsg }
	)?,
}

export type _ServerMessagePartialFields = {
	msg: (
		{ type: "welcome", value: WelcomeMsg | _WelcomeMsgPartialFields }
		| { type: "run", value: RunCommand | _RunCommandPartialFields }
		| { type: "rpcResponse", value: _runtime.ClientRpcResponse | _runtime._ClientRpcResponsePartialFields }
		| { type: "kill", value: KillCommand | _KillCommandPartialFields }
		| { type: "setTargetMode", value: SetTargetModeMsg | _SetTargetModeMsgPartialFields }
	)?,
}

export type ServerMessage = typeof(setmetatable({} :: _ServerMessageFields, {} :: _ServerMessageImpl))
type _ServerMessageMessage = proto.Message<ServerMessage, _ServerMessagePartialFields>

type _SetTargetModeMsgImpl = {
	__index: _SetTargetModeMsgImpl,
	new: (fields: _SetTargetModeMsgPartialFields?) -> SetTargetModeMsg,
	encode: (self: SetTargetModeMsg) -> buffer,
	decode: (input: buffer) -> SetTargetModeMsg,
	jsonEncode: (self: SetTargetModeMsg) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SetTargetModeMsg,
	descriptor: proto.Descriptor,
}

type _SetTargetModeMsgFields = {
	targetMode: string,
}

export type _SetTargetModeMsgPartialFields = {
	targetMode: string?,
}

export type SetTargetModeMsg = typeof(setmetatable({} :: _SetTargetModeMsgFields, {} :: _SetTargetModeMsgImpl))
type _SetTargetModeMsgMessage = proto.Message<SetTargetModeMsg, _SetTargetModeMsgPartialFields>

type _StudioStateMsgImpl = {
	__index: _StudioStateMsgImpl,
	new: (fields: _StudioStateMsgPartialFields?) -> StudioStateMsg,
	encode: (self: StudioStateMsg) -> buffer,
	decode: (input: buffer) -> StudioStateMsg,
	jsonEncode: (self: StudioStateMsg) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> StudioStateMsg,
	descriptor: proto.Descriptor,
}

type _StudioStateMsgFields = {
	domId: string,
	mcpStudioId: string?,
	isServer: boolean,
	isClient: boolean,
	isRunning: boolean,
	isEdit: boolean,
	mode: string,
	domKind: string,
	gameName: string,
	placeId: number,
	gameId: number,
	clientInfo: ClientInfo?,
	jobId: string?,
	clientDomIdsJson: string?,
	sessionGuid: string?,
	studioId: string,
}

export type _StudioStateMsgPartialFields = {
	domId: string?,
	mcpStudioId: string?,
	isServer: boolean?,
	isClient: boolean?,
	isRunning: boolean?,
	isEdit: boolean?,
	mode: string?,
	domKind: string?,
	gameName: string?,
	placeId: number?,
	gameId: number?,
	clientInfo: (ClientInfo | _ClientInfoPartialFields)?,
	jobId: string?,
	clientDomIdsJson: string?,
	sessionGuid: string?,
	studioId: string?,
}

export type StudioStateMsg = typeof(setmetatable({} :: _StudioStateMsgFields, {} :: _StudioStateMsgImpl))
type _StudioStateMsgMessage = proto.Message<StudioStateMsg, _StudioStateMsgPartialFields>

type _ClientInfoImpl = {
	__index: _ClientInfoImpl,
	new: (fields: _ClientInfoPartialFields?) -> ClientInfo,
	encode: (self: ClientInfo) -> buffer,
	decode: (input: buffer) -> ClientInfo,
	jsonEncode: (self: ClientInfo) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ClientInfo,
	descriptor: proto.Descriptor,
}

type _ClientInfoFields = {
	name: string,
	userId: number,
}

export type _ClientInfoPartialFields = {
	name: string?,
	userId: number?,
}

export type ClientInfo = typeof(setmetatable({} :: _ClientInfoFields, {} :: _ClientInfoImpl))
type _ClientInfoMessage = proto.Message<ClientInfo, _ClientInfoPartialFields>

type _WelcomeMsgImpl = {
	__index: _WelcomeMsgImpl,
	new: (fields: _WelcomeMsgPartialFields?) -> WelcomeMsg,
	encode: (self: WelcomeMsg) -> buffer,
	decode: (input: buffer) -> WelcomeMsg,
	jsonEncode: (self: WelcomeMsg) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> WelcomeMsg,
	descriptor: proto.Descriptor,
}

type _WelcomeMsgFields = {
	version: string,
}

export type _WelcomeMsgPartialFields = {
	version: string?,
}

export type WelcomeMsg = typeof(setmetatable({} :: _WelcomeMsgFields, {} :: _WelcomeMsgImpl))
type _WelcomeMsgMessage = proto.Message<WelcomeMsg, _WelcomeMsgPartialFields>

type _RunCommandImpl = {
	__index: _RunCommandImpl,
	new: (fields: _RunCommandPartialFields?) -> RunCommand,
	encode: (self: RunCommand) -> buffer,
	decode: (input: buffer) -> RunCommand,
	jsonEncode: (self: RunCommand) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> RunCommand,
	descriptor: proto.Descriptor,
}

type _RunCommandFields = {
	executionId: string,
	script: string,
	context: string,
	cacheRequires: boolean?,
	scriptArgs: { string },
	returnFile: string?,
	showReturn: boolean?,
	outputFile: string?,
	verbose: boolean?,
	instancePath: string?,
	scriptPath: string?,
	profile: boolean?,
	logFilter: LogFilter?,
}

export type _RunCommandPartialFields = {
	executionId: string?,
	script: string?,
	context: string?,
	cacheRequires: boolean?,
	scriptArgs: { string }?,
	returnFile: string?,
	showReturn: boolean?,
	outputFile: string?,
	verbose: boolean?,
	instancePath: string?,
	scriptPath: string?,
	profile: boolean?,
	logFilter: (LogFilter | _LogFilterPartialFields)?,
}

export type RunCommand = typeof(setmetatable({} :: _RunCommandFields, {} :: _RunCommandImpl))
type _RunCommandMessage = proto.Message<RunCommand, _RunCommandPartialFields>

type _KillCommandImpl = {
	__index: _KillCommandImpl,
	new: (fields: _KillCommandPartialFields?) -> KillCommand,
	encode: (self: KillCommand) -> buffer,
	decode: (input: buffer) -> KillCommand,
	jsonEncode: (self: KillCommand) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> KillCommand,
	descriptor: proto.Descriptor,
}

type _KillCommandFields = {
	executionId: string,
}

export type _KillCommandPartialFields = {
	executionId: string?,
}

export type KillCommand = typeof(setmetatable({} :: _KillCommandFields, {} :: _KillCommandImpl))
type _KillCommandMessage = proto.Message<KillCommand, _KillCommandPartialFields>

type _ExecutionDoneImpl = {
	__index: _ExecutionDoneImpl,
	new: (fields: _ExecutionDonePartialFields?) -> ExecutionDone,
	encode: (self: ExecutionDone) -> buffer,
	decode: (input: buffer) -> ExecutionDone,
	jsonEncode: (self: ExecutionDone) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ExecutionDone,
	descriptor: proto.Descriptor,
}

type _ExecutionDoneFields = {
	executionId: string,
	success: boolean,
	error: string?,
	returnValue: string?,
}

export type _ExecutionDonePartialFields = {
	executionId: string?,
	success: boolean?,
	error: string?,
	returnValue: string?,
}

export type ExecutionDone = typeof(setmetatable({} :: _ExecutionDoneFields, {} :: _ExecutionDoneImpl))
type _ExecutionDoneMessage = proto.Message<ExecutionDone, _ExecutionDonePartialFields>

type _ExecutionKilledImpl = {
	__index: _ExecutionKilledImpl,
	new: (fields: _ExecutionKilledPartialFields?) -> ExecutionKilled,
	encode: (self: ExecutionKilled) -> buffer,
	decode: (input: buffer) -> ExecutionKilled,
	jsonEncode: (self: ExecutionKilled) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ExecutionKilled,
	descriptor: proto.Descriptor,
}

type _ExecutionKilledFields = {
	executionId: string,
}

export type _ExecutionKilledPartialFields = {
	executionId: string?,
}

export type ExecutionKilled = typeof(setmetatable({} :: _ExecutionKilledFields, {} :: _ExecutionKilledImpl))
type _ExecutionKilledMessage = proto.Message<ExecutionKilled, _ExecutionKilledPartialFields>

type _ProcessStateMessage = proto.Enum<ProcessState, _ProcessStateVariants>
export type ProcessState =
	"PROCESS_STATE_QUEUED"
	| "PROCESS_STATE_RUNNING"
	| "PROCESS_STATE_DONE"
	| "PROCESS_STATE_ERROR"
	| "PROCESS_STATE_KILLED"
	| "PROCESS_STATE_DISCONNECTED"
	| number -- Unknown
type _ProcessStateVariants = {
	["PROCESS_STATE_QUEUED"]: "PROCESS_STATE_QUEUED",
	["PROCESS_STATE_RUNNING"]: "PROCESS_STATE_RUNNING",
	["PROCESS_STATE_DONE"]: "PROCESS_STATE_DONE",
	["PROCESS_STATE_ERROR"]: "PROCESS_STATE_ERROR",
	["PROCESS_STATE_KILLED"]: "PROCESS_STATE_KILLED",
	["PROCESS_STATE_DISCONNECTED"]: "PROCESS_STATE_DISCONNECTED",
}

do
	local _BackendMessageImpl = {}
	_BackendMessageImpl.__index = _BackendMessageImpl

	function _BackendMessageImpl.new(data: _BackendMessagePartialFields?): BackendMessage
		return setmetatable({
			msg = (function()
				if data == nil or data.msg == nil then
					return nil
				end
				local v = data.msg
				if
					(v :: any).type == "register"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.RegisterRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "domConnect"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.DomConnect.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "domDisconnect"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.DomDisconnect.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "domPluginMessage"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.DomPluginMessage.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "stateSnapshot"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StateSnapshot.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "playerStatus"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.PlayerStatus.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "filesComplete"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FilesComplete.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "sessionExited"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.SessionExited.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "saveResult"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.SaveResult.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _BackendMessageImpl :: _BackendMessageImpl)
	end

	function _BackendMessageImpl.encode(self: BackendMessage): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.msg ~= nil then
			if self.msg.type == "register" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "domConnect" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "domDisconnect" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "domPluginMessage" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "stateSnapshot" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "playerStatus" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "filesComplete" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "sessionExited" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "saveResult" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BackendMessageImpl.decode(input: buffer): BackendMessage
		local self = _BackendMessageImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "register", value = messages.RegisterRequest.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "domConnect", value = messages.DomConnect.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "domDisconnect", value = messages.DomDisconnect.decode(value) }
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "domPluginMessage", value = messages.DomPluginMessage.decode(value) }
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "stateSnapshot", value = messages.StateSnapshot.decode(value) }
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "playerStatus", value = messages.PlayerStatus.decode(value) }
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "filesComplete", value = messages.FilesComplete.decode(value) }
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "sessionExited", value = messages.SessionExited.decode(value) }
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "saveResult", value = messages.SaveResult.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BackendMessageImpl.jsonEncode(self: BackendMessage): any
		local output = {}

		if self.msg ~= nil then
			if self.msg.type == "register" then
				output.register = self.msg.value:jsonEncode()
			elseif self.msg.type == "domConnect" then
				output.domConnect = self.msg.value:jsonEncode()
			elseif self.msg.type == "domDisconnect" then
				output.domDisconnect = self.msg.value:jsonEncode()
			elseif self.msg.type == "domPluginMessage" then
				output.domPluginMessage = self.msg.value:jsonEncode()
			elseif self.msg.type == "stateSnapshot" then
				output.stateSnapshot = self.msg.value:jsonEncode()
			elseif self.msg.type == "playerStatus" then
				output.playerStatus = self.msg.value:jsonEncode()
			elseif self.msg.type == "filesComplete" then
				output.filesComplete = self.msg.value:jsonEncode()
			elseif self.msg.type == "sessionExited" then
				output.sessionExited = self.msg.value:jsonEncode()
			elseif self.msg.type == "saveResult" then
				output.saveResult = self.msg.value:jsonEncode()
			end
		end

		return output
	end

	function _BackendMessageImpl.jsonDecode(input: { [string]: any }): BackendMessage
		local self = _BackendMessageImpl.new()

		if input.register ~= nil then
			self.msg = { type = "register", value = messages.RegisterRequest.jsonDecode(input.register) }
		end

		if input.domConnect ~= nil then
			self.msg = { type = "domConnect", value = messages.DomConnect.jsonDecode(input.domConnect) }
		end

		if input.domDisconnect ~= nil then
			self.msg = { type = "domDisconnect", value = messages.DomDisconnect.jsonDecode(input.domDisconnect) }
		end

		if input.domPluginMessage ~= nil then
			self.msg =
				{ type = "domPluginMessage", value = messages.DomPluginMessage.jsonDecode(input.domPluginMessage) }
		end

		if input.stateSnapshot ~= nil then
			self.msg = { type = "stateSnapshot", value = messages.StateSnapshot.jsonDecode(input.stateSnapshot) }
		end

		if input.playerStatus ~= nil then
			self.msg = { type = "playerStatus", value = messages.PlayerStatus.jsonDecode(input.playerStatus) }
		end

		if input.filesComplete ~= nil then
			self.msg = { type = "filesComplete", value = messages.FilesComplete.jsonDecode(input.filesComplete) }
		end

		if input.sessionExited ~= nil then
			self.msg = { type = "sessionExited", value = messages.SessionExited.jsonDecode(input.sessionExited) }
		end

		if input.saveResult ~= nil then
			self.msg = { type = "saveResult", value = messages.SaveResult.jsonDecode(input.saveResult) }
		end

		return self
	end

	_BackendMessageImpl.descriptor = {
		name = "BackendMessage",
		fullName = "rodeo.BackendMessage",
	}

	messages.BackendMessage = _BackendMessageImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BackendMessage)
end

do
	local _FileChunkImpl = {}
	_FileChunkImpl.__index = _FileChunkImpl

	function _FileChunkImpl.new(data: _FileChunkPartialFields?): FileChunk
		return setmetatable({
			filename = if data == nil or data.filename == nil then "" else data.filename,
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
			data = if data == nil or data.data == nil then buffer.create(0) else data.data,
			isLast = if data == nil or data.isLast == nil then false else data.isLast,
		}, _FileChunkImpl :: _FileChunkImpl)
	end

	function _FileChunkImpl.encode(self: FileChunk): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.filename ~= nil and self.filename ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.filename)
		end

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		if self.data ~= nil and buffer.len(self.data) > 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, self.data, buffer.len(self.data))
		end

		if self.isLast then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.isLast then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FileChunkImpl.decode(input: buffer): FileChunk
		local self = _FileChunkImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 4 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.isLast = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.filename = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.data = value
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FileChunkImpl.jsonEncode(self: FileChunk): any
		local output = {}

		if self.filename ~= nil and self.filename ~= "" then
			output.filename = self.filename
		end

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		if self.data ~= nil and buffer.len(self.data) > 0 then
			output.data = proto.json.serializeBuffer(self.data)
		end

		if self.isLast then
			output.isLast = self.isLast
		end

		return output
	end

	function _FileChunkImpl.jsonDecode(input: { [string]: any }): FileChunk
		local self = _FileChunkImpl.new()

		if input.filename ~= nil then
			self.filename = input.filename
		end

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		if input.data ~= nil then
			self.data = proto.json.deserializeBuffer(input.data)
		end

		if input.isLast ~= nil then
			self.isLast = input.isLast
		end

		return self
	end

	_FileChunkImpl.descriptor = {
		name = "FileChunk",
		fullName = "rodeo.FileChunk",
	}

	messages.FileChunk = _FileChunkImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FileChunk)
end

do
	local _FileAckImpl = {}
	_FileAckImpl.__index = _FileAckImpl

	function _FileAckImpl.new(data: _FileAckPartialFields?): FileAck
		return setmetatable({}, _FileAckImpl :: _FileAckImpl)
	end

	function _FileAckImpl.encode(self: FileAck): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FileAckImpl.decode(input: buffer): FileAck
		local self = _FileAckImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FileAckImpl.jsonEncode(self: FileAck): any
		local output = {}

		return output
	end

	function _FileAckImpl.jsonDecode(input: { [string]: any }): FileAck
		local self = _FileAckImpl.new()

		return self
	end

	_FileAckImpl.descriptor = {
		name = "FileAck",
		fullName = "rodeo.FileAck",
	}

	messages.FileAck = _FileAckImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FileAck)
end

do
	local _FilesCompleteImpl = {}
	_FilesCompleteImpl.__index = _FilesCompleteImpl

	function _FilesCompleteImpl.new(data: _FilesCompletePartialFields?): FilesComplete
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
		}, _FilesCompleteImpl :: _FilesCompleteImpl)
	end

	function _FilesCompleteImpl.encode(self: FilesComplete): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FilesCompleteImpl.decode(input: buffer): FilesComplete
		local self = _FilesCompleteImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FilesCompleteImpl.jsonEncode(self: FilesComplete): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		return output
	end

	function _FilesCompleteImpl.jsonDecode(input: { [string]: any }): FilesComplete
		local self = _FilesCompleteImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		return self
	end

	_FilesCompleteImpl.descriptor = {
		name = "FilesComplete",
		fullName = "rodeo.FilesComplete",
	}

	messages.FilesComplete = _FilesCompleteImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FilesComplete)
end

do
	local _RegisterRequestImpl = {}
	_RegisterRequestImpl.__index = _RegisterRequestImpl

	function _RegisterRequestImpl.new(data: _RegisterRequestPartialFields?): RegisterRequest
		return setmetatable({
			kind = if data == nil or data.kind == nil then "" else data.kind,
			name = if data == nil or data.name == nil then "" else data.name,
			port = if data == nil or data.port == nil then nil else data.port,
		}, _RegisterRequestImpl :: _RegisterRequestImpl)
	end

	function _RegisterRequestImpl.encode(self: RegisterRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil and self.kind ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.kind)
		end

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.port ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.port)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _RegisterRequestImpl.decode(input: buffer): RegisterRequest
		local self = _RegisterRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 3 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.port = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _RegisterRequestImpl.jsonEncode(self: RegisterRequest): any
		local output = {}

		if self.kind ~= nil and self.kind ~= "" then
			output.kind = self.kind
		end

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.port ~= nil then
			output.port = self.port
		end

		return output
	end

	function _RegisterRequestImpl.jsonDecode(input: { [string]: any }): RegisterRequest
		local self = _RegisterRequestImpl.new()

		if input.kind ~= nil then
			self.kind = input.kind
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.port ~= nil then
			self.port = input.port
		end

		return self
	end

	_RegisterRequestImpl.descriptor = {
		name = "RegisterRequest",
		fullName = "rodeo.RegisterRequest",
	}

	messages.RegisterRequest = _RegisterRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.RegisterRequest)
end

do
	local _DomConnectImpl = {}
	_DomConnectImpl.__index = _DomConnectImpl

	function _DomConnectImpl.new(data: _DomConnectPartialFields?): DomConnect
		return setmetatable({
			domId = if data == nil or data.domId == nil then "" else data.domId,
			stateJson = if data == nil or data.stateJson == nil then "" else data.stateJson,
		}, _DomConnectImpl :: _DomConnectImpl)
	end

	function _DomConnectImpl.encode(self: DomConnect): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.domId ~= nil and self.domId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domId)
		end

		if self.stateJson ~= nil and self.stateJson ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.stateJson)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DomConnectImpl.decode(input: buffer): DomConnect
		local self = _DomConnectImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.stateJson = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DomConnectImpl.jsonEncode(self: DomConnect): any
		local output = {}

		if self.domId ~= nil and self.domId ~= "" then
			output.domId = self.domId
		end

		if self.stateJson ~= nil and self.stateJson ~= "" then
			output.stateJson = self.stateJson
		end

		return output
	end

	function _DomConnectImpl.jsonDecode(input: { [string]: any }): DomConnect
		local self = _DomConnectImpl.new()

		if input.domId ~= nil then
			self.domId = input.domId
		end

		if input.stateJson ~= nil then
			self.stateJson = input.stateJson
		end

		return self
	end

	_DomConnectImpl.descriptor = {
		name = "DomConnect",
		fullName = "rodeo.DomConnect",
	}

	messages.DomConnect = _DomConnectImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DomConnect)
end

do
	local _DomDisconnectImpl = {}
	_DomDisconnectImpl.__index = _DomDisconnectImpl

	function _DomDisconnectImpl.new(data: _DomDisconnectPartialFields?): DomDisconnect
		return setmetatable({
			domId = if data == nil or data.domId == nil then "" else data.domId,
		}, _DomDisconnectImpl :: _DomDisconnectImpl)
	end

	function _DomDisconnectImpl.encode(self: DomDisconnect): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.domId ~= nil and self.domId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DomDisconnectImpl.decode(input: buffer): DomDisconnect
		local self = _DomDisconnectImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DomDisconnectImpl.jsonEncode(self: DomDisconnect): any
		local output = {}

		if self.domId ~= nil and self.domId ~= "" then
			output.domId = self.domId
		end

		return output
	end

	function _DomDisconnectImpl.jsonDecode(input: { [string]: any }): DomDisconnect
		local self = _DomDisconnectImpl.new()

		if input.domId ~= nil then
			self.domId = input.domId
		end

		return self
	end

	_DomDisconnectImpl.descriptor = {
		name = "DomDisconnect",
		fullName = "rodeo.DomDisconnect",
	}

	messages.DomDisconnect = _DomDisconnectImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DomDisconnect)
end

do
	local _DomPluginMessageImpl = {}
	_DomPluginMessageImpl.__index = _DomPluginMessageImpl

	function _DomPluginMessageImpl.new(data: _DomPluginMessagePartialFields?): DomPluginMessage
		return setmetatable({
			domId = if data == nil or data.domId == nil then "" else data.domId,
			message = if data == nil or data.message == nil
				then nil
				elseif getmetatable(data.message :: any) == nil then messages.PluginMessage.new(
					data.message :: any
				)
				else (data.message :: any) :: PluginMessage,
		}, _DomPluginMessageImpl :: _DomPluginMessageImpl)
	end

	function _DomPluginMessageImpl.encode(self: DomPluginMessage): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.domId ~= nil and self.domId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domId)
		end

		if self.message ~= nil then
			local encoded = self.message:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DomPluginMessageImpl.decode(input: buffer): DomPluginMessage
		local self = _DomPluginMessageImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.message = messages.PluginMessage.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DomPluginMessageImpl.jsonEncode(self: DomPluginMessage): any
		local output = {}

		if self.domId ~= nil and self.domId ~= "" then
			output.domId = self.domId
		end

		if self.message ~= nil then
			output.message = self.message:jsonEncode()
		end

		return output
	end

	function _DomPluginMessageImpl.jsonDecode(input: { [string]: any }): DomPluginMessage
		local self = _DomPluginMessageImpl.new()

		if input.domId ~= nil then
			self.domId = input.domId
		end

		if input.message ~= nil then
			self.message = messages.PluginMessage.jsonDecode(input.message)
		end

		return self
	end

	_DomPluginMessageImpl.descriptor = {
		name = "DomPluginMessage",
		fullName = "rodeo.DomPluginMessage",
	}

	messages.DomPluginMessage = _DomPluginMessageImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DomPluginMessage)
end

do
	local _DomServerMessageImpl = {}
	_DomServerMessageImpl.__index = _DomServerMessageImpl

	function _DomServerMessageImpl.new(data: _DomServerMessagePartialFields?): DomServerMessage
		return setmetatable({
			domId = if data == nil or data.domId == nil then "" else data.domId,
			message = if data == nil or data.message == nil
				then nil
				elseif getmetatable(data.message :: any) == nil then messages.ServerMessage.new(
					data.message :: any
				)
				else (data.message :: any) :: ServerMessage,
		}, _DomServerMessageImpl :: _DomServerMessageImpl)
	end

	function _DomServerMessageImpl.encode(self: DomServerMessage): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.domId ~= nil and self.domId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domId)
		end

		if self.message ~= nil then
			local encoded = self.message:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DomServerMessageImpl.decode(input: buffer): DomServerMessage
		local self = _DomServerMessageImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.message = messages.ServerMessage.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DomServerMessageImpl.jsonEncode(self: DomServerMessage): any
		local output = {}

		if self.domId ~= nil and self.domId ~= "" then
			output.domId = self.domId
		end

		if self.message ~= nil then
			output.message = self.message:jsonEncode()
		end

		return output
	end

	function _DomServerMessageImpl.jsonDecode(input: { [string]: any }): DomServerMessage
		local self = _DomServerMessageImpl.new()

		if input.domId ~= nil then
			self.domId = input.domId
		end

		if input.message ~= nil then
			self.message = messages.ServerMessage.jsonDecode(input.message)
		end

		return self
	end

	_DomServerMessageImpl.descriptor = {
		name = "DomServerMessage",
		fullName = "rodeo.DomServerMessage",
	}

	messages.DomServerMessage = _DomServerMessageImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DomServerMessage)
end

do
	local _StateSnapshotImpl = {}
	_StateSnapshotImpl.__index = _StateSnapshotImpl

	function _StateSnapshotImpl.new(data: _StateSnapshotPartialFields?): StateSnapshot
		return setmetatable({
			doms = (function()
				local src = if data == nil then nil else data.doms
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.DomSnapshot.new(v :: any)
						else (v :: any) :: DomSnapshot
				end
				return out
			end)(),
			studios = (function()
				local src = if data == nil then nil else data.studios
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.StudioSnapshot.new(v :: any)
						else (v :: any) :: StudioSnapshot
				end
				return out
			end)(),
			studioInstances = (function()
				local src = if data == nil then nil else data.studioInstances
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.StudioInstanceState.new(v :: any)
						else (v :: any) :: StudioInstanceState
				end
				return out
			end)(),
		}, _StateSnapshotImpl :: _StateSnapshotImpl)
	end

	function _StateSnapshotImpl.encode(self: StateSnapshot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.doms ~= nil and #self.doms > 0 then
			for _, value in self.doms do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.studios ~= nil and #self.studios > 0 then
			for _, value in self.studios do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.studioInstances ~= nil and #self.studioInstances > 0 then
			for _, value in self.studioInstances do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StateSnapshotImpl.decode(input: buffer): StateSnapshot
		local self = _StateSnapshotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.doms, messages.DomSnapshot.decode(value))
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.studios, messages.StudioSnapshot.decode(value))
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.studioInstances, messages.StudioInstanceState.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StateSnapshotImpl.jsonEncode(self: StateSnapshot): any
		local output = {}

		if self.doms ~= nil and #self.doms > 0 then
			local newOutput = {}
			for _, value in self.doms do
				table.insert(newOutput, value:jsonEncode())
			end
			output.doms = newOutput
		end

		if self.studios ~= nil and #self.studios > 0 then
			local newOutput = {}
			for _, value in self.studios do
				table.insert(newOutput, value:jsonEncode())
			end
			output.studios = newOutput
		end

		if self.studioInstances ~= nil and #self.studioInstances > 0 then
			local newOutput = {}
			for _, value in self.studioInstances do
				table.insert(newOutput, value:jsonEncode())
			end
			output.studioInstances = newOutput
		end

		return output
	end

	function _StateSnapshotImpl.jsonDecode(input: { [string]: any }): StateSnapshot
		local self = _StateSnapshotImpl.new()

		if input.doms ~= nil then
			local newOutput: { DomSnapshot } = {}
			for _, value in input.doms do
				table.insert(newOutput, messages.DomSnapshot.jsonDecode(value))
			end

			self.doms = newOutput
		end

		if input.studios ~= nil then
			local newOutput: { StudioSnapshot } = {}
			for _, value in input.studios do
				table.insert(newOutput, messages.StudioSnapshot.jsonDecode(value))
			end

			self.studios = newOutput
		end

		if input.studioInstances ~= nil then
			local newOutput: { StudioInstanceState } = {}
			for _, value in input.studioInstances do
				table.insert(newOutput, messages.StudioInstanceState.jsonDecode(value))
			end

			self.studioInstances = newOutput
		end

		return self
	end

	_StateSnapshotImpl.descriptor = {
		name = "StateSnapshot",
		fullName = "rodeo.StateSnapshot",
	}

	messages.StateSnapshot = _StateSnapshotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StateSnapshot)
end

do
	local _StudioInstanceStateImpl = {}
	_StudioInstanceStateImpl.__index = _StudioInstanceStateImpl

	function _StudioInstanceStateImpl.new(data: _StudioInstanceStatePartialFields?): StudioInstanceState
		return setmetatable({
			sessionGuid = if data == nil or data.sessionGuid == nil then "" else data.sessionGuid,
			status = if data == nil or data.status == nil then "" else data.status,
			error = if data == nil or data.error == nil then nil else data.error,
		}, _StudioInstanceStateImpl :: _StudioInstanceStateImpl)
	end

	function _StudioInstanceStateImpl.encode(self: StudioInstanceState): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		if self.status ~= nil and self.status ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.status)
		end

		if self.error ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.error)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioInstanceStateImpl.decode(input: buffer): StudioInstanceState
		local self = _StudioInstanceStateImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.status = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.error = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioInstanceStateImpl.jsonEncode(self: StudioInstanceState): any
		local output = {}

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output.sessionGuid = self.sessionGuid
		end

		if self.status ~= nil and self.status ~= "" then
			output.status = self.status
		end

		if self.error ~= nil then
			output.error = self.error
		end

		return output
	end

	function _StudioInstanceStateImpl.jsonDecode(input: { [string]: any }): StudioInstanceState
		local self = _StudioInstanceStateImpl.new()

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		if input.status ~= nil then
			self.status = input.status
		end

		if input.error ~= nil then
			self.error = input.error
		end

		return self
	end

	_StudioInstanceStateImpl.descriptor = {
		name = "StudioInstanceState",
		fullName = "rodeo.StudioInstanceState",
	}

	messages.StudioInstanceState = _StudioInstanceStateImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioInstanceState)
end

do
	local _DomSnapshotImpl = {}
	_DomSnapshotImpl.__index = _DomSnapshotImpl

	function _DomSnapshotImpl.new(data: _DomSnapshotPartialFields?): DomSnapshot
		return setmetatable({
			domId = if data == nil or data.domId == nil then "" else data.domId,
			backendId = if data == nil or data.backendId == nil then nil else data.backendId,
			mode = if data == nil or data.mode == nil then nil else data.mode,
			domKind = if data == nil or data.domKind == nil then nil else data.domKind,
			sessionGuid = if data == nil or data.sessionGuid == nil then nil else data.sessionGuid,
			studioId = if data == nil or data.studioId == nil then nil else data.studioId,
			placeId = if data == nil or data.placeId == nil then nil else data.placeId,
			gameName = if data == nil or data.gameName == nil then nil else data.gameName,
			activeRuns = if data == nil or data.activeRuns == nil then 0 else data.activeRuns,
			connected = if data == nil or data.connected == nil then false else data.connected,
			userName = if data == nil or data.userName == nil then nil else data.userName,
			userId = if data == nil or data.userId == nil then nil else data.userId,
		}, _DomSnapshotImpl :: _DomSnapshotImpl)
	end

	function _DomSnapshotImpl.encode(self: DomSnapshot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.domId ~= nil and self.domId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domId)
		end

		if self.backendId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.backendId)
		end

		if self.mode ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mode)
		end

		if self.domKind ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domKind)
		end

		if self.sessionGuid ~= nil then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		if self.studioId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.studioId)
		end

		if self.placeId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.placeId)
		end

		if self.gameName ~= nil then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.gameName)
		end

		if self.activeRuns ~= nil and self.activeRuns ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.activeRuns)
		end

		if self.connected then
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.connected then 1 else 0)
		end

		if self.userName ~= nil then
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.userName)
		end

		if self.userId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.userId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DomSnapshotImpl.decode(input: buffer): DomSnapshot
		local self = _DomSnapshotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 6 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.placeId = value
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.activeRuns = value
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.connected = value ~= 0
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.userId = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.backendId = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mode = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domKind = proto.decodeString(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.gameName = proto.decodeString(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.userName = proto.decodeString(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.studioId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DomSnapshotImpl.jsonEncode(self: DomSnapshot): any
		local output = {}

		if self.domId ~= nil and self.domId ~= "" then
			output.domId = self.domId
		end

		if self.backendId ~= nil then
			output.backendId = self.backendId
		end

		if self.mode ~= nil then
			output.mode = self.mode
		end

		if self.domKind ~= nil then
			output.domKind = self.domKind
		end

		if self.sessionGuid ~= nil then
			output.sessionGuid = self.sessionGuid
		end

		if self.studioId ~= nil then
			output.studioId = self.studioId
		end

		if self.placeId ~= nil then
			output.placeId = self.placeId
		end

		if self.gameName ~= nil then
			output.gameName = self.gameName
		end

		if self.activeRuns ~= nil and self.activeRuns ~= 0 then
			output.activeRuns = self.activeRuns
		end

		if self.connected then
			output.connected = self.connected
		end

		if self.userName ~= nil then
			output.userName = self.userName
		end

		if self.userId ~= nil then
			output.userId = self.userId
		end

		return output
	end

	function _DomSnapshotImpl.jsonDecode(input: { [string]: any }): DomSnapshot
		local self = _DomSnapshotImpl.new()

		if input.domId ~= nil then
			self.domId = input.domId
		end

		if input.backendId ~= nil then
			self.backendId = input.backendId
		end

		if input.mode ~= nil then
			self.mode = input.mode
		end

		if input.domKind ~= nil then
			self.domKind = input.domKind
		end

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		if input.studioId ~= nil then
			self.studioId = input.studioId
		end

		if input.placeId ~= nil then
			self.placeId = input.placeId
		end

		if input.gameName ~= nil then
			self.gameName = input.gameName
		end

		if input.activeRuns ~= nil then
			self.activeRuns = input.activeRuns
		end

		if input.connected ~= nil then
			self.connected = input.connected
		end

		if input.userName ~= nil then
			self.userName = input.userName
		end

		if input.userId ~= nil then
			self.userId = input.userId
		end

		return self
	end

	_DomSnapshotImpl.descriptor = {
		name = "DomSnapshot",
		fullName = "rodeo.DomSnapshot",
	}

	messages.DomSnapshot = _DomSnapshotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DomSnapshot)
end

do
	local _StudioStateImpl = {}
	_StudioStateImpl.__index = _StudioStateImpl

	function _StudioStateImpl.new(data: _StudioStatePartialFields?): StudioState
		return setmetatable({
			studioId = if data == nil or data.studioId == nil then "" else data.studioId,
			backendId = if data == nil or data.backendId == nil then "" else data.backendId,
			sessionId = if data == nil or data.sessionId == nil then nil else data.sessionId,
			placeName = if data == nil or data.placeName == nil then "" else data.placeName,
			placeId = if data == nil or data.placeId == nil then 0 else data.placeId,
			status = if data == nil or data.status == nil then "" else data.status,
			studioMode = if data == nil or data.studioMode == nil then "" else data.studioMode,
			editDomId = if data == nil or data.editDomId == nil then nil else data.editDomId,
			doms = (function()
				local src = if data == nil then nil else data.doms
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.StudioDom.new(v :: any)
						else (v :: any) :: StudioDom
				end
				return out
			end)(),
		}, _StudioStateImpl :: _StudioStateImpl)
	end

	function _StudioStateImpl.encode(self: StudioState): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.studioId ~= nil and self.studioId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.studioId)
		end

		if self.backendId ~= nil and self.backendId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.backendId)
		end

		if self.sessionId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionId)
		end

		if self.placeName ~= nil and self.placeName ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.placeName)
		end

		if self.placeId ~= nil and self.placeId ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.placeId)
		end

		if self.status ~= nil and self.status ~= "" then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.status)
		end

		if self.studioMode ~= nil and self.studioMode ~= "" then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.studioMode)
		end

		if self.editDomId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.editDomId)
		end

		if self.doms ~= nil and #self.doms > 0 then
			for _, value in self.doms do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioStateImpl.decode(input: buffer): StudioState
		local self = _StudioStateImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 5 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.placeId = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.studioId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.backendId = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionId = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.placeName = proto.decodeString(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.status = proto.decodeString(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.studioMode = proto.decodeString(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.editDomId = proto.decodeString(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.doms, messages.StudioDom.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioStateImpl.jsonEncode(self: StudioState): any
		local output = {}

		if self.studioId ~= nil and self.studioId ~= "" then
			output.studioId = self.studioId
		end

		if self.backendId ~= nil and self.backendId ~= "" then
			output.backendId = self.backendId
		end

		if self.sessionId ~= nil then
			output.sessionId = self.sessionId
		end

		if self.placeName ~= nil and self.placeName ~= "" then
			output.placeName = self.placeName
		end

		if self.placeId ~= nil and self.placeId ~= 0 then
			output.placeId = self.placeId
		end

		if self.status ~= nil and self.status ~= "" then
			output.status = self.status
		end

		if self.studioMode ~= nil and self.studioMode ~= "" then
			output.studioMode = self.studioMode
		end

		if self.editDomId ~= nil then
			output.editDomId = self.editDomId
		end

		if self.doms ~= nil and #self.doms > 0 then
			local newOutput = {}
			for _, value in self.doms do
				table.insert(newOutput, value:jsonEncode())
			end
			output.doms = newOutput
		end

		return output
	end

	function _StudioStateImpl.jsonDecode(input: { [string]: any }): StudioState
		local self = _StudioStateImpl.new()

		if input.studioId ~= nil then
			self.studioId = input.studioId
		end

		if input.backendId ~= nil then
			self.backendId = input.backendId
		end

		if input.sessionId ~= nil then
			self.sessionId = input.sessionId
		end

		if input.placeName ~= nil then
			self.placeName = input.placeName
		end

		if input.placeId ~= nil then
			self.placeId = input.placeId
		end

		if input.status ~= nil then
			self.status = input.status
		end

		if input.studioMode ~= nil then
			self.studioMode = input.studioMode
		end

		if input.editDomId ~= nil then
			self.editDomId = input.editDomId
		end

		if input.doms ~= nil then
			local newOutput: { StudioDom } = {}
			for _, value in input.doms do
				table.insert(newOutput, messages.StudioDom.jsonDecode(value))
			end

			self.doms = newOutput
		end

		return self
	end

	_StudioStateImpl.descriptor = {
		name = "StudioState",
		fullName = "rodeo.StudioState",
	}

	messages.StudioState = _StudioStateImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioState)
end

do
	local _StudioDomImpl = {}
	_StudioDomImpl.__index = _StudioDomImpl

	function _StudioDomImpl.new(data: _StudioDomPartialFields?): StudioDom
		return setmetatable({
			domId = if data == nil or data.domId == nil then "" else data.domId,
			domKind = if data == nil or data.domKind == nil then "" else data.domKind,
			userName = if data == nil or data.userName == nil then nil else data.userName,
			userId = if data == nil or data.userId == nil then nil else data.userId,
		}, _StudioDomImpl :: _StudioDomImpl)
	end

	function _StudioDomImpl.encode(self: StudioDom): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.domId ~= nil and self.domId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domId)
		end

		if self.domKind ~= nil and self.domKind ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domKind)
		end

		if self.userName ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.userName)
		end

		if self.userId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.userId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioDomImpl.decode(input: buffer): StudioDom
		local self = _StudioDomImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 4 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.userId = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domKind = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.userName = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioDomImpl.jsonEncode(self: StudioDom): any
		local output = {}

		if self.domId ~= nil and self.domId ~= "" then
			output.domId = self.domId
		end

		if self.domKind ~= nil and self.domKind ~= "" then
			output.domKind = self.domKind
		end

		if self.userName ~= nil then
			output.userName = self.userName
		end

		if self.userId ~= nil then
			output.userId = self.userId
		end

		return output
	end

	function _StudioDomImpl.jsonDecode(input: { [string]: any }): StudioDom
		local self = _StudioDomImpl.new()

		if input.domId ~= nil then
			self.domId = input.domId
		end

		if input.domKind ~= nil then
			self.domKind = input.domKind
		end

		if input.userName ~= nil then
			self.userName = input.userName
		end

		if input.userId ~= nil then
			self.userId = input.userId
		end

		return self
	end

	_StudioDomImpl.descriptor = {
		name = "StudioDom",
		fullName = "rodeo.StudioDom",
	}

	messages.StudioDom = _StudioDomImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioDom)
end

do
	local _StudioSnapshotImpl = {}
	_StudioSnapshotImpl.__index = _StudioSnapshotImpl

	function _StudioSnapshotImpl.new(data: _StudioSnapshotPartialFields?): StudioSnapshot
		return setmetatable({
			sessionGuid = if data == nil or data.sessionGuid == nil then "" else data.sessionGuid,
			mode = if data == nil or data.mode == nil then "" else data.mode,
			name = if data == nil or data.name == nil then "" else data.name,
			placeId = if data == nil or data.placeId == nil then 0 else data.placeId,
		}, _StudioSnapshotImpl :: _StudioSnapshotImpl)
	end

	function _StudioSnapshotImpl.encode(self: StudioSnapshot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		if self.mode ~= nil and self.mode ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mode)
		end

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.placeId ~= nil and self.placeId ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.placeId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioSnapshotImpl.decode(input: buffer): StudioSnapshot
		local self = _StudioSnapshotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 4 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.placeId = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mode = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioSnapshotImpl.jsonEncode(self: StudioSnapshot): any
		local output = {}

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output.sessionGuid = self.sessionGuid
		end

		if self.mode ~= nil and self.mode ~= "" then
			output.mode = self.mode
		end

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.placeId ~= nil and self.placeId ~= 0 then
			output.placeId = self.placeId
		end

		return output
	end

	function _StudioSnapshotImpl.jsonDecode(input: { [string]: any }): StudioSnapshot
		local self = _StudioSnapshotImpl.new()

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		if input.mode ~= nil then
			self.mode = input.mode
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.placeId ~= nil then
			self.placeId = input.placeId
		end

		return self
	end

	_StudioSnapshotImpl.descriptor = {
		name = "StudioSnapshot",
		fullName = "rodeo.StudioSnapshot",
	}

	messages.StudioSnapshot = _StudioSnapshotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioSnapshot)
end

do
	local _PlayerStatusImpl = {}
	_PlayerStatusImpl.__index = _PlayerStatusImpl

	function _PlayerStatusImpl.new(data: _PlayerStatusPartialFields?): PlayerStatus
		return setmetatable({
			state = if data == nil or data.state == nil then "" else data.state,
			placeId = if data == nil or data.placeId == nil then nil else data.placeId,
			error = if data == nil or data.error == nil then nil else data.error,
		}, _PlayerStatusImpl :: _PlayerStatusImpl)
	end

	function _PlayerStatusImpl.encode(self: PlayerStatus): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.state ~= nil and self.state ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.state)
		end

		if self.placeId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.placeId)
		end

		if self.error ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.error)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PlayerStatusImpl.decode(input: buffer): PlayerStatus
		local self = _PlayerStatusImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntU64(input, cursor)
					self.placeId = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.state = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.error = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PlayerStatusImpl.jsonEncode(self: PlayerStatus): any
		local output = {}

		if self.state ~= nil and self.state ~= "" then
			output.state = self.state
		end

		if self.placeId ~= nil then
			output.placeId = self.placeId
		end

		if self.error ~= nil then
			output.error = self.error
		end

		return output
	end

	function _PlayerStatusImpl.jsonDecode(input: { [string]: any }): PlayerStatus
		local self = _PlayerStatusImpl.new()

		if input.state ~= nil then
			self.state = input.state
		end

		if input.placeId ~= nil then
			self.placeId = input.placeId
		end

		if input.error ~= nil then
			self.error = input.error
		end

		return self
	end

	_PlayerStatusImpl.descriptor = {
		name = "PlayerStatus",
		fullName = "rodeo.PlayerStatus",
	}

	messages.PlayerStatus = _PlayerStatusImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PlayerStatus)
end

do
	local _MasterMessageImpl = {}
	_MasterMessageImpl.__index = _MasterMessageImpl

	function _MasterMessageImpl.new(data: _MasterMessagePartialFields?): MasterMessage
		return setmetatable({
			msg = (function()
				if data == nil or data.msg == nil then
					return nil
				end
				local v = data.msg
				if
					(v :: any).type == "registered"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.RegisterResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "domServerMessage"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.DomServerMessage.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "save"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.SaveCommand.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "runCompleted"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.RunCompleted.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "launchStudio"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.LaunchStudioCommand.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "closeStudio"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.CloseStudioCommand.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _MasterMessageImpl :: _MasterMessageImpl)
	end

	function _MasterMessageImpl.encode(self: MasterMessage): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.msg ~= nil then
			if self.msg.type == "registered" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "domServerMessage" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "save" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "runCompleted" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "launchStudio" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "closeStudio" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MasterMessageImpl.decode(input: buffer): MasterMessage
		local self = _MasterMessageImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "registered", value = messages.RegisterResponse.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "domServerMessage", value = messages.DomServerMessage.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "save", value = messages.SaveCommand.decode(value) }
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "runCompleted", value = messages.RunCompleted.decode(value) }
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "launchStudio", value = messages.LaunchStudioCommand.decode(value) }
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "closeStudio", value = messages.CloseStudioCommand.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MasterMessageImpl.jsonEncode(self: MasterMessage): any
		local output = {}

		if self.msg ~= nil then
			if self.msg.type == "registered" then
				output.registered = self.msg.value:jsonEncode()
			elseif self.msg.type == "domServerMessage" then
				output.domServerMessage = self.msg.value:jsonEncode()
			elseif self.msg.type == "save" then
				output.save = self.msg.value:jsonEncode()
			elseif self.msg.type == "runCompleted" then
				output.runCompleted = self.msg.value:jsonEncode()
			elseif self.msg.type == "launchStudio" then
				output.launchStudio = self.msg.value:jsonEncode()
			elseif self.msg.type == "closeStudio" then
				output.closeStudio = self.msg.value:jsonEncode()
			end
		end

		return output
	end

	function _MasterMessageImpl.jsonDecode(input: { [string]: any }): MasterMessage
		local self = _MasterMessageImpl.new()

		if input.registered ~= nil then
			self.msg = { type = "registered", value = messages.RegisterResponse.jsonDecode(input.registered) }
		end

		if input.domServerMessage ~= nil then
			self.msg =
				{ type = "domServerMessage", value = messages.DomServerMessage.jsonDecode(input.domServerMessage) }
		end

		if input.save ~= nil then
			self.msg = { type = "save", value = messages.SaveCommand.jsonDecode(input.save) }
		end

		if input.runCompleted ~= nil then
			self.msg = { type = "runCompleted", value = messages.RunCompleted.jsonDecode(input.runCompleted) }
		end

		if input.launchStudio ~= nil then
			self.msg = { type = "launchStudio", value = messages.LaunchStudioCommand.jsonDecode(input.launchStudio) }
		end

		if input.closeStudio ~= nil then
			self.msg = { type = "closeStudio", value = messages.CloseStudioCommand.jsonDecode(input.closeStudio) }
		end

		return self
	end

	_MasterMessageImpl.descriptor = {
		name = "MasterMessage",
		fullName = "rodeo.MasterMessage",
	}

	messages.MasterMessage = _MasterMessageImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MasterMessage)
end

do
	local _CloseStudioCommandImpl = {}
	_CloseStudioCommandImpl.__index = _CloseStudioCommandImpl

	function _CloseStudioCommandImpl.new(data: _CloseStudioCommandPartialFields?): CloseStudioCommand
		return setmetatable({
			sessionGuid = if data == nil or data.sessionGuid == nil then "" else data.sessionGuid,
		}, _CloseStudioCommandImpl :: _CloseStudioCommandImpl)
	end

	function _CloseStudioCommandImpl.encode(self: CloseStudioCommand): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CloseStudioCommandImpl.decode(input: buffer): CloseStudioCommand
		local self = _CloseStudioCommandImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CloseStudioCommandImpl.jsonEncode(self: CloseStudioCommand): any
		local output = {}

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output.sessionGuid = self.sessionGuid
		end

		return output
	end

	function _CloseStudioCommandImpl.jsonDecode(input: { [string]: any }): CloseStudioCommand
		local self = _CloseStudioCommandImpl.new()

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		return self
	end

	_CloseStudioCommandImpl.descriptor = {
		name = "CloseStudioCommand",
		fullName = "rodeo.CloseStudioCommand",
	}

	messages.CloseStudioCommand = _CloseStudioCommandImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CloseStudioCommand)
end

do
	local _RunCompletedImpl = {}
	_RunCompletedImpl.__index = _RunCompletedImpl

	function _RunCompletedImpl.new(data: _RunCompletedPartialFields?): RunCompleted
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
		}, _RunCompletedImpl :: _RunCompletedImpl)
	end

	function _RunCompletedImpl.encode(self: RunCompleted): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _RunCompletedImpl.decode(input: buffer): RunCompleted
		local self = _RunCompletedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _RunCompletedImpl.jsonEncode(self: RunCompleted): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		return output
	end

	function _RunCompletedImpl.jsonDecode(input: { [string]: any }): RunCompleted
		local self = _RunCompletedImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		return self
	end

	_RunCompletedImpl.descriptor = {
		name = "RunCompleted",
		fullName = "rodeo.RunCompleted",
	}

	messages.RunCompleted = _RunCompletedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.RunCompleted)
end

do
	local _RegisterResponseImpl = {}
	_RegisterResponseImpl.__index = _RegisterResponseImpl

	function _RegisterResponseImpl.new(data: _RegisterResponsePartialFields?): RegisterResponse
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			masterId = if data == nil or data.masterId == nil then "" else data.masterId,
		}, _RegisterResponseImpl :: _RegisterResponseImpl)
	end

	function _RegisterResponseImpl.encode(self: RegisterResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.masterId ~= nil and self.masterId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.masterId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _RegisterResponseImpl.decode(input: buffer): RegisterResponse
		local self = _RegisterResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.masterId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _RegisterResponseImpl.jsonEncode(self: RegisterResponse): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.masterId ~= nil and self.masterId ~= "" then
			output.masterId = self.masterId
		end

		return output
	end

	function _RegisterResponseImpl.jsonDecode(input: { [string]: any }): RegisterResponse
		local self = _RegisterResponseImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.masterId ~= nil then
			self.masterId = input.masterId
		end

		return self
	end

	_RegisterResponseImpl.descriptor = {
		name = "RegisterResponse",
		fullName = "rodeo.RegisterResponse",
	}

	messages.RegisterResponse = _RegisterResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.RegisterResponse)
end

do
	local _SaveCommandImpl = {}
	_SaveCommandImpl.__index = _SaveCommandImpl

	function _SaveCommandImpl.new(data: _SaveCommandPartialFields?): SaveCommand
		return setmetatable({
			requestId = if data == nil or data.requestId == nil then "" else data.requestId,
			sessionGuid = if data == nil or data.sessionGuid == nil then nil else data.sessionGuid,
		}, _SaveCommandImpl :: _SaveCommandImpl)
	end

	function _SaveCommandImpl.encode(self: SaveCommand): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.requestId ~= nil and self.requestId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.requestId)
		end

		if self.sessionGuid ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SaveCommandImpl.decode(input: buffer): SaveCommand
		local self = _SaveCommandImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.requestId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SaveCommandImpl.jsonEncode(self: SaveCommand): any
		local output = {}

		if self.requestId ~= nil and self.requestId ~= "" then
			output.requestId = self.requestId
		end

		if self.sessionGuid ~= nil then
			output.sessionGuid = self.sessionGuid
		end

		return output
	end

	function _SaveCommandImpl.jsonDecode(input: { [string]: any }): SaveCommand
		local self = _SaveCommandImpl.new()

		if input.requestId ~= nil then
			self.requestId = input.requestId
		end

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		return self
	end

	_SaveCommandImpl.descriptor = {
		name = "SaveCommand",
		fullName = "rodeo.SaveCommand",
	}

	messages.SaveCommand = _SaveCommandImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SaveCommand)
end

do
	local _SaveResultImpl = {}
	_SaveResultImpl.__index = _SaveResultImpl

	function _SaveResultImpl.new(data: _SaveResultPartialFields?): SaveResult
		return setmetatable({
			requestId = if data == nil or data.requestId == nil then "" else data.requestId,
			sessionGuid = if data == nil or data.sessionGuid == nil then nil else data.sessionGuid,
			saved = if data == nil or data.saved == nil then false else data.saved,
			path = if data == nil or data.path == nil then nil else data.path,
			error = if data == nil or data.error == nil then nil else data.error,
		}, _SaveResultImpl :: _SaveResultImpl)
	end

	function _SaveResultImpl.encode(self: SaveResult): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.requestId ~= nil and self.requestId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.requestId)
		end

		if self.sessionGuid ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		if self.saved then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.saved then 1 else 0)
		end

		if self.path ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		if self.error ~= nil then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.error)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SaveResultImpl.decode(input: buffer): SaveResult
		local self = _SaveResultImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 3 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.saved = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.requestId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.error = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SaveResultImpl.jsonEncode(self: SaveResult): any
		local output = {}

		if self.requestId ~= nil and self.requestId ~= "" then
			output.requestId = self.requestId
		end

		if self.sessionGuid ~= nil then
			output.sessionGuid = self.sessionGuid
		end

		if self.saved then
			output.saved = self.saved
		end

		if self.path ~= nil then
			output.path = self.path
		end

		if self.error ~= nil then
			output.error = self.error
		end

		return output
	end

	function _SaveResultImpl.jsonDecode(input: { [string]: any }): SaveResult
		local self = _SaveResultImpl.new()

		if input.requestId ~= nil then
			self.requestId = input.requestId
		end

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		if input.saved ~= nil then
			self.saved = input.saved
		end

		if input.path ~= nil then
			self.path = input.path
		end

		if input.error ~= nil then
			self.error = input.error
		end

		return self
	end

	_SaveResultImpl.descriptor = {
		name = "SaveResult",
		fullName = "rodeo.SaveResult",
	}

	messages.SaveResult = _SaveResultImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SaveResult)
end

do
	local _LaunchStudioCommandImpl = {}
	_LaunchStudioCommandImpl.__index = _LaunchStudioCommandImpl

	function _LaunchStudioCommandImpl.new(data: _LaunchStudioCommandPartialFields?): LaunchStudioCommand
		return setmetatable({
			placeFile = if data == nil or data.placeFile == nil then nil else data.placeFile,
			placeId = if data == nil or data.placeId == nil then nil else data.placeId,
			fflags = if data == nil or data.fflags == nil then {} else data.fflags,
			background = if data == nil or data.background == nil then false else data.background,
			detached = if data == nil or data.detached == nil then false else data.detached,
			savePath = if data == nil or data.savePath == nil then nil else data.savePath,
			fflagFile = if data == nil or data.fflagFile == nil then nil else data.fflagFile,
			sessionGuid = if data == nil or data.sessionGuid == nil then "" else data.sessionGuid,
			showWidgets = if data == nil or data.showWidgets == nil then "" else data.showWidgets,
			profile = if data == nil or data.profile == nil then false else data.profile,
		}, _LaunchStudioCommandImpl :: _LaunchStudioCommandImpl)
	end

	function _LaunchStudioCommandImpl.encode(self: LaunchStudioCommand): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.placeFile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.placeFile)
		end

		if self.placeId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.placeId)
		end

		if self.fflags ~= nil and #self.fflags > 0 then
			for _, value in self.fflags do
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.background then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.background then 1 else 0)
		end

		if self.detached then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.detached then 1 else 0)
		end

		if self.savePath ~= nil then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.savePath)
		end

		if self.fflagFile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.fflagFile)
		end

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		if self.showWidgets ~= nil and self.showWidgets ~= "" then
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.showWidgets)
		end

		if self.profile then
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.profile then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _LaunchStudioCommandImpl.decode(input: buffer): LaunchStudioCommand
		local self = _LaunchStudioCommandImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntU64(input, cursor)
					self.placeId = value
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.background = value ~= 0
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.detached = value ~= 0
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.profile = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.placeFile = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.fflags, proto.decodeString(value))
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.savePath = proto.decodeString(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.fflagFile = proto.decodeString(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.showWidgets = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _LaunchStudioCommandImpl.jsonEncode(self: LaunchStudioCommand): any
		local output = {}

		if self.placeFile ~= nil then
			output.placeFile = self.placeFile
		end

		if self.placeId ~= nil then
			output.placeId = self.placeId
		end

		if self.fflags ~= nil and #self.fflags > 0 then
			local newOutput = {}
			for _, value in self.fflags do
				table.insert(newOutput, value)
			end
			output.fflags = newOutput
		end

		if self.background then
			output.background = self.background
		end

		if self.detached then
			output.detached = self.detached
		end

		if self.savePath ~= nil then
			output.savePath = self.savePath
		end

		if self.fflagFile ~= nil then
			output.fflagFile = self.fflagFile
		end

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output.sessionGuid = self.sessionGuid
		end

		if self.showWidgets ~= nil and self.showWidgets ~= "" then
			output.showWidgets = self.showWidgets
		end

		if self.profile then
			output.profile = self.profile
		end

		return output
	end

	function _LaunchStudioCommandImpl.jsonDecode(input: { [string]: any }): LaunchStudioCommand
		local self = _LaunchStudioCommandImpl.new()

		if input.placeFile ~= nil then
			self.placeFile = input.placeFile
		end

		if input.placeId ~= nil then
			self.placeId = input.placeId
		end

		if input.fflags ~= nil then
			local newOutput: { string } = {}
			for _, value in input.fflags do
				table.insert(newOutput, value)
			end

			self.fflags = newOutput
		end

		if input.background ~= nil then
			self.background = input.background
		end

		if input.detached ~= nil then
			self.detached = input.detached
		end

		if input.savePath ~= nil then
			self.savePath = input.savePath
		end

		if input.fflagFile ~= nil then
			self.fflagFile = input.fflagFile
		end

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		if input.showWidgets ~= nil then
			self.showWidgets = input.showWidgets
		end

		if input.profile ~= nil then
			self.profile = input.profile
		end

		return self
	end

	_LaunchStudioCommandImpl.descriptor = {
		name = "LaunchStudioCommand",
		fullName = "rodeo.LaunchStudioCommand",
	}

	messages.LaunchStudioCommand = _LaunchStudioCommandImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.LaunchStudioCommand)
end

do
	local _SessionExitedImpl = {}
	_SessionExitedImpl.__index = _SessionExitedImpl

	function _SessionExitedImpl.new(data: _SessionExitedPartialFields?): SessionExited
		return setmetatable({
			sessionGuid = if data == nil or data.sessionGuid == nil then "" else data.sessionGuid,
			reason = if data == nil or data.reason == nil then "" else data.reason,
		}, _SessionExitedImpl :: _SessionExitedImpl)
	end

	function _SessionExitedImpl.encode(self: SessionExited): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		if self.reason ~= nil and self.reason ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.reason)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SessionExitedImpl.decode(input: buffer): SessionExited
		local self = _SessionExitedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.reason = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SessionExitedImpl.jsonEncode(self: SessionExited): any
		local output = {}

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output.sessionGuid = self.sessionGuid
		end

		if self.reason ~= nil and self.reason ~= "" then
			output.reason = self.reason
		end

		return output
	end

	function _SessionExitedImpl.jsonDecode(input: { [string]: any }): SessionExited
		local self = _SessionExitedImpl.new()

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		if input.reason ~= nil then
			self.reason = input.reason
		end

		return self
	end

	_SessionExitedImpl.descriptor = {
		name = "SessionExited",
		fullName = "rodeo.SessionExited",
	}

	messages.SessionExited = _SessionExitedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SessionExited)
end

do
	local _RunClientMessageImpl = {}
	_RunClientMessageImpl.__index = _RunClientMessageImpl

	function _RunClientMessageImpl.new(data: _RunClientMessagePartialFields?): RunClientMessage
		return setmetatable({
			msg = (function()
				if data == nil or data.msg == nil then
					return nil
				end
				local v = data.msg
				if
					(v :: any).type == "submit"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.SubmitRequest.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "rpcResponse"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = _runtime.ClientRpcResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "kill"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.KillCommand.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _RunClientMessageImpl :: _RunClientMessageImpl)
	end

	function _RunClientMessageImpl.encode(self: RunClientMessage): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.msg ~= nil then
			if self.msg.type == "submit" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "rpcResponse" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "kill" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _RunClientMessageImpl.decode(input: buffer): RunClientMessage
		local self = _RunClientMessageImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "submit", value = messages.SubmitRequest.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "rpcResponse", value = _runtime.ClientRpcResponse.decode(value) }
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "kill", value = messages.KillCommand.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _RunClientMessageImpl.jsonEncode(self: RunClientMessage): any
		local output = {}

		if self.msg ~= nil then
			if self.msg.type == "submit" then
				output.submit = self.msg.value:jsonEncode()
			elseif self.msg.type == "rpcResponse" then
				output.rpcResponse = self.msg.value:jsonEncode()
			elseif self.msg.type == "kill" then
				output.kill = self.msg.value:jsonEncode()
			end
		end

		return output
	end

	function _RunClientMessageImpl.jsonDecode(input: { [string]: any }): RunClientMessage
		local self = _RunClientMessageImpl.new()

		if input.submit ~= nil then
			self.msg = { type = "submit", value = messages.SubmitRequest.jsonDecode(input.submit) }
		end

		if input.rpcResponse ~= nil then
			self.msg = { type = "rpcResponse", value = _runtime.ClientRpcResponse.jsonDecode(input.rpcResponse) }
		end

		if input.kill ~= nil then
			self.msg = { type = "kill", value = messages.KillCommand.jsonDecode(input.kill) }
		end

		return self
	end

	_RunClientMessageImpl.descriptor = {
		name = "RunClientMessage",
		fullName = "rodeo.RunClientMessage",
	}

	messages.RunClientMessage = _RunClientMessageImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.RunClientMessage)
end

do
	local _SubmitRequestImpl = {}
	_SubmitRequestImpl.__index = _SubmitRequestImpl

	function _SubmitRequestImpl.new(data: _SubmitRequestPartialFields?): SubmitRequest
		return setmetatable({
			script = if data == nil or data.script == nil then "" else data.script,
			mode = if data == nil or data.mode == nil then nil else data.mode,
			session = if data == nil or data.session == nil then nil else data.session,
			domId = if data == nil or data.domId == nil then nil else data.domId,
			domKind = if data == nil or data.domKind == nil then nil else data.domKind,
			context = if data == nil or data.context == nil then nil else data.context,
			cacheRequires = if data == nil or data.cacheRequires == nil then nil else data.cacheRequires,
			scriptArgs = if data == nil or data.scriptArgs == nil then {} else data.scriptArgs,
			returnFile = if data == nil or data.returnFile == nil then nil else data.returnFile,
			showReturn = if data == nil or data.showReturn == nil then nil else data.showReturn,
			outputFile = if data == nil or data.outputFile == nil then nil else data.outputFile,
			verbose = if data == nil or data.verbose == nil then nil else data.verbose,
			instancePath = if data == nil or data.instancePath == nil then nil else data.instancePath,
			scriptPath = if data == nil or data.scriptPath == nil then nil else data.scriptPath,
			profile = if data == nil or data.profile == nil then nil else data.profile,
			logFilter = if data == nil or data.logFilter == nil
				then nil
				elseif getmetatable(data.logFilter :: any) == nil then messages.LogFilter.new(
					data.logFilter :: any
				)
				else (data.logFilter :: any) :: LogFilter,
		}, _SubmitRequestImpl :: _SubmitRequestImpl)
	end

	function _SubmitRequestImpl.encode(self: SubmitRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.script ~= nil and self.script ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.script)
		end

		if self.mode ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mode)
		end

		if self.session ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.session)
		end

		if self.domId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domId)
		end

		if self.domKind ~= nil then
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domKind)
		end

		if self.context ~= nil then
			output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.context)
		end

		if self.cacheRequires ~= nil then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.cacheRequires then 1 else 0)
		end

		if self.scriptArgs ~= nil and #self.scriptArgs > 0 then
			for _, value in self.scriptArgs do
				output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.returnFile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.returnFile)
		end

		if self.showReturn ~= nil then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.showReturn then 1 else 0)
		end

		if self.outputFile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.outputFile)
		end

		if self.verbose ~= nil then
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.verbose then 1 else 0)
		end

		if self.instancePath ~= nil then
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.instancePath)
		end

		if self.scriptPath ~= nil then
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.scriptPath)
		end

		if self.profile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.profile then 1 else 0)
		end

		if self.logFilter ~= nil then
			local encoded = self.logFilter:encode()
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SubmitRequestImpl.decode(input: buffer): SubmitRequest
		local self = _SubmitRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 5 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.cacheRequires = value ~= 0
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.showReturn = value ~= 0
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.verbose = value ~= 0
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.profile = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.script = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mode = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.session = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domId = proto.decodeString(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.scriptArgs, proto.decodeString(value))
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.returnFile = proto.decodeString(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.outputFile = proto.decodeString(value)
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.instancePath = proto.decodeString(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.scriptPath = proto.decodeString(value)
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.logFilter = messages.LogFilter.decode(value)
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domKind = proto.decodeString(value)
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.context = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SubmitRequestImpl.jsonEncode(self: SubmitRequest): any
		local output = {}

		if self.script ~= nil and self.script ~= "" then
			output.script = self.script
		end

		if self.mode ~= nil then
			output.mode = self.mode
		end

		if self.session ~= nil then
			output.session = self.session
		end

		if self.domId ~= nil then
			output.domId = self.domId
		end

		if self.domKind ~= nil then
			output.domKind = self.domKind
		end

		if self.context ~= nil then
			output.context = self.context
		end

		if self.cacheRequires ~= nil then
			output.cacheRequires = self.cacheRequires
		end

		if self.scriptArgs ~= nil and #self.scriptArgs > 0 then
			local newOutput = {}
			for _, value in self.scriptArgs do
				table.insert(newOutput, value)
			end
			output.scriptArgs = newOutput
		end

		if self.returnFile ~= nil then
			output.returnFile = self.returnFile
		end

		if self.showReturn ~= nil then
			output.showReturn = self.showReturn
		end

		if self.outputFile ~= nil then
			output.outputFile = self.outputFile
		end

		if self.verbose ~= nil then
			output.verbose = self.verbose
		end

		if self.instancePath ~= nil then
			output.instancePath = self.instancePath
		end

		if self.scriptPath ~= nil then
			output.scriptPath = self.scriptPath
		end

		if self.profile ~= nil then
			output.profile = self.profile
		end

		if self.logFilter ~= nil then
			output.logFilter = self.logFilter:jsonEncode()
		end

		return output
	end

	function _SubmitRequestImpl.jsonDecode(input: { [string]: any }): SubmitRequest
		local self = _SubmitRequestImpl.new()

		if input.script ~= nil then
			self.script = input.script
		end

		if input.mode ~= nil then
			self.mode = input.mode
		end

		if input.session ~= nil then
			self.session = input.session
		end

		if input.domId ~= nil then
			self.domId = input.domId
		end

		if input.domKind ~= nil then
			self.domKind = input.domKind
		end

		if input.context ~= nil then
			self.context = input.context
		end

		if input.cacheRequires ~= nil then
			self.cacheRequires = input.cacheRequires
		end

		if input.scriptArgs ~= nil then
			local newOutput: { string } = {}
			for _, value in input.scriptArgs do
				table.insert(newOutput, value)
			end

			self.scriptArgs = newOutput
		end

		if input.returnFile ~= nil then
			self.returnFile = input.returnFile
		end

		if input.showReturn ~= nil then
			self.showReturn = input.showReturn
		end

		if input.outputFile ~= nil then
			self.outputFile = input.outputFile
		end

		if input.verbose ~= nil then
			self.verbose = input.verbose
		end

		if input.instancePath ~= nil then
			self.instancePath = input.instancePath
		end

		if input.scriptPath ~= nil then
			self.scriptPath = input.scriptPath
		end

		if input.profile ~= nil then
			self.profile = input.profile
		end

		if input.logFilter ~= nil then
			self.logFilter = messages.LogFilter.jsonDecode(input.logFilter)
		end

		return self
	end

	_SubmitRequestImpl.descriptor = {
		name = "SubmitRequest",
		fullName = "rodeo.SubmitRequest",
	}

	messages.SubmitRequest = _SubmitRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SubmitRequest)
end

do
	local _RunEventImpl = {}
	_RunEventImpl.__index = _RunEventImpl

	function _RunEventImpl.new(data: _RunEventPartialFields?): RunEvent
		return setmetatable({
			event = (function()
				if data == nil or data.event == nil then
					return nil
				end
				local v = data.event
				if
					(v :: any).type == "created"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ProcessCreated.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "fileChunk"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.FileChunk.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "complete"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.RunComplete.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "rpcCall"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = _runtime.ClientRpcCall.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "executionDone"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ExecutionDone.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "executionKilled"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ExecutionKilled.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _RunEventImpl :: _RunEventImpl)
	end

	function _RunEventImpl.encode(self: RunEvent): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.event ~= nil then
			if self.event.type == "created" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "disconnect" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.event.value)
			elseif self.event.type == "fileChunk" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "complete" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "rpcCall" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "executionDone" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "executionKilled" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _RunEventImpl.decode(input: buffer): RunEvent
		local self = _RunEventImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "created", value = messages.ProcessCreated.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "disconnect", value = proto.decodeString(value) }
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "fileChunk", value = messages.FileChunk.decode(value) }
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "complete", value = messages.RunComplete.decode(value) }
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "rpcCall", value = _runtime.ClientRpcCall.decode(value) }
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "executionDone", value = messages.ExecutionDone.decode(value) }
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "executionKilled", value = messages.ExecutionKilled.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _RunEventImpl.jsonEncode(self: RunEvent): any
		local output = {}

		if self.event ~= nil then
			if self.event.type == "created" then
				output.created = self.event.value:jsonEncode()
			elseif self.event.type == "disconnect" then
				output.disconnect = self.event.value
			elseif self.event.type == "fileChunk" then
				output.fileChunk = self.event.value:jsonEncode()
			elseif self.event.type == "complete" then
				output.complete = self.event.value:jsonEncode()
			elseif self.event.type == "rpcCall" then
				output.rpcCall = self.event.value:jsonEncode()
			elseif self.event.type == "executionDone" then
				output.executionDone = self.event.value:jsonEncode()
			elseif self.event.type == "executionKilled" then
				output.executionKilled = self.event.value:jsonEncode()
			end
		end

		return output
	end

	function _RunEventImpl.jsonDecode(input: { [string]: any }): RunEvent
		local self = _RunEventImpl.new()

		if input.created ~= nil then
			self.event = { type = "created", value = messages.ProcessCreated.jsonDecode(input.created) }
		end

		if input.disconnect ~= nil then
			self.event = { type = "disconnect", value = input.disconnect }
		end

		if input.fileChunk ~= nil then
			self.event = { type = "fileChunk", value = messages.FileChunk.jsonDecode(input.fileChunk) }
		end

		if input.complete ~= nil then
			self.event = { type = "complete", value = messages.RunComplete.jsonDecode(input.complete) }
		end

		if input.rpcCall ~= nil then
			self.event = { type = "rpcCall", value = _runtime.ClientRpcCall.jsonDecode(input.rpcCall) }
		end

		if input.executionDone ~= nil then
			self.event = { type = "executionDone", value = messages.ExecutionDone.jsonDecode(input.executionDone) }
		end

		if input.executionKilled ~= nil then
			self.event =
				{ type = "executionKilled", value = messages.ExecutionKilled.jsonDecode(input.executionKilled) }
		end

		return self
	end

	_RunEventImpl.descriptor = {
		name = "RunEvent",
		fullName = "rodeo.RunEvent",
	}

	messages.RunEvent = _RunEventImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.RunEvent)
end

do
	local _RunCompleteImpl = {}
	_RunCompleteImpl.__index = _RunCompleteImpl

	function _RunCompleteImpl.new(data: _RunCompletePartialFields?): RunComplete
		return setmetatable({}, _RunCompleteImpl :: _RunCompleteImpl)
	end

	function _RunCompleteImpl.encode(self: RunComplete): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _RunCompleteImpl.decode(input: buffer): RunComplete
		local self = _RunCompleteImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _RunCompleteImpl.jsonEncode(self: RunComplete): any
		local output = {}

		return output
	end

	function _RunCompleteImpl.jsonDecode(input: { [string]: any }): RunComplete
		local self = _RunCompleteImpl.new()

		return self
	end

	_RunCompleteImpl.descriptor = {
		name = "RunComplete",
		fullName = "rodeo.RunComplete",
	}

	messages.RunComplete = _RunCompleteImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.RunComplete)
end

do
	local _ProcessCreatedImpl = {}
	_ProcessCreatedImpl.__index = _ProcessCreatedImpl

	function _ProcessCreatedImpl.new(data: _ProcessCreatedPartialFields?): ProcessCreated
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
		}, _ProcessCreatedImpl :: _ProcessCreatedImpl)
	end

	function _ProcessCreatedImpl.encode(self: ProcessCreated): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessCreatedImpl.decode(input: buffer): ProcessCreated
		local self = _ProcessCreatedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessCreatedImpl.jsonEncode(self: ProcessCreated): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		return output
	end

	function _ProcessCreatedImpl.jsonDecode(input: { [string]: any }): ProcessCreated
		local self = _ProcessCreatedImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		return self
	end

	_ProcessCreatedImpl.descriptor = {
		name = "ProcessCreated",
		fullName = "rodeo.ProcessCreated",
	}

	messages.ProcessCreated = _ProcessCreatedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessCreated)
end

do
	local _HealthRequestImpl = {}
	_HealthRequestImpl.__index = _HealthRequestImpl

	function _HealthRequestImpl.new(data: _HealthRequestPartialFields?): HealthRequest
		return setmetatable({}, _HealthRequestImpl :: _HealthRequestImpl)
	end

	function _HealthRequestImpl.encode(self: HealthRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HealthRequestImpl.decode(input: buffer): HealthRequest
		local self = _HealthRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HealthRequestImpl.jsonEncode(self: HealthRequest): any
		local output = {}

		return output
	end

	function _HealthRequestImpl.jsonDecode(input: { [string]: any }): HealthRequest
		local self = _HealthRequestImpl.new()

		return self
	end

	_HealthRequestImpl.descriptor = {
		name = "HealthRequest",
		fullName = "rodeo.HealthRequest",
	}

	messages.HealthRequest = _HealthRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HealthRequest)
end

do
	local _GetStateRequestImpl = {}
	_GetStateRequestImpl.__index = _GetStateRequestImpl

	function _GetStateRequestImpl.new(data: _GetStateRequestPartialFields?): GetStateRequest
		return setmetatable({}, _GetStateRequestImpl :: _GetStateRequestImpl)
	end

	function _GetStateRequestImpl.encode(self: GetStateRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetStateRequestImpl.decode(input: buffer): GetStateRequest
		local self = _GetStateRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetStateRequestImpl.jsonEncode(self: GetStateRequest): any
		local output = {}

		return output
	end

	function _GetStateRequestImpl.jsonDecode(input: { [string]: any }): GetStateRequest
		local self = _GetStateRequestImpl.new()

		return self
	end

	_GetStateRequestImpl.descriptor = {
		name = "GetStateRequest",
		fullName = "rodeo.GetStateRequest",
	}

	messages.GetStateRequest = _GetStateRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetStateRequest)
end

do
	local _ListProcessesRequestImpl = {}
	_ListProcessesRequestImpl.__index = _ListProcessesRequestImpl

	function _ListProcessesRequestImpl.new(data: _ListProcessesRequestPartialFields?): ListProcessesRequest
		return setmetatable({}, _ListProcessesRequestImpl :: _ListProcessesRequestImpl)
	end

	function _ListProcessesRequestImpl.encode(self: ListProcessesRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ListProcessesRequestImpl.decode(input: buffer): ListProcessesRequest
		local self = _ListProcessesRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ListProcessesRequestImpl.jsonEncode(self: ListProcessesRequest): any
		local output = {}

		return output
	end

	function _ListProcessesRequestImpl.jsonDecode(input: { [string]: any }): ListProcessesRequest
		local self = _ListProcessesRequestImpl.new()

		return self
	end

	_ListProcessesRequestImpl.descriptor = {
		name = "ListProcessesRequest",
		fullName = "rodeo.ListProcessesRequest",
	}

	messages.ListProcessesRequest = _ListProcessesRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ListProcessesRequest)
end

do
	local _KillProcessRequestImpl = {}
	_KillProcessRequestImpl.__index = _KillProcessRequestImpl

	function _KillProcessRequestImpl.new(data: _KillProcessRequestPartialFields?): KillProcessRequest
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
		}, _KillProcessRequestImpl :: _KillProcessRequestImpl)
	end

	function _KillProcessRequestImpl.encode(self: KillProcessRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _KillProcessRequestImpl.decode(input: buffer): KillProcessRequest
		local self = _KillProcessRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _KillProcessRequestImpl.jsonEncode(self: KillProcessRequest): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		return output
	end

	function _KillProcessRequestImpl.jsonDecode(input: { [string]: any }): KillProcessRequest
		local self = _KillProcessRequestImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		return self
	end

	_KillProcessRequestImpl.descriptor = {
		name = "KillProcessRequest",
		fullName = "rodeo.KillProcessRequest",
	}

	messages.KillProcessRequest = _KillProcessRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.KillProcessRequest)
end

do
	local _LaunchStudioRequestImpl = {}
	_LaunchStudioRequestImpl.__index = _LaunchStudioRequestImpl

	function _LaunchStudioRequestImpl.new(data: _LaunchStudioRequestPartialFields?): LaunchStudioRequest
		return setmetatable({
			backend = if data == nil or data.backend == nil then "" else data.backend,
			placeFile = if data == nil or data.placeFile == nil then nil else data.placeFile,
			placeId = if data == nil or data.placeId == nil then nil else data.placeId,
			fflags = if data == nil or data.fflags == nil then {} else data.fflags,
			background = if data == nil or data.background == nil then false else data.background,
			detached = if data == nil or data.detached == nil then false else data.detached,
			savePath = if data == nil or data.savePath == nil then nil else data.savePath,
			fflagFile = if data == nil or data.fflagFile == nil then nil else data.fflagFile,
			showWidgets = if data == nil or data.showWidgets == nil then "" else data.showWidgets,
			profile = if data == nil or data.profile == nil then false else data.profile,
		}, _LaunchStudioRequestImpl :: _LaunchStudioRequestImpl)
	end

	function _LaunchStudioRequestImpl.encode(self: LaunchStudioRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.backend ~= nil and self.backend ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.backend)
		end

		if self.placeFile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.placeFile)
		end

		if self.placeId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.placeId)
		end

		if self.fflags ~= nil and #self.fflags > 0 then
			for _, value in self.fflags do
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.background then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.background then 1 else 0)
		end

		if self.detached then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.detached then 1 else 0)
		end

		if self.savePath ~= nil then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.savePath)
		end

		if self.fflagFile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.fflagFile)
		end

		if self.showWidgets ~= nil and self.showWidgets ~= "" then
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.showWidgets)
		end

		if self.profile then
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.profile then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _LaunchStudioRequestImpl.decode(input: buffer): LaunchStudioRequest
		local self = _LaunchStudioRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 3 then
					local value
					value, cursor = proto.readVarIntU64(input, cursor)
					self.placeId = value
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.background = value ~= 0
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.detached = value ~= 0
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.profile = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.backend = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.placeFile = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.fflags, proto.decodeString(value))
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.savePath = proto.decodeString(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.fflagFile = proto.decodeString(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.showWidgets = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _LaunchStudioRequestImpl.jsonEncode(self: LaunchStudioRequest): any
		local output = {}

		if self.backend ~= nil and self.backend ~= "" then
			output.backend = self.backend
		end

		if self.placeFile ~= nil then
			output.placeFile = self.placeFile
		end

		if self.placeId ~= nil then
			output.placeId = self.placeId
		end

		if self.fflags ~= nil and #self.fflags > 0 then
			local newOutput = {}
			for _, value in self.fflags do
				table.insert(newOutput, value)
			end
			output.fflags = newOutput
		end

		if self.background then
			output.background = self.background
		end

		if self.detached then
			output.detached = self.detached
		end

		if self.savePath ~= nil then
			output.savePath = self.savePath
		end

		if self.fflagFile ~= nil then
			output.fflagFile = self.fflagFile
		end

		if self.showWidgets ~= nil and self.showWidgets ~= "" then
			output.showWidgets = self.showWidgets
		end

		if self.profile then
			output.profile = self.profile
		end

		return output
	end

	function _LaunchStudioRequestImpl.jsonDecode(input: { [string]: any }): LaunchStudioRequest
		local self = _LaunchStudioRequestImpl.new()

		if input.backend ~= nil then
			self.backend = input.backend
		end

		if input.placeFile ~= nil then
			self.placeFile = input.placeFile
		end

		if input.placeId ~= nil then
			self.placeId = input.placeId
		end

		if input.fflags ~= nil then
			local newOutput: { string } = {}
			for _, value in input.fflags do
				table.insert(newOutput, value)
			end

			self.fflags = newOutput
		end

		if input.background ~= nil then
			self.background = input.background
		end

		if input.detached ~= nil then
			self.detached = input.detached
		end

		if input.savePath ~= nil then
			self.savePath = input.savePath
		end

		if input.fflagFile ~= nil then
			self.fflagFile = input.fflagFile
		end

		if input.showWidgets ~= nil then
			self.showWidgets = input.showWidgets
		end

		if input.profile ~= nil then
			self.profile = input.profile
		end

		return self
	end

	_LaunchStudioRequestImpl.descriptor = {
		name = "LaunchStudioRequest",
		fullName = "rodeo.LaunchStudioRequest",
	}

	messages.LaunchStudioRequest = _LaunchStudioRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.LaunchStudioRequest)
end

do
	local _LaunchStudioEventImpl = {}
	_LaunchStudioEventImpl.__index = _LaunchStudioEventImpl

	function _LaunchStudioEventImpl.new(data: _LaunchStudioEventPartialFields?): LaunchStudioEvent
		return setmetatable({
			event = (function()
				if data == nil or data.event == nil then
					return nil
				end
				local v = data.event
				if
					(v :: any).type == "launching"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StudioLaunching.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "ready"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StudioReady.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "error"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StudioLaunchError.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _LaunchStudioEventImpl :: _LaunchStudioEventImpl)
	end

	function _LaunchStudioEventImpl.encode(self: LaunchStudioEvent): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.event ~= nil then
			if self.event.type == "launching" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "ready" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "error" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _LaunchStudioEventImpl.decode(input: buffer): LaunchStudioEvent
		local self = _LaunchStudioEventImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "launching", value = messages.StudioLaunching.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "ready", value = messages.StudioReady.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "error", value = messages.StudioLaunchError.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _LaunchStudioEventImpl.jsonEncode(self: LaunchStudioEvent): any
		local output = {}

		if self.event ~= nil then
			if self.event.type == "launching" then
				output.launching = self.event.value:jsonEncode()
			elseif self.event.type == "ready" then
				output.ready = self.event.value:jsonEncode()
			elseif self.event.type == "error" then
				output.error = self.event.value:jsonEncode()
			end
		end

		return output
	end

	function _LaunchStudioEventImpl.jsonDecode(input: { [string]: any }): LaunchStudioEvent
		local self = _LaunchStudioEventImpl.new()

		if input.launching ~= nil then
			self.event = { type = "launching", value = messages.StudioLaunching.jsonDecode(input.launching) }
		end

		if input.ready ~= nil then
			self.event = { type = "ready", value = messages.StudioReady.jsonDecode(input.ready) }
		end

		if input.error ~= nil then
			self.event = { type = "error", value = messages.StudioLaunchError.jsonDecode(input.error) }
		end

		return self
	end

	_LaunchStudioEventImpl.descriptor = {
		name = "LaunchStudioEvent",
		fullName = "rodeo.LaunchStudioEvent",
	}

	messages.LaunchStudioEvent = _LaunchStudioEventImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.LaunchStudioEvent)
end

do
	local _StudioLaunchingImpl = {}
	_StudioLaunchingImpl.__index = _StudioLaunchingImpl

	function _StudioLaunchingImpl.new(data: _StudioLaunchingPartialFields?): StudioLaunching
		return setmetatable({}, _StudioLaunchingImpl :: _StudioLaunchingImpl)
	end

	function _StudioLaunchingImpl.encode(self: StudioLaunching): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioLaunchingImpl.decode(input: buffer): StudioLaunching
		local self = _StudioLaunchingImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioLaunchingImpl.jsonEncode(self: StudioLaunching): any
		local output = {}

		return output
	end

	function _StudioLaunchingImpl.jsonDecode(input: { [string]: any }): StudioLaunching
		local self = _StudioLaunchingImpl.new()

		return self
	end

	_StudioLaunchingImpl.descriptor = {
		name = "StudioLaunching",
		fullName = "rodeo.StudioLaunching",
	}

	messages.StudioLaunching = _StudioLaunchingImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioLaunching)
end

do
	local _StudioReadyImpl = {}
	_StudioReadyImpl.__index = _StudioReadyImpl

	function _StudioReadyImpl.new(data: _StudioReadyPartialFields?): StudioReady
		return setmetatable({
			backendId = if data == nil or data.backendId == nil then "" else data.backendId,
			sessionGuid = if data == nil or data.sessionGuid == nil then "" else data.sessionGuid,
		}, _StudioReadyImpl :: _StudioReadyImpl)
	end

	function _StudioReadyImpl.encode(self: StudioReady): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.backendId ~= nil and self.backendId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.backendId)
		end

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioReadyImpl.decode(input: buffer): StudioReady
		local self = _StudioReadyImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.backendId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioReadyImpl.jsonEncode(self: StudioReady): any
		local output = {}

		if self.backendId ~= nil and self.backendId ~= "" then
			output.backendId = self.backendId
		end

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output.sessionGuid = self.sessionGuid
		end

		return output
	end

	function _StudioReadyImpl.jsonDecode(input: { [string]: any }): StudioReady
		local self = _StudioReadyImpl.new()

		if input.backendId ~= nil then
			self.backendId = input.backendId
		end

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		return self
	end

	_StudioReadyImpl.descriptor = {
		name = "StudioReady",
		fullName = "rodeo.StudioReady",
	}

	messages.StudioReady = _StudioReadyImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioReady)
end

do
	local _StudioLaunchErrorImpl = {}
	_StudioLaunchErrorImpl.__index = _StudioLaunchErrorImpl

	function _StudioLaunchErrorImpl.new(data: _StudioLaunchErrorPartialFields?): StudioLaunchError
		return setmetatable({
			message = if data == nil or data.message == nil then "" else data.message,
		}, _StudioLaunchErrorImpl :: _StudioLaunchErrorImpl)
	end

	function _StudioLaunchErrorImpl.encode(self: StudioLaunchError): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.message ~= nil and self.message ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.message)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioLaunchErrorImpl.decode(input: buffer): StudioLaunchError
		local self = _StudioLaunchErrorImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.message = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioLaunchErrorImpl.jsonEncode(self: StudioLaunchError): any
		local output = {}

		if self.message ~= nil and self.message ~= "" then
			output.message = self.message
		end

		return output
	end

	function _StudioLaunchErrorImpl.jsonDecode(input: { [string]: any }): StudioLaunchError
		local self = _StudioLaunchErrorImpl.new()

		if input.message ~= nil then
			self.message = input.message
		end

		return self
	end

	_StudioLaunchErrorImpl.descriptor = {
		name = "StudioLaunchError",
		fullName = "rodeo.StudioLaunchError",
	}

	messages.StudioLaunchError = _StudioLaunchErrorImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioLaunchError)
end

do
	local _SetStudioModeRequestImpl = {}
	_SetStudioModeRequestImpl.__index = _SetStudioModeRequestImpl

	function _SetStudioModeRequestImpl.new(data: _SetStudioModeRequestPartialFields?): SetStudioModeRequest
		return setmetatable({
			sessionGuid = if data == nil or data.sessionGuid == nil then "" else data.sessionGuid,
			mode = if data == nil or data.mode == nil then "" else data.mode,
		}, _SetStudioModeRequestImpl :: _SetStudioModeRequestImpl)
	end

	function _SetStudioModeRequestImpl.encode(self: SetStudioModeRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		if self.mode ~= nil and self.mode ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mode)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SetStudioModeRequestImpl.decode(input: buffer): SetStudioModeRequest
		local self = _SetStudioModeRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mode = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SetStudioModeRequestImpl.jsonEncode(self: SetStudioModeRequest): any
		local output = {}

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output.sessionGuid = self.sessionGuid
		end

		if self.mode ~= nil and self.mode ~= "" then
			output.mode = self.mode
		end

		return output
	end

	function _SetStudioModeRequestImpl.jsonDecode(input: { [string]: any }): SetStudioModeRequest
		local self = _SetStudioModeRequestImpl.new()

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		if input.mode ~= nil then
			self.mode = input.mode
		end

		return self
	end

	_SetStudioModeRequestImpl.descriptor = {
		name = "SetStudioModeRequest",
		fullName = "rodeo.SetStudioModeRequest",
	}

	messages.SetStudioModeRequest = _SetStudioModeRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SetStudioModeRequest)
end

do
	local _SetStudioModeResponseImpl = {}
	_SetStudioModeResponseImpl.__index = _SetStudioModeResponseImpl

	function _SetStudioModeResponseImpl.new(data: _SetStudioModeResponsePartialFields?): SetStudioModeResponse
		return setmetatable({
			ok = if data == nil or data.ok == nil then false else data.ok,
			mode = if data == nil or data.mode == nil then "" else data.mode,
		}, _SetStudioModeResponseImpl :: _SetStudioModeResponseImpl)
	end

	function _SetStudioModeResponseImpl.encode(self: SetStudioModeResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.ok then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.ok then 1 else 0)
		end

		if self.mode ~= nil and self.mode ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mode)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SetStudioModeResponseImpl.decode(input: buffer): SetStudioModeResponse
		local self = _SetStudioModeResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.ok = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mode = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SetStudioModeResponseImpl.jsonEncode(self: SetStudioModeResponse): any
		local output = {}

		if self.ok then
			output.ok = self.ok
		end

		if self.mode ~= nil and self.mode ~= "" then
			output.mode = self.mode
		end

		return output
	end

	function _SetStudioModeResponseImpl.jsonDecode(input: { [string]: any }): SetStudioModeResponse
		local self = _SetStudioModeResponseImpl.new()

		if input.ok ~= nil then
			self.ok = input.ok
		end

		if input.mode ~= nil then
			self.mode = input.mode
		end

		return self
	end

	_SetStudioModeResponseImpl.descriptor = {
		name = "SetStudioModeResponse",
		fullName = "rodeo.SetStudioModeResponse",
	}

	messages.SetStudioModeResponse = _SetStudioModeResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SetStudioModeResponse)
end

do
	local _CloseStudioRequestImpl = {}
	_CloseStudioRequestImpl.__index = _CloseStudioRequestImpl

	function _CloseStudioRequestImpl.new(data: _CloseStudioRequestPartialFields?): CloseStudioRequest
		return setmetatable({
			sessionGuid = if data == nil or data.sessionGuid == nil then "" else data.sessionGuid,
		}, _CloseStudioRequestImpl :: _CloseStudioRequestImpl)
	end

	function _CloseStudioRequestImpl.encode(self: CloseStudioRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CloseStudioRequestImpl.decode(input: buffer): CloseStudioRequest
		local self = _CloseStudioRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CloseStudioRequestImpl.jsonEncode(self: CloseStudioRequest): any
		local output = {}

		if self.sessionGuid ~= nil and self.sessionGuid ~= "" then
			output.sessionGuid = self.sessionGuid
		end

		return output
	end

	function _CloseStudioRequestImpl.jsonDecode(input: { [string]: any }): CloseStudioRequest
		local self = _CloseStudioRequestImpl.new()

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		return self
	end

	_CloseStudioRequestImpl.descriptor = {
		name = "CloseStudioRequest",
		fullName = "rodeo.CloseStudioRequest",
	}

	messages.CloseStudioRequest = _CloseStudioRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CloseStudioRequest)
end

do
	local _CloseStudioEventImpl = {}
	_CloseStudioEventImpl.__index = _CloseStudioEventImpl

	function _CloseStudioEventImpl.new(data: _CloseStudioEventPartialFields?): CloseStudioEvent
		return setmetatable({
			event = (function()
				if data == nil or data.event == nil then
					return nil
				end
				local v = data.event
				if
					(v :: any).type == "closing"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StudioClosing.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "closed"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StudioClosed.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _CloseStudioEventImpl :: _CloseStudioEventImpl)
	end

	function _CloseStudioEventImpl.encode(self: CloseStudioEvent): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.event ~= nil then
			if self.event.type == "closing" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "closed" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CloseStudioEventImpl.decode(input: buffer): CloseStudioEvent
		local self = _CloseStudioEventImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "closing", value = messages.StudioClosing.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "closed", value = messages.StudioClosed.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CloseStudioEventImpl.jsonEncode(self: CloseStudioEvent): any
		local output = {}

		if self.event ~= nil then
			if self.event.type == "closing" then
				output.closing = self.event.value:jsonEncode()
			elseif self.event.type == "closed" then
				output.closed = self.event.value:jsonEncode()
			end
		end

		return output
	end

	function _CloseStudioEventImpl.jsonDecode(input: { [string]: any }): CloseStudioEvent
		local self = _CloseStudioEventImpl.new()

		if input.closing ~= nil then
			self.event = { type = "closing", value = messages.StudioClosing.jsonDecode(input.closing) }
		end

		if input.closed ~= nil then
			self.event = { type = "closed", value = messages.StudioClosed.jsonDecode(input.closed) }
		end

		return self
	end

	_CloseStudioEventImpl.descriptor = {
		name = "CloseStudioEvent",
		fullName = "rodeo.CloseStudioEvent",
	}

	messages.CloseStudioEvent = _CloseStudioEventImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CloseStudioEvent)
end

do
	local _StudioClosingImpl = {}
	_StudioClosingImpl.__index = _StudioClosingImpl

	function _StudioClosingImpl.new(data: _StudioClosingPartialFields?): StudioClosing
		return setmetatable({}, _StudioClosingImpl :: _StudioClosingImpl)
	end

	function _StudioClosingImpl.encode(self: StudioClosing): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioClosingImpl.decode(input: buffer): StudioClosing
		local self = _StudioClosingImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioClosingImpl.jsonEncode(self: StudioClosing): any
		local output = {}

		return output
	end

	function _StudioClosingImpl.jsonDecode(input: { [string]: any }): StudioClosing
		local self = _StudioClosingImpl.new()

		return self
	end

	_StudioClosingImpl.descriptor = {
		name = "StudioClosing",
		fullName = "rodeo.StudioClosing",
	}

	messages.StudioClosing = _StudioClosingImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioClosing)
end

do
	local _StudioClosedImpl = {}
	_StudioClosedImpl.__index = _StudioClosedImpl

	function _StudioClosedImpl.new(data: _StudioClosedPartialFields?): StudioClosed
		return setmetatable({}, _StudioClosedImpl :: _StudioClosedImpl)
	end

	function _StudioClosedImpl.encode(self: StudioClosed): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioClosedImpl.decode(input: buffer): StudioClosed
		local self = _StudioClosedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioClosedImpl.jsonEncode(self: StudioClosed): any
		local output = {}

		return output
	end

	function _StudioClosedImpl.jsonDecode(input: { [string]: any }): StudioClosed
		local self = _StudioClosedImpl.new()

		return self
	end

	_StudioClosedImpl.descriptor = {
		name = "StudioClosed",
		fullName = "rodeo.StudioClosed",
	}

	messages.StudioClosed = _StudioClosedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioClosed)
end

do
	local _SavePlaceRequestImpl = {}
	_SavePlaceRequestImpl.__index = _SavePlaceRequestImpl

	function _SavePlaceRequestImpl.new(data: _SavePlaceRequestPartialFields?): SavePlaceRequest
		return setmetatable({
			backend = if data == nil or data.backend == nil then nil else data.backend,
			sessionGuid = if data == nil or data.sessionGuid == nil then nil else data.sessionGuid,
		}, _SavePlaceRequestImpl :: _SavePlaceRequestImpl)
	end

	function _SavePlaceRequestImpl.encode(self: SavePlaceRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.backend ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.backend)
		end

		if self.sessionGuid ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SavePlaceRequestImpl.decode(input: buffer): SavePlaceRequest
		local self = _SavePlaceRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.backend = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SavePlaceRequestImpl.jsonEncode(self: SavePlaceRequest): any
		local output = {}

		if self.backend ~= nil then
			output.backend = self.backend
		end

		if self.sessionGuid ~= nil then
			output.sessionGuid = self.sessionGuid
		end

		return output
	end

	function _SavePlaceRequestImpl.jsonDecode(input: { [string]: any }): SavePlaceRequest
		local self = _SavePlaceRequestImpl.new()

		if input.backend ~= nil then
			self.backend = input.backend
		end

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		return self
	end

	_SavePlaceRequestImpl.descriptor = {
		name = "SavePlaceRequest",
		fullName = "rodeo.SavePlaceRequest",
	}

	messages.SavePlaceRequest = _SavePlaceRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SavePlaceRequest)
end

do
	local _SavePlaceResponseImpl = {}
	_SavePlaceResponseImpl.__index = _SavePlaceResponseImpl

	function _SavePlaceResponseImpl.new(data: _SavePlaceResponsePartialFields?): SavePlaceResponse
		return setmetatable({
			saved = if data == nil or data.saved == nil then false else data.saved,
			path = if data == nil or data.path == nil then nil else data.path,
			error = if data == nil or data.error == nil then nil else data.error,
		}, _SavePlaceResponseImpl :: _SavePlaceResponseImpl)
	end

	function _SavePlaceResponseImpl.encode(self: SavePlaceResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.saved then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.saved then 1 else 0)
		end

		if self.path ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.path)
		end

		if self.error ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.error)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SavePlaceResponseImpl.decode(input: buffer): SavePlaceResponse
		local self = _SavePlaceResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.saved = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.path = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.error = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SavePlaceResponseImpl.jsonEncode(self: SavePlaceResponse): any
		local output = {}

		if self.saved then
			output.saved = self.saved
		end

		if self.path ~= nil then
			output.path = self.path
		end

		if self.error ~= nil then
			output.error = self.error
		end

		return output
	end

	function _SavePlaceResponseImpl.jsonDecode(input: { [string]: any }): SavePlaceResponse
		local self = _SavePlaceResponseImpl.new()

		if input.saved ~= nil then
			self.saved = input.saved
		end

		if input.path ~= nil then
			self.path = input.path
		end

		if input.error ~= nil then
			self.error = input.error
		end

		return self
	end

	_SavePlaceResponseImpl.descriptor = {
		name = "SavePlaceResponse",
		fullName = "rodeo.SavePlaceResponse",
	}

	messages.SavePlaceResponse = _SavePlaceResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SavePlaceResponse)
end

do
	local _ListBackendsRequestImpl = {}
	_ListBackendsRequestImpl.__index = _ListBackendsRequestImpl

	function _ListBackendsRequestImpl.new(data: _ListBackendsRequestPartialFields?): ListBackendsRequest
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ListBackendsRequestImpl :: _ListBackendsRequestImpl)
	end

	function _ListBackendsRequestImpl.encode(self: ListBackendsRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.kind)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ListBackendsRequestImpl.decode(input: buffer): ListBackendsRequest
		local self = _ListBackendsRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ListBackendsRequestImpl.jsonEncode(self: ListBackendsRequest): any
		local output = {}

		if self.kind ~= nil then
			output.kind = self.kind
		end

		return output
	end

	function _ListBackendsRequestImpl.jsonDecode(input: { [string]: any }): ListBackendsRequest
		local self = _ListBackendsRequestImpl.new()

		if input.kind ~= nil then
			self.kind = input.kind
		end

		return self
	end

	_ListBackendsRequestImpl.descriptor = {
		name = "ListBackendsRequest",
		fullName = "rodeo.ListBackendsRequest",
	}

	messages.ListBackendsRequest = _ListBackendsRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ListBackendsRequest)
end

do
	local _ListBackendsResponseImpl = {}
	_ListBackendsResponseImpl.__index = _ListBackendsResponseImpl

	function _ListBackendsResponseImpl.new(data: _ListBackendsResponsePartialFields?): ListBackendsResponse
		return setmetatable({
			backends = (function()
				local src = if data == nil then nil else data.backends
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.BackendInfo.new(v :: any)
						else (v :: any) :: BackendInfo
				end
				return out
			end)(),
		}, _ListBackendsResponseImpl :: _ListBackendsResponseImpl)
	end

	function _ListBackendsResponseImpl.encode(self: ListBackendsResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.backends ~= nil and #self.backends > 0 then
			for _, value in self.backends do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ListBackendsResponseImpl.decode(input: buffer): ListBackendsResponse
		local self = _ListBackendsResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.backends, messages.BackendInfo.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ListBackendsResponseImpl.jsonEncode(self: ListBackendsResponse): any
		local output = {}

		if self.backends ~= nil and #self.backends > 0 then
			local newOutput = {}
			for _, value in self.backends do
				table.insert(newOutput, value:jsonEncode())
			end
			output.backends = newOutput
		end

		return output
	end

	function _ListBackendsResponseImpl.jsonDecode(input: { [string]: any }): ListBackendsResponse
		local self = _ListBackendsResponseImpl.new()

		if input.backends ~= nil then
			local newOutput: { BackendInfo } = {}
			for _, value in input.backends do
				table.insert(newOutput, messages.BackendInfo.jsonDecode(value))
			end

			self.backends = newOutput
		end

		return self
	end

	_ListBackendsResponseImpl.descriptor = {
		name = "ListBackendsResponse",
		fullName = "rodeo.ListBackendsResponse",
	}

	messages.ListBackendsResponse = _ListBackendsResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ListBackendsResponse)
end

do
	local _BackendInfoImpl = {}
	_BackendInfoImpl.__index = _BackendInfoImpl

	function _BackendInfoImpl.new(data: _BackendInfoPartialFields?): BackendInfo
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			kind = if data == nil or data.kind == nil then "" else data.kind,
			name = if data == nil or data.name == nil then "" else data.name,
		}, _BackendInfoImpl :: _BackendInfoImpl)
	end

	function _BackendInfoImpl.encode(self: BackendInfo): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.kind ~= nil and self.kind ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.kind)
		end

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BackendInfoImpl.decode(input: buffer): BackendInfo
		local self = _BackendInfoImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BackendInfoImpl.jsonEncode(self: BackendInfo): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.kind ~= nil and self.kind ~= "" then
			output.kind = self.kind
		end

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		return output
	end

	function _BackendInfoImpl.jsonDecode(input: { [string]: any }): BackendInfo
		local self = _BackendInfoImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.kind ~= nil then
			self.kind = input.kind
		end

		if input.name ~= nil then
			self.name = input.name
		end

		return self
	end

	_BackendInfoImpl.descriptor = {
		name = "BackendInfo",
		fullName = "rodeo.BackendInfo",
	}

	messages.BackendInfo = _BackendInfoImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BackendInfo)
end

do
	local _RodeoSnapshotImpl = {}
	_RodeoSnapshotImpl.__index = _RodeoSnapshotImpl

	function _RodeoSnapshotImpl.new(data: _RodeoSnapshotPartialFields?): RodeoSnapshot
		return setmetatable({
			backends = (function()
				local src = if data == nil then nil else data.backends
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.BackendInfo.new(v :: any)
						else (v :: any) :: BackendInfo
				end
				return out
			end)(),
			processes = (function()
				local src = if data == nil then nil else data.processes
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.ProcessInfo.new(v :: any)
						else (v :: any) :: ProcessInfo
				end
				return out
			end)(),
			studios = (function()
				local src = if data == nil then nil else data.studios
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.StudioState.new(v :: any)
						else (v :: any) :: StudioState
				end
				return out
			end)(),
		}, _RodeoSnapshotImpl :: _RodeoSnapshotImpl)
	end

	function _RodeoSnapshotImpl.encode(self: RodeoSnapshot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.backends ~= nil and #self.backends > 0 then
			for _, value in self.backends do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.processes ~= nil and #self.processes > 0 then
			for _, value in self.processes do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.studios ~= nil and #self.studios > 0 then
			for _, value in self.studios do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _RodeoSnapshotImpl.decode(input: buffer): RodeoSnapshot
		local self = _RodeoSnapshotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.backends, messages.BackendInfo.decode(value))
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.processes, messages.ProcessInfo.decode(value))
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.studios, messages.StudioState.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _RodeoSnapshotImpl.jsonEncode(self: RodeoSnapshot): any
		local output = {}

		if self.backends ~= nil and #self.backends > 0 then
			local newOutput = {}
			for _, value in self.backends do
				table.insert(newOutput, value:jsonEncode())
			end
			output.backends = newOutput
		end

		if self.processes ~= nil and #self.processes > 0 then
			local newOutput = {}
			for _, value in self.processes do
				table.insert(newOutput, value:jsonEncode())
			end
			output.processes = newOutput
		end

		if self.studios ~= nil and #self.studios > 0 then
			local newOutput = {}
			for _, value in self.studios do
				table.insert(newOutput, value:jsonEncode())
			end
			output.studios = newOutput
		end

		return output
	end

	function _RodeoSnapshotImpl.jsonDecode(input: { [string]: any }): RodeoSnapshot
		local self = _RodeoSnapshotImpl.new()

		if input.backends ~= nil then
			local newOutput: { BackendInfo } = {}
			for _, value in input.backends do
				table.insert(newOutput, messages.BackendInfo.jsonDecode(value))
			end

			self.backends = newOutput
		end

		if input.processes ~= nil then
			local newOutput: { ProcessInfo } = {}
			for _, value in input.processes do
				table.insert(newOutput, messages.ProcessInfo.jsonDecode(value))
			end

			self.processes = newOutput
		end

		if input.studios ~= nil then
			local newOutput: { StudioState } = {}
			for _, value in input.studios do
				table.insert(newOutput, messages.StudioState.jsonDecode(value))
			end

			self.studios = newOutput
		end

		return self
	end

	_RodeoSnapshotImpl.descriptor = {
		name = "RodeoSnapshot",
		fullName = "rodeo.RodeoSnapshot",
	}

	messages.RodeoSnapshot = _RodeoSnapshotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.RodeoSnapshot)
end

do
	local _LogFilterImpl = {}
	_LogFilterImpl.__index = _LogFilterImpl

	function _LogFilterImpl.new(data: _LogFilterPartialFields?): LogFilter
		return setmetatable({
			enableWarn = if data == nil or data.enableWarn == nil then false else data.enableWarn,
			enableError = if data == nil or data.enableError == nil then false else data.enableError,
			enableInfo = if data == nil or data.enableInfo == nil then false else data.enableInfo,
			enableOutput = if data == nil or data.enableOutput == nil then false else data.enableOutput,
			enableLogs = if data == nil or data.enableLogs == nil then false else data.enableLogs,
		}, _LogFilterImpl :: _LogFilterImpl)
	end

	function _LogFilterImpl.encode(self: LogFilter): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.enableWarn then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.enableWarn then 1 else 0)
		end

		if self.enableError then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.enableError then 1 else 0)
		end

		if self.enableInfo then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.enableInfo then 1 else 0)
		end

		if self.enableOutput then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.enableOutput then 1 else 0)
		end

		if self.enableLogs then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.enableLogs then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _LogFilterImpl.decode(input: buffer): LogFilter
		local self = _LogFilterImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.enableWarn = value ~= 0
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.enableError = value ~= 0
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.enableInfo = value ~= 0
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.enableOutput = value ~= 0
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.enableLogs = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _LogFilterImpl.jsonEncode(self: LogFilter): any
		local output = {}

		if self.enableWarn then
			output.enableWarn = self.enableWarn
		end

		if self.enableError then
			output.enableError = self.enableError
		end

		if self.enableInfo then
			output.enableInfo = self.enableInfo
		end

		if self.enableOutput then
			output.enableOutput = self.enableOutput
		end

		if self.enableLogs then
			output.enableLogs = self.enableLogs
		end

		return output
	end

	function _LogFilterImpl.jsonDecode(input: { [string]: any }): LogFilter
		local self = _LogFilterImpl.new()

		if input.enableWarn ~= nil then
			self.enableWarn = input.enableWarn
		end

		if input.enableError ~= nil then
			self.enableError = input.enableError
		end

		if input.enableInfo ~= nil then
			self.enableInfo = input.enableInfo
		end

		if input.enableOutput ~= nil then
			self.enableOutput = input.enableOutput
		end

		if input.enableLogs ~= nil then
			self.enableLogs = input.enableLogs
		end

		return self
	end

	_LogFilterImpl.descriptor = {
		name = "LogFilter",
		fullName = "rodeo.LogFilter",
	}

	messages.LogFilter = _LogFilterImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.LogFilter)
end

do
	local _HealthResponseImpl = {}
	_HealthResponseImpl.__index = _HealthResponseImpl

	function _HealthResponseImpl.new(data: _HealthResponsePartialFields?): HealthResponse
		return setmetatable({
			launched = if data == nil or data.launched == nil then false else data.launched,
			contextCount = if data == nil or data.contextCount == nil then 0 else data.contextCount,
			totalDoms = if data == nil or data.totalDoms == nil then 0 else data.totalDoms,
			totalQueued = if data == nil or data.totalQueued == nil then 0 else data.totalQueued,
			contexts = (function()
				local src = if data == nil then nil else data.contexts
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.ContextInfo.new(v :: any)
						else (v :: any) :: ContextInfo
				end
				return out
			end)(),
		}, _HealthResponseImpl :: _HealthResponseImpl)
	end

	function _HealthResponseImpl.encode(self: HealthResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.launched then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.launched then 1 else 0)
		end

		if self.contextCount ~= nil and self.contextCount ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.contextCount)
		end

		if self.totalDoms ~= nil and self.totalDoms ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.totalDoms)
		end

		if self.totalQueued ~= nil and self.totalQueued ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.totalQueued)
		end

		if self.contexts ~= nil and #self.contexts > 0 then
			for _, value in self.contexts do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HealthResponseImpl.decode(input: buffer): HealthResponse
		local self = _HealthResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.launched = value ~= 0
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.contextCount = value
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.totalDoms = value
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.totalQueued = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.contexts, messages.ContextInfo.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HealthResponseImpl.jsonEncode(self: HealthResponse): any
		local output = {}

		if self.launched then
			output.launched = self.launched
		end

		if self.contextCount ~= nil and self.contextCount ~= 0 then
			output.contextCount = self.contextCount
		end

		if self.totalDoms ~= nil and self.totalDoms ~= 0 then
			output.totalDoms = self.totalDoms
		end

		if self.totalQueued ~= nil and self.totalQueued ~= 0 then
			output.totalQueued = self.totalQueued
		end

		if self.contexts ~= nil and #self.contexts > 0 then
			local newOutput = {}
			for _, value in self.contexts do
				table.insert(newOutput, value:jsonEncode())
			end
			output.contexts = newOutput
		end

		return output
	end

	function _HealthResponseImpl.jsonDecode(input: { [string]: any }): HealthResponse
		local self = _HealthResponseImpl.new()

		if input.launched ~= nil then
			self.launched = input.launched
		end

		if input.contextCount ~= nil then
			self.contextCount = input.contextCount
		end

		if input.totalDoms ~= nil then
			self.totalDoms = input.totalDoms
		end

		if input.totalQueued ~= nil then
			self.totalQueued = input.totalQueued
		end

		if input.contexts ~= nil then
			local newOutput: { ContextInfo } = {}
			for _, value in input.contexts do
				table.insert(newOutput, messages.ContextInfo.jsonDecode(value))
			end

			self.contexts = newOutput
		end

		return self
	end

	_HealthResponseImpl.descriptor = {
		name = "HealthResponse",
		fullName = "rodeo.HealthResponse",
	}

	messages.HealthResponse = _HealthResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HealthResponse)
end

do
	local _ContextInfoImpl = {}
	_ContextInfoImpl.__index = _ContextInfoImpl

	function _ContextInfoImpl.new(data: _ContextInfoPartialFields?): ContextInfo
		return setmetatable({
			bitset = if data == nil or data.bitset == nil then 0 else data.bitset,
			domCount = if data == nil or data.domCount == nil then 0 else data.domCount,
			totalQueued = if data == nil or data.totalQueued == nil then 0 else data.totalQueued,
			doms = (function()
				local src = if data == nil then nil else data.doms
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.DomInfo.new(v :: any)
						else (v :: any) :: DomInfo
				end
				return out
			end)(),
		}, _ContextInfoImpl :: _ContextInfoImpl)
	end

	function _ContextInfoImpl.encode(self: ContextInfo): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.bitset ~= nil and self.bitset ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.bitset)
		end

		if self.domCount ~= nil and self.domCount ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.domCount)
		end

		if self.totalQueued ~= nil and self.totalQueued ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.totalQueued)
		end

		if self.doms ~= nil and #self.doms > 0 then
			for _, value in self.doms do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ContextInfoImpl.decode(input: buffer): ContextInfo
		local self = _ContextInfoImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.bitset = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.domCount = value
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.totalQueued = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.doms, messages.DomInfo.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ContextInfoImpl.jsonEncode(self: ContextInfo): any
		local output = {}

		if self.bitset ~= nil and self.bitset ~= 0 then
			output.bitset = self.bitset
		end

		if self.domCount ~= nil and self.domCount ~= 0 then
			output.domCount = self.domCount
		end

		if self.totalQueued ~= nil and self.totalQueued ~= 0 then
			output.totalQueued = self.totalQueued
		end

		if self.doms ~= nil and #self.doms > 0 then
			local newOutput = {}
			for _, value in self.doms do
				table.insert(newOutput, value:jsonEncode())
			end
			output.doms = newOutput
		end

		return output
	end

	function _ContextInfoImpl.jsonDecode(input: { [string]: any }): ContextInfo
		local self = _ContextInfoImpl.new()

		if input.bitset ~= nil then
			self.bitset = input.bitset
		end

		if input.domCount ~= nil then
			self.domCount = input.domCount
		end

		if input.totalQueued ~= nil then
			self.totalQueued = input.totalQueued
		end

		if input.doms ~= nil then
			local newOutput: { DomInfo } = {}
			for _, value in input.doms do
				table.insert(newOutput, messages.DomInfo.jsonDecode(value))
			end

			self.doms = newOutput
		end

		return self
	end

	_ContextInfoImpl.descriptor = {
		name = "ContextInfo",
		fullName = "rodeo.ContextInfo",
	}

	messages.ContextInfo = _ContextInfoImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ContextInfo)
end

do
	local _DomInfoImpl = {}
	_DomInfoImpl.__index = _DomInfoImpl

	function _DomInfoImpl.new(data: _DomInfoPartialFields?): DomInfo
		return setmetatable({
			rodeoId = if data == nil or data.rodeoId == nil then "" else data.rodeoId,
			activeCount = if data == nil or data.activeCount == nil then 0 else data.activeCount,
			isIdle = if data == nil or data.isIdle == nil then false else data.isIdle,
		}, _DomInfoImpl :: _DomInfoImpl)
	end

	function _DomInfoImpl.encode(self: DomInfo): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.rodeoId ~= nil and self.rodeoId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.rodeoId)
		end

		if self.activeCount ~= nil and self.activeCount ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.activeCount)
		end

		if self.isIdle then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.isIdle then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DomInfoImpl.decode(input: buffer): DomInfo
		local self = _DomInfoImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntU32(input, cursor)
					self.activeCount = value
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.isIdle = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.rodeoId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DomInfoImpl.jsonEncode(self: DomInfo): any
		local output = {}

		if self.rodeoId ~= nil and self.rodeoId ~= "" then
			output.rodeoId = self.rodeoId
		end

		if self.activeCount ~= nil and self.activeCount ~= 0 then
			output.activeCount = self.activeCount
		end

		if self.isIdle then
			output.isIdle = self.isIdle
		end

		return output
	end

	function _DomInfoImpl.jsonDecode(input: { [string]: any }): DomInfo
		local self = _DomInfoImpl.new()

		if input.rodeoId ~= nil then
			self.rodeoId = input.rodeoId
		end

		if input.activeCount ~= nil then
			self.activeCount = input.activeCount
		end

		if input.isIdle ~= nil then
			self.isIdle = input.isIdle
		end

		return self
	end

	_DomInfoImpl.descriptor = {
		name = "DomInfo",
		fullName = "rodeo.DomInfo",
	}

	messages.DomInfo = _DomInfoImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DomInfo)
end

do
	local _ProcessInfoImpl = {}
	_ProcessInfoImpl.__index = _ProcessInfoImpl

	function _ProcessInfoImpl.new(data: _ProcessInfoPartialFields?): ProcessInfo
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
			state = if data == nil or data.state == nil then "" else data.state,
			mode = if data == nil or data.mode == nil then "" else data.mode,
			context = if data == nil or data.context == nil then "" else data.context,
			domKind = if data == nil or data.domKind == nil then "" else data.domKind,
			studioId = if data == nil or data.studioId == nil then nil else data.studioId,
			sessionId = if data == nil or data.sessionId == nil then nil else data.sessionId,
			domId = if data == nil or data.domId == nil then nil else data.domId,
			createdAt = if data == nil or data.createdAt == nil then 0 else data.createdAt,
		}, _ProcessInfoImpl :: _ProcessInfoImpl)
	end

	function _ProcessInfoImpl.encode(self: ProcessInfo): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		if self.state ~= nil and self.state ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.state)
		end

		if self.mode ~= nil and self.mode ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mode)
		end

		if self.context ~= nil and self.context ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.context)
		end

		if self.domKind ~= nil and self.domKind ~= "" then
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domKind)
		end

		if self.studioId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.studioId)
		end

		if self.sessionId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionId)
		end

		if self.domId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domId)
		end

		if self.createdAt ~= nil and self.createdAt ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.i64)
			output, cursor = proto.writeDouble(output, cursor, self.createdAt)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessInfoImpl.decode(input: buffer): ProcessInfo
		local self = _ProcessInfoImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.state = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mode = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.context = proto.decodeString(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.studioId = proto.decodeString(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionId = proto.decodeString(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domId = proto.decodeString(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domKind = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				if field == 8 then
					local value
					value, cursor = proto.readDouble(input, cursor)
					self.createdAt = value
					continue
				end

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessInfoImpl.jsonEncode(self: ProcessInfo): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		if self.state ~= nil and self.state ~= "" then
			output.state = self.state
		end

		if self.mode ~= nil and self.mode ~= "" then
			output.mode = self.mode
		end

		if self.context ~= nil and self.context ~= "" then
			output.context = self.context
		end

		if self.domKind ~= nil and self.domKind ~= "" then
			output.domKind = self.domKind
		end

		if self.studioId ~= nil then
			output.studioId = self.studioId
		end

		if self.sessionId ~= nil then
			output.sessionId = self.sessionId
		end

		if self.domId ~= nil then
			output.domId = self.domId
		end

		if self.createdAt ~= nil and self.createdAt ~= 0 then
			output.createdAt = proto.json.serializeNumber(self.createdAt)
		end

		return output
	end

	function _ProcessInfoImpl.jsonDecode(input: { [string]: any }): ProcessInfo
		local self = _ProcessInfoImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		if input.state ~= nil then
			self.state = input.state
		end

		if input.mode ~= nil then
			self.mode = input.mode
		end

		if input.context ~= nil then
			self.context = input.context
		end

		if input.domKind ~= nil then
			self.domKind = input.domKind
		end

		if input.studioId ~= nil then
			self.studioId = input.studioId
		end

		if input.sessionId ~= nil then
			self.sessionId = input.sessionId
		end

		if input.domId ~= nil then
			self.domId = input.domId
		end

		if input.createdAt ~= nil then
			self.createdAt = proto.json.deserializeNumber(input.createdAt)
		end

		return self
	end

	_ProcessInfoImpl.descriptor = {
		name = "ProcessInfo",
		fullName = "rodeo.ProcessInfo",
	}

	messages.ProcessInfo = _ProcessInfoImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessInfo)
end

do
	local _ProcessListResponseImpl = {}
	_ProcessListResponseImpl.__index = _ProcessListResponseImpl

	function _ProcessListResponseImpl.new(data: _ProcessListResponsePartialFields?): ProcessListResponse
		return setmetatable({
			processes = (function()
				local src = if data == nil then nil else data.processes
				if src == nil then
					return {}
				end
				local out = table.create(#src)
				for i, v in src do
					out[i] = if getmetatable(v :: any) == nil
						then messages.ProcessInfo.new(v :: any)
						else (v :: any) :: ProcessInfo
				end
				return out
			end)(),
		}, _ProcessListResponseImpl :: _ProcessListResponseImpl)
	end

	function _ProcessListResponseImpl.encode(self: ProcessListResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.processes ~= nil and #self.processes > 0 then
			for _, value in self.processes do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProcessListResponseImpl.decode(input: buffer): ProcessListResponse
		local self = _ProcessListResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.processes, messages.ProcessInfo.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProcessListResponseImpl.jsonEncode(self: ProcessListResponse): any
		local output = {}

		if self.processes ~= nil and #self.processes > 0 then
			local newOutput = {}
			for _, value in self.processes do
				table.insert(newOutput, value:jsonEncode())
			end
			output.processes = newOutput
		end

		return output
	end

	function _ProcessListResponseImpl.jsonDecode(input: { [string]: any }): ProcessListResponse
		local self = _ProcessListResponseImpl.new()

		if input.processes ~= nil then
			local newOutput: { ProcessInfo } = {}
			for _, value in input.processes do
				table.insert(newOutput, messages.ProcessInfo.jsonDecode(value))
			end

			self.processes = newOutput
		end

		return self
	end

	_ProcessListResponseImpl.descriptor = {
		name = "ProcessListResponse",
		fullName = "rodeo.ProcessListResponse",
	}

	messages.ProcessListResponse = _ProcessListResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProcessListResponse)
end

do
	local _KillResponseImpl = {}
	_KillResponseImpl.__index = _KillResponseImpl

	function _KillResponseImpl.new(data: _KillResponsePartialFields?): KillResponse
		return setmetatable({
			killed = if data == nil or data.killed == nil then false else data.killed,
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
		}, _KillResponseImpl :: _KillResponseImpl)
	end

	function _KillResponseImpl.encode(self: KillResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.killed then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.killed then 1 else 0)
		end

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _KillResponseImpl.decode(input: buffer): KillResponse
		local self = _KillResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.killed = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _KillResponseImpl.jsonEncode(self: KillResponse): any
		local output = {}

		if self.killed then
			output.killed = self.killed
		end

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		return output
	end

	function _KillResponseImpl.jsonDecode(input: { [string]: any }): KillResponse
		local self = _KillResponseImpl.new()

		if input.killed ~= nil then
			self.killed = input.killed
		end

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		return self
	end

	_KillResponseImpl.descriptor = {
		name = "KillResponse",
		fullName = "rodeo.KillResponse",
	}

	messages.KillResponse = _KillResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.KillResponse)
end

do
	local _DisconnectMsgImpl = {}
	_DisconnectMsgImpl.__index = _DisconnectMsgImpl

	function _DisconnectMsgImpl.new(data: _DisconnectMsgPartialFields?): DisconnectMsg
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
		}, _DisconnectMsgImpl :: _DisconnectMsgImpl)
	end

	function _DisconnectMsgImpl.encode(self: DisconnectMsg): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DisconnectMsgImpl.decode(input: buffer): DisconnectMsg
		local self = _DisconnectMsgImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DisconnectMsgImpl.jsonEncode(self: DisconnectMsg): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		return output
	end

	function _DisconnectMsgImpl.jsonDecode(input: { [string]: any }): DisconnectMsg
		local self = _DisconnectMsgImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		return self
	end

	_DisconnectMsgImpl.descriptor = {
		name = "DisconnectMsg",
		fullName = "rodeo.DisconnectMsg",
	}

	messages.DisconnectMsg = _DisconnectMsgImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DisconnectMsg)
end

do
	local _PluginMessageImpl = {}
	_PluginMessageImpl.__index = _PluginMessageImpl

	function _PluginMessageImpl.new(data: _PluginMessagePartialFields?): PluginMessage
		return setmetatable({
			msg = (function()
				if data == nil or data.msg == nil then
					return nil
				end
				local v = data.msg
				if
					(v :: any).type == "studioState"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.StudioStateMsg.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "rpc"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = _runtime.ClientRpcCall.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "done"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ExecutionDone.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "killed"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.ExecutionKilled.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _PluginMessageImpl :: _PluginMessageImpl)
	end

	function _PluginMessageImpl.encode(self: PluginMessage): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.msg ~= nil then
			if self.msg.type == "studioState" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "rpc" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "done" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "killed" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PluginMessageImpl.decode(input: buffer): PluginMessage
		local self = _PluginMessageImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "studioState", value = messages.StudioStateMsg.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "rpc", value = _runtime.ClientRpcCall.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "done", value = messages.ExecutionDone.decode(value) }
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "killed", value = messages.ExecutionKilled.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PluginMessageImpl.jsonEncode(self: PluginMessage): any
		local output = {}

		if self.msg ~= nil then
			if self.msg.type == "studioState" then
				output.studioState = self.msg.value:jsonEncode()
			elseif self.msg.type == "rpc" then
				output.rpc = self.msg.value:jsonEncode()
			elseif self.msg.type == "done" then
				output.done = self.msg.value:jsonEncode()
			elseif self.msg.type == "killed" then
				output.killed = self.msg.value:jsonEncode()
			end
		end

		return output
	end

	function _PluginMessageImpl.jsonDecode(input: { [string]: any }): PluginMessage
		local self = _PluginMessageImpl.new()

		if input.studioState ~= nil then
			self.msg = { type = "studioState", value = messages.StudioStateMsg.jsonDecode(input.studioState) }
		end

		if input.rpc ~= nil then
			self.msg = { type = "rpc", value = _runtime.ClientRpcCall.jsonDecode(input.rpc) }
		end

		if input.done ~= nil then
			self.msg = { type = "done", value = messages.ExecutionDone.jsonDecode(input.done) }
		end

		if input.killed ~= nil then
			self.msg = { type = "killed", value = messages.ExecutionKilled.jsonDecode(input.killed) }
		end

		return self
	end

	_PluginMessageImpl.descriptor = {
		name = "PluginMessage",
		fullName = "rodeo.PluginMessage",
	}

	messages.PluginMessage = _PluginMessageImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PluginMessage)
end

do
	local _ServerMessageImpl = {}
	_ServerMessageImpl.__index = _ServerMessageImpl

	function _ServerMessageImpl.new(data: _ServerMessagePartialFields?): ServerMessage
		return setmetatable({
			msg = (function()
				if data == nil or data.msg == nil then
					return nil
				end
				local v = data.msg
				if
					(v :: any).type == "welcome"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.WelcomeMsg.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "run"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.RunCommand.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "rpcResponse"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = _runtime.ClientRpcResponse.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "kill"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.KillCommand.new((v :: any).value) } :: any
				elseif
					(v :: any).type == "setTargetMode"
					and (v :: any).value ~= nil
					and getmetatable((v :: any).value) == nil
				then
					return { type = (v :: any).type, value = messages.SetTargetModeMsg.new((v :: any).value) } :: any
				end
				return v :: any
			end)(),
		}, _ServerMessageImpl :: _ServerMessageImpl)
	end

	function _ServerMessageImpl.encode(self: ServerMessage): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.msg ~= nil then
			if self.msg.type == "welcome" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "run" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "rpcResponse" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "kill" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.msg.type == "setTargetMode" then
				local encoded = self.msg.value:encode()
				output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ServerMessageImpl.decode(input: buffer): ServerMessage
		local self = _ServerMessageImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "welcome", value = messages.WelcomeMsg.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "run", value = messages.RunCommand.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "rpcResponse", value = _runtime.ClientRpcResponse.decode(value) }
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "kill", value = messages.KillCommand.decode(value) }
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.msg = { type = "setTargetMode", value = messages.SetTargetModeMsg.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ServerMessageImpl.jsonEncode(self: ServerMessage): any
		local output = {}

		if self.msg ~= nil then
			if self.msg.type == "welcome" then
				output.welcome = self.msg.value:jsonEncode()
			elseif self.msg.type == "run" then
				output.run = self.msg.value:jsonEncode()
			elseif self.msg.type == "rpcResponse" then
				output.rpcResponse = self.msg.value:jsonEncode()
			elseif self.msg.type == "kill" then
				output.kill = self.msg.value:jsonEncode()
			elseif self.msg.type == "setTargetMode" then
				output.setTargetMode = self.msg.value:jsonEncode()
			end
		end

		return output
	end

	function _ServerMessageImpl.jsonDecode(input: { [string]: any }): ServerMessage
		local self = _ServerMessageImpl.new()

		if input.welcome ~= nil then
			self.msg = { type = "welcome", value = messages.WelcomeMsg.jsonDecode(input.welcome) }
		end

		if input.run ~= nil then
			self.msg = { type = "run", value = messages.RunCommand.jsonDecode(input.run) }
		end

		if input.rpcResponse ~= nil then
			self.msg = { type = "rpcResponse", value = _runtime.ClientRpcResponse.jsonDecode(input.rpcResponse) }
		end

		if input.kill ~= nil then
			self.msg = { type = "kill", value = messages.KillCommand.jsonDecode(input.kill) }
		end

		if input.setTargetMode ~= nil then
			self.msg = { type = "setTargetMode", value = messages.SetTargetModeMsg.jsonDecode(input.setTargetMode) }
		end

		return self
	end

	_ServerMessageImpl.descriptor = {
		name = "ServerMessage",
		fullName = "rodeo.ServerMessage",
	}

	messages.ServerMessage = _ServerMessageImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ServerMessage)
end

do
	local _SetTargetModeMsgImpl = {}
	_SetTargetModeMsgImpl.__index = _SetTargetModeMsgImpl

	function _SetTargetModeMsgImpl.new(data: _SetTargetModeMsgPartialFields?): SetTargetModeMsg
		return setmetatable({
			targetMode = if data == nil or data.targetMode == nil then "" else data.targetMode,
		}, _SetTargetModeMsgImpl :: _SetTargetModeMsgImpl)
	end

	function _SetTargetModeMsgImpl.encode(self: SetTargetModeMsg): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.targetMode ~= nil and self.targetMode ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.targetMode)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SetTargetModeMsgImpl.decode(input: buffer): SetTargetModeMsg
		local self = _SetTargetModeMsgImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.targetMode = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SetTargetModeMsgImpl.jsonEncode(self: SetTargetModeMsg): any
		local output = {}

		if self.targetMode ~= nil and self.targetMode ~= "" then
			output.targetMode = self.targetMode
		end

		return output
	end

	function _SetTargetModeMsgImpl.jsonDecode(input: { [string]: any }): SetTargetModeMsg
		local self = _SetTargetModeMsgImpl.new()

		if input.targetMode ~= nil then
			self.targetMode = input.targetMode
		end

		return self
	end

	_SetTargetModeMsgImpl.descriptor = {
		name = "SetTargetModeMsg",
		fullName = "rodeo.SetTargetModeMsg",
	}

	messages.SetTargetModeMsg = _SetTargetModeMsgImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SetTargetModeMsg)
end

do
	local _StudioStateMsgImpl = {}
	_StudioStateMsgImpl.__index = _StudioStateMsgImpl

	function _StudioStateMsgImpl.new(data: _StudioStateMsgPartialFields?): StudioStateMsg
		return setmetatable({
			domId = if data == nil or data.domId == nil then "" else data.domId,
			mcpStudioId = if data == nil or data.mcpStudioId == nil then nil else data.mcpStudioId,
			isServer = if data == nil or data.isServer == nil then false else data.isServer,
			isClient = if data == nil or data.isClient == nil then false else data.isClient,
			isRunning = if data == nil or data.isRunning == nil then false else data.isRunning,
			isEdit = if data == nil or data.isEdit == nil then false else data.isEdit,
			mode = if data == nil or data.mode == nil then "" else data.mode,
			domKind = if data == nil or data.domKind == nil then "" else data.domKind,
			gameName = if data == nil or data.gameName == nil then "" else data.gameName,
			placeId = if data == nil or data.placeId == nil then 0 else data.placeId,
			gameId = if data == nil or data.gameId == nil then 0 else data.gameId,
			clientInfo = if data == nil or data.clientInfo == nil
				then nil
				elseif getmetatable(data.clientInfo :: any) == nil then messages.ClientInfo.new(
					data.clientInfo :: any
				)
				else (data.clientInfo :: any) :: ClientInfo,
			jobId = if data == nil or data.jobId == nil then nil else data.jobId,
			clientDomIdsJson = if data == nil or data.clientDomIdsJson == nil then nil else data.clientDomIdsJson,
			sessionGuid = if data == nil or data.sessionGuid == nil then nil else data.sessionGuid,
			studioId = if data == nil or data.studioId == nil then "" else data.studioId,
		}, _StudioStateMsgImpl :: _StudioStateMsgImpl)
	end

	function _StudioStateMsgImpl.encode(self: StudioStateMsg): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.domId ~= nil and self.domId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domId)
		end

		if self.mcpStudioId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mcpStudioId)
		end

		if self.isServer then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.isServer then 1 else 0)
		end

		if self.isClient then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.isClient then 1 else 0)
		end

		if self.isRunning then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.isRunning then 1 else 0)
		end

		if self.isEdit then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.isEdit then 1 else 0)
		end

		if self.mode ~= nil and self.mode ~= "" then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.mode)
		end

		if self.domKind ~= nil and self.domKind ~= "" then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.domKind)
		end

		if self.gameName ~= nil and self.gameName ~= "" then
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.gameName)
		end

		if self.placeId ~= nil and self.placeId ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.placeId)
		end

		if self.gameId ~= nil and self.gameId ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.gameId)
		end

		if self.clientInfo ~= nil then
			local encoded = self.clientInfo:encode()
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.jobId ~= nil then
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.jobId)
		end

		if self.clientDomIdsJson ~= nil then
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.clientDomIdsJson)
		end

		if self.sessionGuid ~= nil then
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionGuid)
		end

		if self.studioId ~= nil and self.studioId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.studioId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _StudioStateMsgImpl.decode(input: buffer): StudioStateMsg
		local self = _StudioStateMsgImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 3 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.isServer = value ~= 0
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.isClient = value ~= 0
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.isRunning = value ~= 0
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.isEdit = value ~= 0
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.placeId = value
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.gameId = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mcpStudioId = proto.decodeString(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.mode = proto.decodeString(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.domKind = proto.decodeString(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.gameName = proto.decodeString(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.clientInfo = messages.ClientInfo.decode(value)
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.jobId = proto.decodeString(value)
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.clientDomIdsJson = proto.decodeString(value)
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionGuid = proto.decodeString(value)
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.studioId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _StudioStateMsgImpl.jsonEncode(self: StudioStateMsg): any
		local output = {}

		if self.domId ~= nil and self.domId ~= "" then
			output.domId = self.domId
		end

		if self.mcpStudioId ~= nil then
			output.mcpStudioId = self.mcpStudioId
		end

		if self.isServer then
			output.isServer = self.isServer
		end

		if self.isClient then
			output.isClient = self.isClient
		end

		if self.isRunning then
			output.isRunning = self.isRunning
		end

		if self.isEdit then
			output.isEdit = self.isEdit
		end

		if self.mode ~= nil and self.mode ~= "" then
			output.mode = self.mode
		end

		if self.domKind ~= nil and self.domKind ~= "" then
			output.domKind = self.domKind
		end

		if self.gameName ~= nil and self.gameName ~= "" then
			output.gameName = self.gameName
		end

		if self.placeId ~= nil and self.placeId ~= 0 then
			output.placeId = self.placeId
		end

		if self.gameId ~= nil and self.gameId ~= 0 then
			output.gameId = self.gameId
		end

		if self.clientInfo ~= nil then
			output.clientInfo = self.clientInfo:jsonEncode()
		end

		if self.jobId ~= nil then
			output.jobId = self.jobId
		end

		if self.clientDomIdsJson ~= nil then
			output.clientDomIdsJson = self.clientDomIdsJson
		end

		if self.sessionGuid ~= nil then
			output.sessionGuid = self.sessionGuid
		end

		if self.studioId ~= nil and self.studioId ~= "" then
			output.studioId = self.studioId
		end

		return output
	end

	function _StudioStateMsgImpl.jsonDecode(input: { [string]: any }): StudioStateMsg
		local self = _StudioStateMsgImpl.new()

		if input.domId ~= nil then
			self.domId = input.domId
		end

		if input.mcpStudioId ~= nil then
			self.mcpStudioId = input.mcpStudioId
		end

		if input.isServer ~= nil then
			self.isServer = input.isServer
		end

		if input.isClient ~= nil then
			self.isClient = input.isClient
		end

		if input.isRunning ~= nil then
			self.isRunning = input.isRunning
		end

		if input.isEdit ~= nil then
			self.isEdit = input.isEdit
		end

		if input.mode ~= nil then
			self.mode = input.mode
		end

		if input.domKind ~= nil then
			self.domKind = input.domKind
		end

		if input.gameName ~= nil then
			self.gameName = input.gameName
		end

		if input.placeId ~= nil then
			self.placeId = input.placeId
		end

		if input.gameId ~= nil then
			self.gameId = input.gameId
		end

		if input.clientInfo ~= nil then
			self.clientInfo = messages.ClientInfo.jsonDecode(input.clientInfo)
		end

		if input.jobId ~= nil then
			self.jobId = input.jobId
		end

		if input.clientDomIdsJson ~= nil then
			self.clientDomIdsJson = input.clientDomIdsJson
		end

		if input.sessionGuid ~= nil then
			self.sessionGuid = input.sessionGuid
		end

		if input.studioId ~= nil then
			self.studioId = input.studioId
		end

		return self
	end

	_StudioStateMsgImpl.descriptor = {
		name = "StudioStateMsg",
		fullName = "rodeo.StudioStateMsg",
	}

	messages.StudioStateMsg = _StudioStateMsgImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.StudioStateMsg)
end

do
	local _ClientInfoImpl = {}
	_ClientInfoImpl.__index = _ClientInfoImpl

	function _ClientInfoImpl.new(data: _ClientInfoPartialFields?): ClientInfo
		return setmetatable({
			name = if data == nil or data.name == nil then "" else data.name,
			userId = if data == nil or data.userId == nil then 0 else data.userId,
		}, _ClientInfoImpl :: _ClientInfoImpl)
	end

	function _ClientInfoImpl.encode(self: ClientInfo): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.userId ~= nil and self.userId ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.userId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ClientInfoImpl.decode(input: buffer): ClientInfo
		local self = _ClientInfoImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.userId = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ClientInfoImpl.jsonEncode(self: ClientInfo): any
		local output = {}

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.userId ~= nil and self.userId ~= 0 then
			output.userId = self.userId
		end

		return output
	end

	function _ClientInfoImpl.jsonDecode(input: { [string]: any }): ClientInfo
		local self = _ClientInfoImpl.new()

		if input.name ~= nil then
			self.name = input.name
		end

		if input.userId ~= nil then
			self.userId = input.userId
		end

		return self
	end

	_ClientInfoImpl.descriptor = {
		name = "ClientInfo",
		fullName = "rodeo.ClientInfo",
	}

	messages.ClientInfo = _ClientInfoImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ClientInfo)
end

do
	local _WelcomeMsgImpl = {}
	_WelcomeMsgImpl.__index = _WelcomeMsgImpl

	function _WelcomeMsgImpl.new(data: _WelcomeMsgPartialFields?): WelcomeMsg
		return setmetatable({
			version = if data == nil or data.version == nil then "" else data.version,
		}, _WelcomeMsgImpl :: _WelcomeMsgImpl)
	end

	function _WelcomeMsgImpl.encode(self: WelcomeMsg): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.version ~= nil and self.version ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.version)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _WelcomeMsgImpl.decode(input: buffer): WelcomeMsg
		local self = _WelcomeMsgImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.version = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _WelcomeMsgImpl.jsonEncode(self: WelcomeMsg): any
		local output = {}

		if self.version ~= nil and self.version ~= "" then
			output.version = self.version
		end

		return output
	end

	function _WelcomeMsgImpl.jsonDecode(input: { [string]: any }): WelcomeMsg
		local self = _WelcomeMsgImpl.new()

		if input.version ~= nil then
			self.version = input.version
		end

		return self
	end

	_WelcomeMsgImpl.descriptor = {
		name = "WelcomeMsg",
		fullName = "rodeo.WelcomeMsg",
	}

	messages.WelcomeMsg = _WelcomeMsgImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.WelcomeMsg)
end

do
	local _RunCommandImpl = {}
	_RunCommandImpl.__index = _RunCommandImpl

	function _RunCommandImpl.new(data: _RunCommandPartialFields?): RunCommand
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
			script = if data == nil or data.script == nil then "" else data.script,
			context = if data == nil or data.context == nil then "" else data.context,
			cacheRequires = if data == nil or data.cacheRequires == nil then nil else data.cacheRequires,
			scriptArgs = if data == nil or data.scriptArgs == nil then {} else data.scriptArgs,
			returnFile = if data == nil or data.returnFile == nil then nil else data.returnFile,
			showReturn = if data == nil or data.showReturn == nil then nil else data.showReturn,
			outputFile = if data == nil or data.outputFile == nil then nil else data.outputFile,
			verbose = if data == nil or data.verbose == nil then nil else data.verbose,
			instancePath = if data == nil or data.instancePath == nil then nil else data.instancePath,
			scriptPath = if data == nil or data.scriptPath == nil then nil else data.scriptPath,
			profile = if data == nil or data.profile == nil then nil else data.profile,
			logFilter = if data == nil or data.logFilter == nil
				then nil
				elseif getmetatable(data.logFilter :: any) == nil then messages.LogFilter.new(
					data.logFilter :: any
				)
				else (data.logFilter :: any) :: LogFilter,
		}, _RunCommandImpl :: _RunCommandImpl)
	end

	function _RunCommandImpl.encode(self: RunCommand): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		if self.script ~= nil and self.script ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.script)
		end

		if self.context ~= nil and self.context ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.context)
		end

		if self.cacheRequires ~= nil then
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.cacheRequires then 1 else 0)
		end

		if self.scriptArgs ~= nil and #self.scriptArgs > 0 then
			for _, value in self.scriptArgs do
				output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.returnFile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.returnFile)
		end

		if self.showReturn ~= nil then
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.showReturn then 1 else 0)
		end

		if self.outputFile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.outputFile)
		end

		if self.verbose ~= nil then
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.verbose then 1 else 0)
		end

		if self.instancePath ~= nil then
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.instancePath)
		end

		if self.scriptPath ~= nil then
			output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.scriptPath)
		end

		if self.profile ~= nil then
			output, cursor = proto.writeTag(output, cursor, 18, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.profile then 1 else 0)
		end

		if self.logFilter ~= nil then
			local encoded = self.logFilter:encode()
			output, cursor = proto.writeTag(output, cursor, 19, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _RunCommandImpl.decode(input: buffer): RunCommand
		local self = _RunCommandImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 9 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.cacheRequires = value ~= 0
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.showReturn = value ~= 0
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.verbose = value ~= 0
					continue
				elseif field == 18 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.profile = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.script = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.context = proto.decodeString(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.scriptArgs, proto.decodeString(value))
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.returnFile = proto.decodeString(value)
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.outputFile = proto.decodeString(value)
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.instancePath = proto.decodeString(value)
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.scriptPath = proto.decodeString(value)
					continue
				elseif field == 19 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.logFilter = messages.LogFilter.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _RunCommandImpl.jsonEncode(self: RunCommand): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		if self.script ~= nil and self.script ~= "" then
			output.script = self.script
		end

		if self.context ~= nil and self.context ~= "" then
			output.context = self.context
		end

		if self.cacheRequires ~= nil then
			output.cacheRequires = self.cacheRequires
		end

		if self.scriptArgs ~= nil and #self.scriptArgs > 0 then
			local newOutput = {}
			for _, value in self.scriptArgs do
				table.insert(newOutput, value)
			end
			output.scriptArgs = newOutput
		end

		if self.returnFile ~= nil then
			output.returnFile = self.returnFile
		end

		if self.showReturn ~= nil then
			output.showReturn = self.showReturn
		end

		if self.outputFile ~= nil then
			output.outputFile = self.outputFile
		end

		if self.verbose ~= nil then
			output.verbose = self.verbose
		end

		if self.instancePath ~= nil then
			output.instancePath = self.instancePath
		end

		if self.scriptPath ~= nil then
			output.scriptPath = self.scriptPath
		end

		if self.profile ~= nil then
			output.profile = self.profile
		end

		if self.logFilter ~= nil then
			output.logFilter = self.logFilter:jsonEncode()
		end

		return output
	end

	function _RunCommandImpl.jsonDecode(input: { [string]: any }): RunCommand
		local self = _RunCommandImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		if input.script ~= nil then
			self.script = input.script
		end

		if input.context ~= nil then
			self.context = input.context
		end

		if input.cacheRequires ~= nil then
			self.cacheRequires = input.cacheRequires
		end

		if input.scriptArgs ~= nil then
			local newOutput: { string } = {}
			for _, value in input.scriptArgs do
				table.insert(newOutput, value)
			end

			self.scriptArgs = newOutput
		end

		if input.returnFile ~= nil then
			self.returnFile = input.returnFile
		end

		if input.showReturn ~= nil then
			self.showReturn = input.showReturn
		end

		if input.outputFile ~= nil then
			self.outputFile = input.outputFile
		end

		if input.verbose ~= nil then
			self.verbose = input.verbose
		end

		if input.instancePath ~= nil then
			self.instancePath = input.instancePath
		end

		if input.scriptPath ~= nil then
			self.scriptPath = input.scriptPath
		end

		if input.profile ~= nil then
			self.profile = input.profile
		end

		if input.logFilter ~= nil then
			self.logFilter = messages.LogFilter.jsonDecode(input.logFilter)
		end

		return self
	end

	_RunCommandImpl.descriptor = {
		name = "RunCommand",
		fullName = "rodeo.RunCommand",
	}

	messages.RunCommand = _RunCommandImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.RunCommand)
end

do
	local _KillCommandImpl = {}
	_KillCommandImpl.__index = _KillCommandImpl

	function _KillCommandImpl.new(data: _KillCommandPartialFields?): KillCommand
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
		}, _KillCommandImpl :: _KillCommandImpl)
	end

	function _KillCommandImpl.encode(self: KillCommand): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _KillCommandImpl.decode(input: buffer): KillCommand
		local self = _KillCommandImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _KillCommandImpl.jsonEncode(self: KillCommand): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		return output
	end

	function _KillCommandImpl.jsonDecode(input: { [string]: any }): KillCommand
		local self = _KillCommandImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		return self
	end

	_KillCommandImpl.descriptor = {
		name = "KillCommand",
		fullName = "rodeo.KillCommand",
	}

	messages.KillCommand = _KillCommandImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.KillCommand)
end

do
	local _ExecutionDoneImpl = {}
	_ExecutionDoneImpl.__index = _ExecutionDoneImpl

	function _ExecutionDoneImpl.new(data: _ExecutionDonePartialFields?): ExecutionDone
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
			success = if data == nil or data.success == nil then false else data.success,
			error = if data == nil or data.error == nil then nil else data.error,
			returnValue = if data == nil or data.returnValue == nil then nil else data.returnValue,
		}, _ExecutionDoneImpl :: _ExecutionDoneImpl)
	end

	function _ExecutionDoneImpl.encode(self: ExecutionDone): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		if self.success then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.success then 1 else 0)
		end

		if self.error ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.error)
		end

		if self.returnValue ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.returnValue)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ExecutionDoneImpl.decode(input: buffer): ExecutionDone
		local self = _ExecutionDoneImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.success = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.error = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.returnValue = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ExecutionDoneImpl.jsonEncode(self: ExecutionDone): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		if self.success then
			output.success = self.success
		end

		if self.error ~= nil then
			output.error = self.error
		end

		if self.returnValue ~= nil then
			output.returnValue = self.returnValue
		end

		return output
	end

	function _ExecutionDoneImpl.jsonDecode(input: { [string]: any }): ExecutionDone
		local self = _ExecutionDoneImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		if input.success ~= nil then
			self.success = input.success
		end

		if input.error ~= nil then
			self.error = input.error
		end

		if input.returnValue ~= nil then
			self.returnValue = input.returnValue
		end

		return self
	end

	_ExecutionDoneImpl.descriptor = {
		name = "ExecutionDone",
		fullName = "rodeo.ExecutionDone",
	}

	messages.ExecutionDone = _ExecutionDoneImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ExecutionDone)
end

do
	local _ExecutionKilledImpl = {}
	_ExecutionKilledImpl.__index = _ExecutionKilledImpl

	function _ExecutionKilledImpl.new(data: _ExecutionKilledPartialFields?): ExecutionKilled
		return setmetatable({
			executionId = if data == nil or data.executionId == nil then "" else data.executionId,
		}, _ExecutionKilledImpl :: _ExecutionKilledImpl)
	end

	function _ExecutionKilledImpl.encode(self: ExecutionKilled): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.executionId ~= nil and self.executionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.executionId)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ExecutionKilledImpl.decode(input: buffer): ExecutionKilled
		local self = _ExecutionKilledImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.executionId = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ExecutionKilledImpl.jsonEncode(self: ExecutionKilled): any
		local output = {}

		if self.executionId ~= nil and self.executionId ~= "" then
			output.executionId = self.executionId
		end

		return output
	end

	function _ExecutionKilledImpl.jsonDecode(input: { [string]: any }): ExecutionKilled
		local self = _ExecutionKilledImpl.new()

		if input.executionId ~= nil then
			self.executionId = input.executionId
		end

		return self
	end

	_ExecutionKilledImpl.descriptor = {
		name = "ExecutionKilled",
		fullName = "rodeo.ExecutionKilled",
	}

	messages.ExecutionKilled = _ExecutionKilledImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ExecutionKilled)
end

messages.ProcessState = {
	fromNumber = function(value: number): ProcessState?
		if value == 0 then
			return "PROCESS_STATE_QUEUED"
		elseif value == 1 then
			return "PROCESS_STATE_RUNNING"
		elseif value == 2 then
			return "PROCESS_STATE_DONE"
		elseif value == 3 then
			return "PROCESS_STATE_ERROR"
		elseif value == 4 then
			return "PROCESS_STATE_KILLED"
		elseif value == 5 then
			return "PROCESS_STATE_DISCONNECTED"
		else
			return nil
		end
	end,

	toNumber = function(self: ProcessState): number
		if self == "PROCESS_STATE_QUEUED" then
			return 0
		elseif self == "PROCESS_STATE_RUNNING" then
			return 1
		elseif self == "PROCESS_STATE_DONE" then
			return 2
		elseif self == "PROCESS_STATE_ERROR" then
			return 3
		elseif self == "PROCESS_STATE_KILLED" then
			return 4
		elseif self == "PROCESS_STATE_DISCONNECTED" then
			return 5
		else
			return self
		end
	end,

	fromName = function(name: string): ProcessState?
		if name == "PROCESS_STATE_QUEUED" then
			return "PROCESS_STATE_QUEUED"
		elseif name == "PROCESS_STATE_RUNNING" then
			return "PROCESS_STATE_RUNNING"
		elseif name == "PROCESS_STATE_DONE" then
			return "PROCESS_STATE_DONE"
		elseif name == "PROCESS_STATE_ERROR" then
			return "PROCESS_STATE_ERROR"
		elseif name == "PROCESS_STATE_KILLED" then
			return "PROCESS_STATE_KILLED"
		elseif name == "PROCESS_STATE_DISCONNECTED" then
			return "PROCESS_STATE_DISCONNECTED"
		else
			return nil
		end
	end,

	variants = {
		PROCESS_STATE_QUEUED = "PROCESS_STATE_QUEUED" :: "PROCESS_STATE_QUEUED",
		PROCESS_STATE_RUNNING = "PROCESS_STATE_RUNNING" :: "PROCESS_STATE_RUNNING",
		PROCESS_STATE_DONE = "PROCESS_STATE_DONE" :: "PROCESS_STATE_DONE",
		PROCESS_STATE_ERROR = "PROCESS_STATE_ERROR" :: "PROCESS_STATE_ERROR",
		PROCESS_STATE_KILLED = "PROCESS_STATE_KILLED" :: "PROCESS_STATE_KILLED",
		PROCESS_STATE_DISCONNECTED = "PROCESS_STATE_DISCONNECTED" :: "PROCESS_STATE_DISCONNECTED",
	},
}

return {
	BackendMessage = messages.BackendMessage,
	FileChunk = messages.FileChunk,
	FileAck = messages.FileAck,
	FilesComplete = messages.FilesComplete,
	RegisterRequest = messages.RegisterRequest,
	DomConnect = messages.DomConnect,
	DomDisconnect = messages.DomDisconnect,
	DomPluginMessage = messages.DomPluginMessage,
	DomServerMessage = messages.DomServerMessage,
	StateSnapshot = messages.StateSnapshot,
	StudioInstanceState = messages.StudioInstanceState,
	DomSnapshot = messages.DomSnapshot,
	StudioState = messages.StudioState,
	StudioDom = messages.StudioDom,
	StudioSnapshot = messages.StudioSnapshot,
	PlayerStatus = messages.PlayerStatus,
	MasterMessage = messages.MasterMessage,
	CloseStudioCommand = messages.CloseStudioCommand,
	RunCompleted = messages.RunCompleted,
	RegisterResponse = messages.RegisterResponse,
	SaveCommand = messages.SaveCommand,
	SaveResult = messages.SaveResult,
	LaunchStudioCommand = messages.LaunchStudioCommand,
	SessionExited = messages.SessionExited,
	RunClientMessage = messages.RunClientMessage,
	SubmitRequest = messages.SubmitRequest,
	RunEvent = messages.RunEvent,
	RunComplete = messages.RunComplete,
	ProcessCreated = messages.ProcessCreated,
	HealthRequest = messages.HealthRequest,
	GetStateRequest = messages.GetStateRequest,
	ListProcessesRequest = messages.ListProcessesRequest,
	KillProcessRequest = messages.KillProcessRequest,
	LaunchStudioRequest = messages.LaunchStudioRequest,
	LaunchStudioEvent = messages.LaunchStudioEvent,
	StudioLaunching = messages.StudioLaunching,
	StudioReady = messages.StudioReady,
	StudioLaunchError = messages.StudioLaunchError,
	SetStudioModeRequest = messages.SetStudioModeRequest,
	SetStudioModeResponse = messages.SetStudioModeResponse,
	CloseStudioRequest = messages.CloseStudioRequest,
	CloseStudioEvent = messages.CloseStudioEvent,
	StudioClosing = messages.StudioClosing,
	StudioClosed = messages.StudioClosed,
	SavePlaceRequest = messages.SavePlaceRequest,
	SavePlaceResponse = messages.SavePlaceResponse,
	ListBackendsRequest = messages.ListBackendsRequest,
	ListBackendsResponse = messages.ListBackendsResponse,
	BackendInfo = messages.BackendInfo,
	RodeoSnapshot = messages.RodeoSnapshot,
	LogFilter = messages.LogFilter,
	HealthResponse = messages.HealthResponse,
	ContextInfo = messages.ContextInfo,
	DomInfo = messages.DomInfo,
	ProcessInfo = messages.ProcessInfo,
	ProcessListResponse = messages.ProcessListResponse,
	KillResponse = messages.KillResponse,
	DisconnectMsg = messages.DisconnectMsg,
	PluginMessage = messages.PluginMessage,
	ServerMessage = messages.ServerMessage,
	SetTargetModeMsg = messages.SetTargetModeMsg,
	StudioStateMsg = messages.StudioStateMsg,
	ClientInfo = messages.ClientInfo,
	WelcomeMsg = messages.WelcomeMsg,
	RunCommand = messages.RunCommand,
	KillCommand = messages.KillCommand,
	ExecutionDone = messages.ExecutionDone,
	ExecutionKilled = messages.ExecutionKilled,
	ProcessState = messages.ProcessState,
}
